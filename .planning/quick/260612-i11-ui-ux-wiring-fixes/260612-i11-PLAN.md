---
phase: quick-260612-i11-ui-ux-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/batches/[id]/page.tsx
  - app/(app)/batches/[id]/segment-switcher.tsx
  - app/(app)/batches/[id]/gallery/page.tsx
  - app/(app)/batches/[id]/compositing/page.tsx
  - app/(app)/batches/[id]/intel-panel.tsx
  - app/(app)/batches/page.tsx
  - app/(app)/products/[id]/page.tsx
  - app/(app)/products/[id]/batches/new/page.tsx
  - app/(app)/products/new/page.tsx
  - app/(app)/products/page.tsx
  - app/(app)/products/product-card.tsx
  - app/(app)/admin/users/page.tsx
  - app/(app)/admin/settings/page.tsx
  - app/(auth)/login/page.tsx
  - app/(app)/not-found.tsx
  - app/not-found.tsx
  - app/(app)/error.tsx
  - app/layout.tsx
  - app/components/theme-provider.tsx
  - app/components/app-shell/theme-toggle.tsx
  - app/components/app-shell/page-breadcrumb.tsx
  - lib/format.ts
autonomous: true
requirements: [UX-AUDIT-A, UX-AUDIT-B, UX-AUDIT-C]

must_haves:
  truths:
    - "Operator can switch between Monitor / Gallery / Compositing from all three batch detail pages via the SegmentSwitcher"
    - "A terminal batch with completed jobs shows a primary 'View outputs' action linking to its gallery"
    - "Product detail page lists the product's 5 most recent batches with status pills, each linking to /batches/[id]"
    - "notFound() on a missing product renders a branded 404 inside the app shell; unmatched root URLs render a branded standalone 404"
    - "Error-card 'retry' actions reload the SAME page (never silently navigate to the batches list)"
    - "Theme toggle persists across page reloads (next-themes localStorage), no hydration warning"
    - "Browser tab titles read '{Page} · Jewelry Render Studio' on every surface"
    - "Dates render via one shared lib/format.ts (relativeTime + formatDateTime) — zero duplicated relativeTime functions"
    - "Gallery and compositing empty states match the canonical dashed empty-state pattern from the products list"
    - "Detail pages show a breadcrumb trail above the h1"
  artifacts:
    - path: "lib/format.ts"
      provides: "relativeTime() + formatDateTime() shared date formatting"
      exports: ["relativeTime", "formatDateTime"]
    - path: "app/(app)/not-found.tsx"
      provides: "In-shell 404 for notFound() under (app)"
    - path: "app/not-found.tsx"
      provides: "Standalone root 404 matching login styling"
    - path: "app/(app)/error.tsx"
      provides: "Client error boundary with reset()"
      contains: "\"use client\""
    - path: "app/components/theme-provider.tsx"
      provides: "next-themes client wrapper"
      contains: "next-themes"
    - path: "app/components/app-shell/page-breadcrumb.tsx"
      provides: "PageBreadcrumb server component"
      exports: ["PageBreadcrumb"]
  key_links:
    - from: "app/(app)/batches/[id]/page.tsx"
      to: "./segment-switcher"
      via: "SegmentSwitcher active=\"monitor\""
      pattern: "SegmentSwitcher.*active=\"monitor\""
    - from: "app/(app)/batches/[id]/gallery/page.tsx"
      to: "../segment-switcher"
      via: "SegmentSwitcher active=\"gallery\""
      pattern: "SegmentSwitcher.*active=\"gallery\""
    - from: "app/(app)/products/[id]/page.tsx"
      to: "prisma.batch"
      via: "Recent batches findMany"
      pattern: "prisma\\.batch\\.findMany"
    - from: "app/layout.tsx"
      to: "next-themes"
      via: "ThemeProvider attribute=\"class\""
      pattern: "ThemeProvider"
    - from: "app/(app)/products/product-card.tsx"
      to: "lib/format.ts"
      via: "relativeTime import"
      pattern: "from \"@/lib/format\""
---

<objective>
UI/UX + screen-wiring fix pass across the Jewelry Render Studio app, executing a COMPLETED UX audit verbatim (12 findings in 3 batches). Batch A wires dead-end screens together (segment switcher everywhere, outputs CTA, product→batches section, 404 pages, honest retry links). Batch B makes theme persist via the already-installed next-themes and fixes app-wide metadata + adds a global error boundary. Batch C consolidates date formatting, restructures the gallery header, normalizes empty states, and adds breadcrumbs.

Purpose: the app is functionally complete (9/9 phases) but screens are poorly wired together — operators hit dead ends, mislabeled links, a non-persisting theme, and inconsistent chrome.
Output: 3 atomic commits (one per batch), all on UI files only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/(app)/batches/[id]/page.tsx
@app/(app)/batches/[id]/segment-switcher.tsx
@app/(app)/batches/[id]/gallery/page.tsx
@app/(app)/batches/[id]/compositing/page.tsx
@app/(app)/products/[id]/page.tsx
@app/(app)/products/page.tsx
@app/(app)/batches/page.tsx
@app/layout.tsx
@app/components/app-shell/theme-toggle.tsx

**HARD SCOPE BOUNDARY (PR is UI-only):** do NOT touch `workers/`, `public/worker-code/`, `lib/orchestration/` dispatch logic, or `prisma/schema.prisma`. The worktree has PRE-EXISTING unrelated modifications (`public/worker-code/render_scene.py`, `workers/runpod-blender/render_scene.py`, `test/intel-webhook-hook.test.ts`, untracked `master.png`, `tmp-judge.ts`, `vitest-out3.txt`) — NEVER `git add -A`; stage only the explicit file paths listed per task.

**Design constraints:** tokens only (globals.css teal/zinc semantic palette — `border-border`, `bg-card`, `text-muted-foreground`, `bg-muted`, etc.). NO purple, no hardcoded hex. Server components by default; `"use client"` only where state/hooks are needed. Match the UI-SPEC comment style at file tops (existing files show the pattern). A Phase-7 guard test asserts no raw amber-/sky-/emerald- palette classes — use semantic `success`/`warning`/`info`/`destructive` tokens only.

<interfaces>
<!-- Verified contracts — use directly, no exploration needed. -->

From app/(app)/batches/status-pill.tsx (import relatively, e.g. `../../batches/status-pill` from products/[id]):
```typescript
export function BatchStatusPill({ status }: { status: BatchStatus }): JSX.Element
// BatchStatus = "queued" | "running" | "completed" | "partly failed" | "failed" | "cancelling" | "cancelled"
```

From @/lib/orchestration/batch-status:
```typescript
export function summarizeJobs(groups: { status: JobStatus; _count: number }[]): BatchProgress
export function deriveBatchStatus(progress: BatchProgress, cancelRequestedAt?: Date | null): BatchStatus
// BatchProgress has .total, .completed, .failed numeric fields
```

From app/(app)/batches/[id]/segment-switcher.tsx ("use client"):
```typescript
export function SegmentSwitcher({ batchId, active }: { batchId: string; active: "monitor" | "gallery" | "compositing" })
```

Tally pattern (loadBatch in batches/[id]/page.tsx lines 158-166 — reuse verbatim for the product page):
```typescript
const tally = new Map<string, number>();
for (const j of batch.jobs) tally.set(j.status, (tally.get(j.status) ?? 0) + 1);
const progress = summarizeJobs([...tally.entries()].map(([status, count]) => ({ status: status as JobStatus, _count: count })));
const status = deriveBatchStatus(progress, batch.cancelRequestedAt);
```

Canonical empty-state pattern (products/page.tsx lines 66-80 — the target style for Batch C item 11):
```tsx
<div className="flex flex-col items-center justify-center gap-3 rounded-lg border border-dashed border-border py-16 text-center">
  <div className="flex size-10 items-center justify-center rounded-full bg-muted text-muted-foreground">
    <Icon className="size-5" strokeWidth={1.75} aria-hidden />
  </div>
  <div className="flex flex-col gap-1">
    <p className="text-sm font-semibold text-foreground">{heading}</p>
    <p className="max-w-sm text-sm text-muted-foreground">{body}</p>
  </div>
  <Button asChild className="mt-2">…CTA…</Button>
</div>
```

Error-card pattern (the calm card style for not-found/error pages):
`rounded-lg border border-border bg-card p-6` + `text-sm text-foreground` body + `Button variant="secondary" className="mt-4"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch A — screen wiring (segments, outputs CTA, product batches, 404s, retry links)</name>
  <files>app/(app)/batches/[id]/page.tsx, app/(app)/batches/[id]/segment-switcher.tsx, app/(app)/batches/[id]/gallery/page.tsx, app/(app)/batches/[id]/compositing/page.tsx, app/(app)/products/[id]/page.tsx, app/(app)/not-found.tsx, app/not-found.tsx, lib/format.ts</files>
  <action>
Execute audit findings 1-5 exactly:

**A1 — SegmentSwitcher on all three batch pages.** In app/(app)/batches/[id]/page.tsx import SegmentSwitcher from "./segment-switcher" and render `<SegmentSwitcher batchId={...} active="monitor" />` as the FIRST child of the top-level `flex flex-col gap-8` wrapper, before `<header>` — exactly matching the compositing page's placement (compositing/page.tsx line 116). Also render it on the not-found error branch (line 66-78) using `id` from params, as compositing does on its error branch. Do the same in gallery/page.tsx (import from "../segment-switcher", `active="gallery"`, first child of the wrapper at line 71 and on the error branch). In segment-switcher.tsx update the stale comment block (lines 15-16): replace the "wiring it onto the monitor/gallery pages is a nice-to-have left for a follow-up" sentence with a note that the switcher renders on all three batch surfaces (monitor, gallery, compositing).

**A2 — "View outputs" on terminal batch.** In batches/[id]/page.tsx header actions div (line 103-113): when `cancelable === 0 && batch.progress.completed > 0`, render a primary `<Button size="sm" asChild>` with `<Link href={`/batches/${batch.id}/gallery`}>` containing an `Images` icon (lucide-react, `className="size-4"`) + label "View outputs". Place it BEFORE the "Rebuild from product" conditional. Keep "Rebuild from product" and CancelBatchControl exactly as-is.

**A3 — Recent batches on product detail.** Create lib/format.ts FIRST (pulled forward from Batch C because this section needs it): export `relativeTime(value: string | number | Date): string` — the exact "just now/Nm ago/Nh ago/Nd ago/Nmo ago/Ny ago" implementation currently duplicated in product-card.tsx lines 35-49 — and `formatDateTime(value: string | number | Date): string` — deterministic "12 Jun 2026, 14:05" via `Intl.DateTimeFormat("en-GB", { day: "2-digit", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit", hour12: false, timeZone: "UTC" })` (explicit locale + options + UTC so server and client always render identical strings; module-level formatter instance, formatted as `"{date}, {time}"` — use formatToParts or two formatters if the literal separator needs control). Then in app/(app)/products/[id]/page.tsx add a "Recent batches" section AFTER the closing `</Tabs>` (line 172): query `prisma.batch.findMany({ where: { productId: id }, orderBy: { createdAt: "desc" }, take: 5, include: { jobs: { select: { status: true } } } })`; per batch derive status via the tally→summarizeJobs→deriveBatchStatus pattern from the interfaces block (import summarizeJobs/deriveBatchStatus from "@/lib/orchestration/batch-status", BatchStatusPill from "../../batches/status-pill"). When the query returns ZERO rows, omit the section entirely (render nothing). Otherwise render: an h2 "Recent batches" (text-base font-semibold text-foreground) with a "View all batches" Link to /batches (text-sm text-muted-foreground hover:text-foreground) on the same row; then one Link row per batch to `/batches/${b.id}` styled like a compact card row (rounded-lg border border-border bg-card px-4 py-3, hover border-foreground/20, focus-visible ring) containing BatchStatusPill, a mono text-xs job-count string (`${progress.total} jobs`), and `relativeTime(b.createdAt)` in mono text-xs tabular-nums muted.

**A4 — 404 pages.** Create app/(app)/not-found.tsx: server component (no "use client") rendered inside the (app) shell automatically; calm card per the error-card pattern: heading "Page not found" (text-base font-semibold), body "This page doesn't exist or was removed." (text-sm text-muted-foreground), then a flex gap-2 row with primary `<Button asChild><Link href="/products">Go to products</Link></Button>` and `<Button variant="secondary" asChild><Link href="/batches">View batches</Link></Button>`. Create app/not-found.tsx: standalone root 404 matching login-page styling (login/page.tsx lines 27-49): `min-h-screen items-center justify-center bg-background` main, centered "Jewelry Render Studio" wordmark span (same classes as login line 30), "This page doesn't exist or was removed." muted line, primary Button asChild Link to "/" labeled "Go home". Both files get a brief UI-SPEC-style top comment.

**A5 — honest retry links (3 files).** Replace the single mislabeled `Button variant="secondary"` "Retry"→/batches in each error card with a flex gap-2 row of two buttons: primary `<Button asChild><Link href={SAME_PAGE_URL}>Try again</Link></Button>` + `<Button variant="secondary" asChild><Link href="/batches">Back to batches</Link></Button>`. SAME_PAGE_URL per file (the `id` from params is in scope in all three): batches/[id]/page.tsx lines 72-74 → `/batches/${id}`; gallery/page.tsx lines 41-44 → `/batches/${id}/gallery`; compositing/page.tsx lines 59-61 → `/batches/${id}/compositing`. Do NOT touch the self-linking Retry buttons on the products/batches LIST pages (those already reload their own page).

Commit ONLY these 8 files: `git add` each path explicitly, then commit `feat(ui): wire batch segments, outputs CTA, product batches, 404 pages, honest retry links`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "SegmentSwitcher" "app/(app)/batches/[id]/page.tsx" && grep -c "SegmentSwitcher" "app/(app)/batches/[id]/gallery/page.tsx" && grep -c "prisma.batch.findMany" "app/(app)/products/[id]/page.tsx" && grep -c "View outputs" "app/(app)/batches/[id]/page.tsx" && grep -rc "Try again" "app/(app)/batches/[id]" && test -f "app/(app)/not-found.tsx" && test -f "app/not-found.tsx" && test -f "lib/format.ts"</automated>
  </verify>
  <done>tsc clean; SegmentSwitcher imported+rendered on monitor and gallery pages; product detail queries its batches and renders the section only when non-empty; terminal-with-outputs batch shows "View outputs"→gallery; all three error cards link "Try again" to their own URL + "Back to batches" secondary; both not-found files exist; lib/format.ts exports relativeTime + formatDateTime; commit 1 contains exactly the 8 listed files.</done>
</task>

<task type="auto">
  <name>Task 2: Batch B — next-themes persistence, app-wide metadata, global error boundary</name>
  <files>app/layout.tsx, app/components/theme-provider.tsx, app/components/app-shell/theme-toggle.tsx, app/(app)/error.tsx, app/(app)/products/page.tsx, app/(app)/products/[id]/page.tsx, app/(app)/products/new/page.tsx, app/(app)/batches/page.tsx, app/(app)/batches/[id]/page.tsx, app/(app)/batches/[id]/gallery/page.tsx, app/(app)/batches/[id]/compositing/page.tsx, app/(app)/products/[id]/batches/new/page.tsx, app/(app)/admin/users/page.tsx, app/(app)/admin/settings/page.tsx, app/(auth)/login/page.tsx</files>
  <action>
Execute audit findings 6-8 exactly:

**B6 — theme persistence.** Create app/components/theme-provider.tsx: `"use client"` wrapper re-exporting next-themes' provider — `export function ThemeProvider({ children, ...props }: React.ComponentProps<typeof NextThemesProvider>)` returning `<NextThemesProvider {...props}>{children}</NextThemesProvider>` (next-themes@^0.4.6 is already in package.json — install NOTHING). In app/layout.tsx: remove the hardcoded `"dark"` from the html className (keep GeistSans/GeistMono variables + cn), add `suppressHydrationWarning` to `<html>`, and wrap the body children in `<ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false} disableTransitionOnChange>`. Update the layout's "Dark is the default" comment to say dark is the default via next-themes with persistence. Rewrite app/components/app-shell/theme-toggle.tsx to use `useTheme()` from "next-themes": `const { resolvedTheme, setTheme } = useTheme()` plus a `mounted` state set in useEffect; until mounted render the SAME Button markup with a static Sun icon and aria-label "Toggle theme" (identical SSR/CSR markup → no hydration mismatch); once mounted, `isDark = resolvedTheme === "dark"`, icon Sun when dark / Moon when light (current semantics), aria-labels "Switch to light theme"/"Switch to dark theme", onClick `setTheme(isDark ? "light" : "dark")`. Drop all direct `document.documentElement.classList` manipulation.

**B7 — metadata.** In app/layout.tsx set `export const metadata: Metadata = { title: { template: "%s · Jewelry Render Studio", default: "Jewelry Render Studio" }, description: "Internal studio for rendering Glamira jewelry catalog imagery." }`. Add static `export const metadata: Metadata = { title: "..." }` (with the `import type { Metadata } from "next"` import) to: products/page.tsx "Products"; products/new/page.tsx "New product" (consistency addition beyond the audit list — every surface gets a title); batches/page.tsx "Batches"; gallery/page.tsx "Outputs"; compositing/page.tsx "Compositing"; products/[id]/batches/new/page.tsx "Build batch"; admin/users/page.tsx "Users"; admin/settings/page.tsx "Domain Settings"; login/page.tsx "Sign in". Add dynamic `generateMetadata` to: products/[id]/page.tsx — `prisma.product.findUnique({ where: { id }, select: { name: true } })`, title `product?.name ?? "Product"` (a second lightweight select is acceptable; do not restructure the page query); batches/[id]/page.tsx — NO DB call, title `` `Batch · ${id.slice(0, 8)}` `` from params. All these pages are server components (verified — none has "use client").

**B8 — error boundary.** Create app/(app)/error.tsx: `"use client"`; default-export `Error({ error, reset }: { error: Error & { digest?: string }; reset: () => void })`; calm card per the error-card pattern: "Something went wrong." (text-sm text-foreground), the digest when present as `font-mono text-xs text-muted-foreground` (e.g. "Reference: {digest}"), and `<Button variant="secondary" className="mt-4" onClick={reset}>Try again</Button>`. UI-SPEC-style top comment. No console.error side effects needed.

Commit ONLY the listed files: `feat(ui): persist theme via next-themes, app-wide page titles, global error boundary`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "ThemeProvider" app/layout.tsx && grep -c "suppressHydrationWarning" app/layout.tsx && bash -c '! grep -q "\"dark\"" app/layout.tsx' && grep -c "useTheme" app/components/app-shell/theme-toggle.tsx && grep -rl "export const metadata\|generateMetadata" "app/(app)" "app/(auth)" | wc -l && grep -c "use client" "app/(app)/error.tsx"</automated>
  </verify>
  <done>tsc clean; html no longer hardcodes "dark" and has suppressHydrationWarning; ThemeProvider wraps children with attribute="class" defaultTheme="dark"; toggle uses useTheme with mounted guard; root title template + 9 static titles + 2 generateMetadata in place; app/(app)/error.tsx is a client boundary calling reset(); commit 2 contains exactly the listed files.</done>
</task>

<task type="auto">
  <name>Task 3: Batch C — shared date formatting, gallery header, canonical empty states, breadcrumbs</name>
  <files>app/(app)/products/product-card.tsx, app/(app)/batches/page.tsx, app/(app)/products/[id]/page.tsx, app/(app)/batches/[id]/intel-panel.tsx, app/(app)/batches/[id]/gallery/page.tsx, app/(app)/batches/[id]/compositing/page.tsx, app/components/app-shell/page-breadcrumb.tsx, app/(app)/batches/[id]/page.tsx, app/(app)/products/[id]/batches/new/page.tsx, app/(app)/products/new/page.tsx</files>
  <action>
Execute audit findings 9-12 exactly (lib/format.ts already exists from Task 1):

**C9 — migrate the four date call sites to lib/format.** product-card.tsx: delete the local relativeTime (lines 35-49), import `{ relativeTime }` from "@/lib/format". batches/page.tsx: delete the local relativeTime (lines 189-201), same import. products/[id]/page.tsx Overview "Uploaded" (line 135): replace the raw `toISOString().slice(0,16).replace("T"," ")` with `formatDateTime(product.createdAt)`. intel-panel.tsx line 345: replace `new Date(a.at).toLocaleString()` with `formatDateTime(a.at)` (client component — lib/format is plain TS, fine to import). After this, lib/format.ts must be the ONLY file defining relativeTime.

**C10 — gallery header hierarchy.** In gallery/page.tsx restructure the header (lines 72-86) to match batch detail's: h1 (`truncate text-xl font-semibold leading-tight text-foreground`) = `{gallery.productName}` only, with `<BatchStatusPill status={batchStatus} />` beside it in a `flex flex-wrap items-center gap-3` row (batchStatus is ALREADY derived on this page — move the `progress`/`batchStatus` computation above the JSX usage; it currently sits before the return, so only the header consumes it earlier). Mono sub-line `font-mono text-xs text-muted-foreground` = `{gallery.id} · {layers.length} layers`. Keep the "Download full set" button on the right. Import BatchStatusPill from "../../status-pill". Section context ("Outputs") now comes from the SegmentSwitcher added in Task 1 — drop the "— Outputs" h1 suffix.

**C11 — canonical empty states.** Restyle gallery empty state (gallery/page.tsx lines 95-101) and compositing empty state (compositing/page.tsx lines 144-156) to the canonical dashed pattern from the interfaces block. Gallery: `Images` icon, heading "No finished outputs yet" (existing copy becomes the semibold heading), body "Outputs will appear here as renders finish." reusing the page's existing phrasing tone, CTA `<Button variant="secondary" asChild className="mt-2"><Link href={`/batches/${gallery.id}`}>Back to job monitor</Link></Button>` (keep secondary — "Download full set" stays the page's one primary). Compositing: `Layers` icon, KEEP the existing heading "No composable layers yet", body, and "View batch progress" CTA copy/target verbatim — only the container changes to the dashed canonical structure.

**C12 — breadcrumbs.** Create app/components/app-shell/page-breadcrumb.tsx: SERVER component (no "use client"); `export function PageBreadcrumb({ items }: { items: { label: string; href?: string }[] })`; renders `<nav aria-label="Breadcrumb">` with a `flex flex-wrap items-center gap-1.5 text-xs text-muted-foreground` row; items with href render as next/link `Link` with `transition-colors hover:text-foreground`; the last/href-less item renders as a plain `<span className="text-foreground">`; separate items with `<ChevronRight className="size-3" aria-hidden />` (lucide-react). UI-SPEC-style top comment. Add it as the topmost element (above SegmentSwitcher where present, above the header otherwise) on six pages: products/[id]/page.tsx → [{Products → /products}, {product.name}]; batches/[id]/page.tsx → [{Batches → /batches}, {batch.productName}]; gallery/page.tsx → [{Batches → /batches}, {gallery.productName → /batches/[id]}, {Outputs}]; compositing/page.tsx → [{Batches → /batches}, {gallery.productName → /batches/[id]}, {Compositing}]; products/[id]/batches/new/page.tsx → [{Products → /products}, {product.name → /products/[id]}, {Build batch}] (product.name is already loaded on that page); products/new/page.tsx → [{Products → /products}, {New}]. On the error/not-found branches of the batch pages skip the breadcrumb (productName unavailable).

Commit ONLY the listed files: `refactor(ui): shared date formatting, gallery header hierarchy, canonical empty states, breadcrumbs`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && bash -c '[ "$(grep -rl "function relativeTime" app lib | wc -l)" = "1" ]' && grep -c "toLocaleString" "app/(app)/batches/[id]/intel-panel.tsx" | grep -qx 0 && grep -rc "PageBreadcrumb" "app/(app)" | grep -v ":0" | wc -l && grep -c "border-dashed" "app/(app)/batches/[id]/gallery/page.tsx" && grep -c "border-dashed" "app/(app)/batches/[id]/compositing/page.tsx"</automated>
  </verify>
  <done>tsc clean; relativeTime defined only in lib/format.ts; intel-panel and product Overview use formatDateTime; gallery h1 = product name + status pill with mono id sub-line; both empty states use the dashed canonical pattern with icon circles; PageBreadcrumb rendered on all six specified pages; commit 3 contains exactly the listed files. Final gate: `npx vitest run` passes (incl. orch-db-only + palette guard tests) and `npx next build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | UI-only pass; no new API routes, no new input parsing, no package installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q11-01 | Information Disclosure | generateMetadata / breadcrumbs / recent-batches render DB strings (product name) | accept | React JSX + Next metadata auto-escape; pages remain behind requireSession/auth layout |
| T-Q11-02 | Tampering | theme via next-themes localStorage | accept | attribute="class" with enableSystem=false; worst case is a cosmetic class flip |
| T-Q11-03 | Elevation of Privilege | new not-found/error surfaces | mitigate | app/(app)/not-found + error render inside the auth-gated (app) layout; root not-found exposes no data, links to / only |
</threat_model>

<verification>
After each task: `npx tsc --noEmit` (minimum gate, per audit constraints).
Final gate (after Task 3): `npx vitest run` (existing source-guard tests: orch-db-only, comp-page-db-only, raw-palette guard must stay green) and `npx next build` (all touched pages are force-dynamic, so no build-time DB access is introduced).
Scope check: `git log --stat -3` shows exactly 3 commits touching only the files listed in this plan — nothing under workers/, public/worker-code/, prisma/, or lib/orchestration dispatch.
</verification>

<success_criteria>
- All 12 audit findings implemented as specified (no scope reduction, no re-audit)
- 3 atomic commits: feat(ui) wiring, feat(ui) theme+meta, refactor(ui) consistency — each compiles independently
- `npx tsc --noEmit`, `npx vitest run`, `npx next build` all green
- Zero changes outside app/, lib/format.ts; pre-existing worktree modifications left unstaged
- No purple, no raw hex, no raw status-palette classes (semantic tokens only)
</success_criteria>

<output>
Create `.planning/quick/260612-i11-ui-ux-wiring-fixes/260612-i11-SUMMARY.md` when done.
</output>
