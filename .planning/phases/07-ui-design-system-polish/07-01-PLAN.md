---
phase: 07-ui-design-system-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/(app)/products/[id]/batches/new/estimate-panel.tsx
  - app/(app)/products/[id]/batches/new/batch-builder.tsx
  - app/(app)/products/[id]/group-assignment.tsx
  - lib/groups/chip.ts
  - test/group-chip-tokens.test.ts
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "Every amber-*/sky-*/emerald-* status-palette class in the 3 builder/product files is replaced by the semantic token mapped by intent (warn->warning, info->info, safe->success, submitting->info)"
    - "Contrast holds in BOTH light and dark themes because the foreground token pairs already exist in globals.css for both :root and .dark"
    - "The stone-group chip class map is defined ONCE in a shared lib module and imported by both batch-builder and group-assignment (no divergent duplicate)"
    - "No new color tokens, no purple, no new dependencies are introduced"
    - "A guard test asserts no raw amber-/sky-/emerald- palette class remains in the 3 cited files"
  artifacts:
    - path: "lib/groups/chip.ts"
      provides: "single shared token-based stone-group chip class map"
      contains: "GROUP_CHIP_CLASS"
    - path: "test/group-chip-tokens.test.ts"
      provides: "guard: no raw status-palette classes remain in the 3 files"
      contains: "amber-"
---

<objective>
Phase 7 is a SMALL coherence sweep, not a redesign. The audit (07-UI-REVIEW.md)
scored the app 27/30 and found exactly ONE real defect cluster: 12 raw Tailwind
status-palette colors (amber-/sky-/emerald-) bypass the already-named semantic
tokens (warning/info/success) in 3 builder/product files, and the stone-group
chip class map is duplicated + divergent across two of those files.

Close UI-01 by (A) swapping each raw status color for its intent-mapped semantic
token, and (B) extracting the duplicated chip map into one shared lib module both
call sites import. No new tokens, no purple, no new deps. UI-02 is already
satisfied per the audit (Work Unit C is a verified no-op).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-ui-design-system-polish/07-UI-REVIEW.md

<read_first>
@app/globals.css
@app/(app)/products/[id]/batches/new/estimate-panel.tsx
@app/(app)/products/[id]/batches/new/batch-builder.tsx
@app/(app)/products/[id]/group-assignment.tsx
@app/(app)/batches/[id]/gallery/layer-card.tsx
</read_first>

<interfaces>
<!-- Semantic tokens already exist in app/globals.css for BOTH themes:
     --color-warning / --color-warning-foreground (warn/caution intent)
     --color-info    / --color-info-foreground    (info/running intent)
     --color-success / --color-success-foreground (safe/go intent)
     Tailwind utilities: text-warning, border-warning, border-l-warning, etc. -->
<!-- Intent map (by semantic role, NOT blind color swap):
     emerald (safe/go estimate zone)   -> success
     amber   (caution/warn notice/zone)-> warning
     sky     (info hint / submitting)  -> info -->
<!-- The gallery already exports GROUP_CHIP (gallery/layer-card.tsx:25) but in a
     FILLED style (bg-*/15 text-*). The builder/assignment chips are OUTLINE style
     (border-*/50 text-*) and also cover alloycolour + unassigned keys. Shapes
     differ -> create a NEW small shared outline-style map in lib/, do not force
     the filled gallery map onto the outline call sites. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status-token sweep (UI-01) — replace 12 raw palette colors by intent</name>
  <files>app/(app)/products/[id]/batches/new/estimate-panel.tsx, app/(app)/products/[id]/batches/new/batch-builder.tsx, app/(app)/products/[id]/group-assignment.tsx</files>
  <behavior>
    estimate-panel.tsx:
      - FRAME.safe: border-l-emerald-500/70 -> border-l-success/70
      - FRAME.warn: border-l-amber-500 border-amber-500/40 -> border-l-warning border-warning/40
      - warn TriangleAlert: text-amber-500 -> text-warning
      - "Creating batch…": text-sky-500 -> text-info
    batch-builder.tsx:
      - STONE_GROUP_CHIP stone2 sky-500 -> info, stone3 amber-500 -> warning (then Task 2 replaces with shared import)
      - all 4 validation notices text-amber-500 (l.232,265,357,429) -> text-warning
    group-assignment.tsx:
      - CHIP_CLASS stone2 sky-500 -> info, stone3 amber-500 -> warning (then Task 2 replaces with shared import)
      - unassigned-count notice text-amber-500 (l.258) -> text-warning
    Map each by SEMANTIC INTENT. Confirm contrast reads in light + dark (foreground
    token pairs are designed for both). NO new tokens, NO purple.
  </behavior>
  <verify>
    <automated>npx vitest run --reporter=dot</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Zero amber-/sky-/emerald- palette classes remain in the 3 files; vitest green; tsc exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: De-dupe stone-group chip map (UI-01 coherence) + guard test</name>
  <files>lib/groups/chip.ts, app/(app)/products/[id]/batches/new/batch-builder.tsx, app/(app)/products/[id]/group-assignment.tsx, test/group-chip-tokens.test.ts</files>
  <behavior>
    Create lib/groups/chip.ts exporting ONE token-based outline chip map keyed by
    every group key both call sites need (alloycolour, diamond, stone2, stone3,
    unassigned) using semantic tokens only: diamond=primary, stone2=info,
    stone3=warning, alloycolour=border-border text-foreground, unassigned=dashed
    muted. batch-builder imports it for STONE_GROUP_CHIP usage; group-assignment
    imports it for CHIP_CLASS usage. Delete both local duplicate definitions.
    Add test/group-chip-tokens.test.ts: a guard reading the 3 source files and
    asserting no /\b(amber|sky|emerald)-\d/ palette class survives, plus a check
    that the shared map exposes the expected token-based classes once.
  </behavior>
  <verify>
    <automated>npx vitest run group-chip-tokens --reporter=dot</automated>
    <automated>npx vitest run --reporter=dot</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>One shared chip map in lib/groups/chip.ts imported by both call sites; no local duplicate; guard test green; full vitest green; tsc exit 0.</done>
</task>

</tasks>

<verification>
- Full `npx vitest run` GREEN.
- `npx tsc --noEmit` exit 0.
- `npx next build` succeeds.
- Zero amber-/sky-/emerald- palette classes in the 3 cited files (guard test).
- One shared stone-group chip map; both call sites import it.
</verification>

<success_criteria>
UI-01 closed: the design system is coherent — all status colors flow through the
existing semantic tokens (warning/info/success) in both themes, and the
stone-group chip contract is defined exactly once. UI-02 confirmed already
satisfied (audit Work Unit C is a no-op). No new tokens/deps/purple.
</success_criteria>

<output>
Create `.planning/phases/07-ui-design-system-polish/07-01-SUMMARY.md` when done.
</output>
