---
phase: 05-outputs-gallery-layered-passes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - prisma/schema.prisma
  - package.json
  - test/out-layer-derive.test.ts
  - test/out-stone-transparency.test.ts
  - test/out-gallery-group.test.ts
  - test/out-gallery-query.test.ts
  - test/out-file-download.test.ts
  - test/out-zip-route.test.ts
autonomous: false
requirements: [OUT-01, OUT-02, OUT-03]
user_setup:
  - service: vercel-blob (worker env)
    why: "Render outputs must be PRIVATE so /api/file get(access:'private') can deliver them (SEC-02, RESEARCH Runtime State Inventory / Pitfall 2)"
    dashboard_config:
      - task: "Set BLOB_ACCESS=private on the RunPod endpoint env so new render outputs upload as private blobs"
        location: "RunPod endpoint → Environment Variables"

must_haves:
  truths:
    - "Layer.jobId is unique so a duplicate webhook upserts one row, never two"
    - "Every Phase-5 requirement has a failing automated test before implementation begins"
    - "The archiver dependency is human-vetted on the registry before any install"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Layer.jobId @unique constraint"
      contains: "@unique"
    - path: "test/out-layer-derive.test.ts"
      provides: "OUT-01 layer-derive + idempotency failing scaffold"
    - path: "test/out-stone-transparency.test.ts"
      provides: "OUT-01 recipe-transparency failing scaffold"
    - path: "test/out-gallery-group.test.ts"
      provides: "OUT-02 grouping failing scaffold"
    - path: "test/out-gallery-query.test.ts"
      provides: "OUT-02 completed-only query failing scaffold"
    - path: "test/out-file-download.test.ts"
      provides: "OUT-03 download proxy failing scaffold"
    - path: "test/out-zip-route.test.ts"
      provides: "OUT-03 zip route failing scaffold"
  key_links:
    - from: "Layer.jobId"
      to: "prisma.layer.upsert({where:{jobId}})"
      via: "unique constraint enabling idempotent upsert"
      pattern: "@unique"
---

<objective>
Stand up the Phase-5 Wave-0 foundation: the schema migration that makes Layer
creation idempotent, the seven failing test scaffolds every later wave will turn
green, and the dependency-legitimacy checkpoint for the NEW `archiver` package.

Purpose: lock the idempotency invariant (one job = one layer) into the DB before
any writer is built, and front-load every requirement's failing test so the
implementation waves are pure RED→GREEN.
Output: `Layer.jobId @unique` migration applied to the live DB, 6 new failing
test files, and a vetted (or replaced) `archiver` dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-outputs-gallery-layered-passes/05-RESEARCH.md
@.planning/phases/05-outputs-gallery-layered-passes/05-VALIDATION.md

<read_first>
@prisma/schema.prisma
@test/setup.ts
@test/factories.ts
@test/orch-db-only.test.ts
@lib/batches/expand.ts
@workers/runpod-blender/handler.py
</read_first>

<interfaces>
<!-- Worker output shape that becomes a Layer (handler.py:181-188): -->
<!-- { job_id, image_key, image_url, image_blob:{url,pathname,content_type,...}, metadata_key, metadata_blob:{...} } -->
<!-- pass + stoneGroup come from Job.combo (expand.ts Combo): { angleKey, metalKey, pass:"metal"|"stone", stoneGroup? } -->
<!-- format derives from image_blob.content_type ("image/png") — NOT hardcoded. -->
<!-- Layer model today: { id, jobId, pass, format, url, metadataUrl, isFlattened } — NO unique on jobId yet. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Vet + install the archiver dependency (T-05-SC)</name>
  <what-built>
    The zip route (Plan 03) needs a streaming multi-entry zip builder. RESEARCH
    tags `archiver ^7.0.1` as [ASSUMED] — registry existence was NOT confirmed
    this session and slopcheck was unavailable. This checkpoint vets it before install.
  </what-built>
  <how-to-verify>
    1. Run `npm view archiver version` — confirm it resolves to a real published version (~7.x).
    2. Visit https://www.npmjs.com/package/archiver — confirm it is the long-established
       (github.com/archiverjs/node-archiver) package, ~30M weekly downloads, not a typosquat.
    3. Confirm @types/archiver exists on DefinitelyTyped.
    If all three pass, approve and the executor runs:
      npm install archiver && npm install -D @types/archiver
    If archiver is unavailable/suspicious, reply "use zip-stream" (RESEARCH fallback) and
    the executor installs zip-stream instead, noting the swap for Plan 03.
  </how-to-verify>
  <resume-signal>Type "approved" (install archiver) or "use zip-stream" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add Layer.jobId @unique and run the BLOCKING migration</name>
  <files>prisma/schema.prisma</files>
  <action>
    In the `Layer` model add `@unique` to the `jobId` field (per locked decision 2:
    one job = exactly one layer — verified by expand.ts producing one row per
    angle×metal×pass and handler.py uploading exactly one image per job). This makes
    `prisma.layer.upsert({where:{jobId}})` race-free against duplicate/late webhooks.
    [BLOCKING] After editing the schema, run the migration directly against the live
    DB (it is additive and there are zero existing Layer rows — no creator exists yet,
    so the migration is empty/safe): `npx prisma migrate dev --name layer-job-unique`.
    Then `npx prisma generate` to refresh the client types.
  </action>
  <verify>
    <automated>npx prisma migrate status</automated>
  </verify>
  <done>`Layer.jobId` has `@unique`; `npx prisma migrate status` exits 0 (no pending migrations); generated client types compile.</done>
</task>

<task type="auto">
  <name>Task 3: Write the six failing Phase-5 test scaffolds (RED)</name>
  <files>test/out-layer-derive.test.ts, test/out-stone-transparency.test.ts, test/out-gallery-group.test.ts, test/out-gallery-query.test.ts, test/out-file-download.test.ts, test/out-zip-route.test.ts</files>
  <action>
    Create one failing test file per behavior in 05-VALIDATION.md, importing modules
    that do not exist yet so each fails RED. Reuse the harness patterns: `fakeSession`
    from test/setup.ts, factories from test/factories.ts, `vi.mock("@/lib/db/prisma")`
    and `vi.mock("@vercel/blob")` per blob-guard.test.ts.

    - test/out-stone-transparency.test.ts (OUT-01): import `buildEnterpriseRecipe` from
      @/lib/enterprise-recipes. Assert a STONE-pass request (pass:"stone", stoneGroup:"diamond")
      produces render.transparent===true, model.exclude_contains CONTAINS the alloycolour/metal
      tokens (metal excluded — true holdout), and postprocess.studio_background.enabled===false.
      Assert a METAL-pass request stays render.transparent===false (opaque) with studio_background
      enabled. Assert the FULL pass is unchanged. (Names: use FALLBACK_TOKENS-style metal tokens.)
    - test/out-layer-derive.test.ts (OUT-01): import `deriveLayerFromResult` from
      @/lib/orchestration/layers (does not exist → RED). Assert it maps a worker output
      object (image_blob.pathname → Layer.url, metadata_key → Layer.metadataUrl, combo.pass →
      Layer.pass, combo.stoneGroup, format from image_blob.content_type) and calls
      prisma.layer.upsert({where:{jobId}, ...}) exactly once; calling it twice for the same
      jobId issues two upserts with the SAME where:{jobId} (idempotent — no second insert key).
      NOTE (W2): 05-VALIDATION lists a separate test/out-layer-idempotent.test.ts, but idempotency
      is intentionally folded INTO out-layer-derive.test.ts here (the duplicate-call → same
      where:{jobId} upsert / no-double-insert assertion above). No separate file is created — the
      VALIDATION map and this plan agree on out-layer-derive.test.ts as the idempotency home.
    - test/out-gallery-group.test.ts (OUT-02): import a pure grouping fn `groupLayers` from
      @/lib/gallery/group (does not exist → RED). Assert it groups Layer+combo rows by
      metalKey/angleKey/pass/stoneGroup using the CORRECT keys (angleKey/metalKey/stoneGroup —
      NOT angle/metal/stone). A fixture using the wrong keys must collapse, proving the guard.
    - test/out-gallery-query.test.ts (OUT-02): mock prisma; import the gallery loader
      `loadBatchGallery` from @/lib/gallery/query (does not exist → RED). Assert the prisma
      call filters jobs to status:"completed" and includes layers; assert no RunPod import.
    - test/out-file-download.test.ts (OUT-03): mock requireSession + @vercel/blob; import
      GET from @/app/api/file/route. Assert unauth → 401; assert that with download=1&name=foo
      the response carries a `Content-Disposition: attachment; filename="..."` header and the
      filename is sanitized (CR/LF/quotes stripped). (This will RED on the disposition assertion
      until Plan 03 extends the route.)
    - test/out-zip-route.test.ts (OUT-03): mock requireSession + prisma + @vercel/blob (+ archiver);
      import GET from the zip route @/app/(app)/batches/[id]/download/route (does not exist → RED).
      Assert unauth → 401; assert the batch is loaded by params.id (IDOR scope) and the zip pulls
      layers via get(pathname,{access:"private"}); assert Content-Type application/zip; assert no
      public/signed URL construction.

    Each test must FAIL now (missing module or missing behavior). Do NOT stub the
    implementations — that is the later waves' job.
  </action>
  <verify>
    <automated>npx vitest run out-stone-transparency out-layer-derive out-gallery-group out-gallery-query out-file-download out-zip-route 2>&1 | findstr /C:"fail" /C:"FAIL"</automated>
  </verify>
  <done>All six new test files exist and FAIL (RED) for the right reason (missing module / missing behavior), not from syntax/setup errors. The existing suite still parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod worker → webhook/reconcile writer | Untrusted worker output JSON crosses into `deriveLayerFromResult` (Plan 02) |
| npm registry → build | New `archiver` dependency enters the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | duplicate webhook → duplicate Layer | mitigate | `Layer.jobId @unique` + upsert (this plan, Task 2) makes the write idempotent |
| T-05-SC | Tampering | npm `archiver` install | mitigate | slopcheck unavailable → blocking human-verify checkpoint (Task 1) confirms registry legitimacy before install |
</threat_model>

<verification>
- `npx prisma migrate status` exits 0 (migration applied).
- The six new test files run RED.
- `archiver` (or zip-stream fallback) present in package.json after the approved checkpoint.
</verification>

<success_criteria>
Layer.jobId is unique in the live DB; all six Phase-5 tests fail for the right
reason; the zip dependency is human-vetted and installed (or swapped for zip-stream).
</success_criteria>

<output>
Create `.planning/phases/05-outputs-gallery-layered-passes/05-01-SUMMARY.md` when done.
</output>
