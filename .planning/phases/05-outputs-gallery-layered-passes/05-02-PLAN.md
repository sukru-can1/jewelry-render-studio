---
phase: 05-outputs-gallery-layered-passes
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - lib/enterprise-recipes.ts
  - lib/orchestration/layers.ts
  - lib/orchestration/webhook.ts
  - lib/orchestration/backfill-layers.ts
  - test/out-stone-transparency.test.ts
  - test/out-layer-derive.test.ts
autonomous: true
requirements: [OUT-01]

must_haves:
  truths:
    - "A new stone-pass batch renders a true transparent-PNG holdout (ONLY the target stone group, no metal, no studio background)"
    - "A metal-pass batch stays an opaque render"
    - "Every completed job produces exactly one Layer row, idempotently, on completion"
    - "Jobs already completed before this ships are backfilled into Layer rows (idempotent, safe via the unique upsert)"
  artifacts:
    - path: "lib/enterprise-recipes.ts"
      provides: "Stone-pass transparency (render.transparent + ONLY-stone-group include + metal-excluded + studio_background off)"
      contains: "transparent"
    - path: "lib/orchestration/layers.ts"
      provides: "deriveLayerFromResult pure helper + upsert"
      exports: ["deriveLayerFromResult"]
    - path: "lib/orchestration/webhook.ts"
      provides: "Layer creation hooked into the COMPLETED branch"
      contains: "deriveLayerFromResult"
    - path: "lib/orchestration/backfill-layers.ts"
      provides: "Idempotent one-shot backfill of pre-existing completed jobs into Layer rows"
      exports: ["backfillCompletedLayers"]
  key_links:
    - from: "lib/orchestration/webhook.ts (completed branch)"
      to: "deriveLayerFromResult"
      via: "call after updateMany on completion (shared by reconcile)"
      pattern: "deriveLayerFromResult"
    - from: "deriveLayerFromResult"
      to: "prisma.layer.upsert"
      via: "where:{jobId} idempotent write"
      pattern: "layer\\.upsert"
    - from: "lib/orchestration/backfill-layers.ts"
      to: "deriveLayerFromResult"
      via: "replays each already-completed job through the same idempotent writer"
      pattern: "deriveLayerFromResult"
---

<objective>
Deliver OUT-01: make stone passes render as true transparent-PNG holdouts at the
recipe level (no worker change), and record every completed job as exactly one
idempotent `Layer` row by hooking `deriveLayerFromResult` into the shared
completion writer — plus a one-shot idempotent backfill so jobs that completed
BEFORE this ships also become visible in the gallery.

Purpose: the gallery (Plan 04) reads `Layer` rows; this plan is what creates them
(for future AND past completions) and what makes the stone layers actually transparent.
Output: corrected recipe transparency for stone passes + `lib/orchestration/layers.ts`
+ the webhook/reconcile hook + `lib/orchestration/backfill-layers.ts` — both Wave-0
tests turned GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-outputs-gallery-layered-passes/05-RESEARCH.md

<read_first>
@lib/enterprise-recipes.ts
@lib/orchestration/webhook.ts
@lib/orchestration/reconcile.ts
@lib/batches/expand.ts
@workers/runpod-blender/handler.py
</read_first>

<interfaces>
<!-- buildVisibility (enterprise-recipes.ts:138) — stone branch CURRENTLY returns
     include: uniqueTokens([...metalTokens, ...stoneGroupTokens]), exclude: [] (THE BUG). -->
<!-- metalTokens = tokensFor(groupTokens,"alloycolour"); stoneGroup tokens = tokensFor(groupTokens, request.stoneGroup). -->
<!-- render.transparent is at enterprise-recipes.ts:199 (currently false for all). -->
<!-- postprocess.studio_background.enabled at :317 (currently true for all). -->
<!-- worker render_scene.py honors render.transparent (film_transparent) — confirmed, no worker edit. -->
<!-- worker filter_product_objects: include_contains is a HARD allow-list (HIDES any object NOT matching);
     exclude_contains is a deny-list. Both apply against object_signature. -->
<!-- applyWebhookResult COMPLETED branch (webhook.ts:47) does updateMany then return. -->
<!-- Worker output: { image_blob:{pathname,content_type}, image_key, metadata_key, metadata_blob:{pathname} } -->
<!-- Job.combo: { angleKey, metalKey, pass:"metal"|"stone", stoneGroup? } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stone-pass recipe transparency + precise holdout (OUT-01, per D-1)</name>
  <files>lib/enterprise-recipes.ts, test/out-stone-transparency.test.ts</files>
  <behavior>
    - Stone pass (pass:"stone", has stoneGroup):
        * render.transparent===true
        * model.include_contains === ONLY the target stoneGroup's tokens — it MUST be
          non-empty AND disjoint from the metal (alloycolour) tokens. (NO metal tokens.)
        * model.exclude_contains is a SUPERSET of the alloycolour metal tokens (metal held out).
        * postprocess.studio_background.enabled===false (so alpha survives postprocess).
      Repeat these assertions for EACH stone group: diamond, stone2, stone3.
    - Metal pass (pass:"metal"): render.transparent===false (opaque); studio_background.enabled===true.
    - Full pass: unchanged from today.
  </behavior>
  <action>
    Implement locked decision 1 — RECIPE-LEVEL ONLY, no worker edit. In
    `buildEnterpriseRecipe`/`buildVisibility`:
    (a) For pass==="stone" with a stoneGroup: rewrite the stone branch so `include` is
        EXACTLY `uniqueTokens(tokensFor(request.groupTokens, request.stoneGroup))` — ONLY the
        target stone group's tokens, with the `...metalTokens` spread REMOVED — and `exclude`
        is `uniqueTokens(metalTokens)` (the alloycolour metal tokens). This is a precise
        single-group holdout. Because the worker's filter_product_objects treats include_contains
        as a HARD allow-list (it HIDES every object that does NOT match), HAZARD: if the target
        stoneGroup's tokens are sparse and under-match the model, the include list hides
        everything → an empty render. Add a code comment flagging that the stone-group tokens
        MUST resolve to real object signatures; the binding proof is the live manual render
        (VALIDATION row), not the unit test.
    (b) Make `render.transparent` pass-dependent: `true` when pass==="stone", else `false`
        (metal stays opaque PNG — literal JPEG would need a worker change, explicitly DEFERRED;
        leave a code comment noting the metal-JPEG deferral).
    (c) Make `postprocess.studio_background.enabled` `false` for pass==="stone" (so the opaque
        studio floor/bg is not composited over the alpha), `true` otherwise.
    Keep the `full` pass behavior unchanged. Add a comment that this is forward-correct: it
    affects NEW batches only — existing Job.recipe rows were already generated and are unchanged.
    Then make test/out-stone-transparency.test.ts pass. The test MUST, for EACH of diamond,
    stone2, stone3, assert: include_contains non-empty; include_contains disjoint from the metal
    tokens; exclude_contains ⊇ the metal tokens; render.transparent===true; studio_background
    disabled — AND assert the metal pass is opaque (transparent===false, studio_background
    enabled). Add an explicit NOTE in the test header comment that this unit test only inspects
    recipe FLAGS — it cannot see rendered pixels — so the live manual render (05-VALIDATION row)
    is the BINDING check for actual alpha + no-metal-bleed.
  </action>
  <verify>
    <automated>npx vitest run out-stone-transparency</automated>
  </verify>
  <done>Stone-pass recipe: transparent + include===ONLY-stone-group (non-empty, metal-disjoint) + exclude ⊇ metal + studio_background off, asserted per group; metal-pass opaque; full unchanged; test GREEN. (Live render is the binding alpha/no-bleed check.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveLayerFromResult helper + idempotent hook + backfill (OUT-01, per D-2)</name>
  <files>lib/orchestration/layers.ts, lib/orchestration/webhook.ts, lib/orchestration/backfill-layers.ts, test/out-layer-derive.test.ts</files>
  <behavior>
    - deriveLayerFromResult(jobId, output) reads image_blob.pathname (or image_key) → url,
      metadata_key (or metadata_blob.pathname) → metadataUrl, pass + stoneGroup from the job's
      stored combo, format from image_blob.content_type, and upserts on {jobId}.
    - A duplicate/late call for the same jobId is a no-op second upsert (same where key) — never a second row.
    - The webhook COMPLETED branch calls it after the updateMany; reconcile gets it free (shared path).
    - backfillCompletedLayers() finds already-completed jobs that have no Layer and replays each
      through deriveLayerFromResult — re-running it is a no-op (idempotent via the unique upsert).
  </behavior>
  <action>
    Create `lib/orchestration/layers.ts` exporting a pure-ish `deriveLayerFromResult(jobId, output)`:
    read the worker output (the shape in <interfaces>), load the job's `combo` (pass + stoneGroup)
    — read combo from the DB or accept it as an argument; prefer reading combo via prisma inside
    the helper so the webhook caller stays thin. Map format from `image_blob.content_type`
    (fallback to extension), NEVER hardcode. Then `prisma.layer.upsert({ where: { jobId },
    create: {...}, update: {...} })` — relying on the Plan-01 `Layer.jobId @unique`. Store
    Layer.url as the blob PATHNAME (NOT the public image_url — SEC-02; the gallery builds the
    /api/file proxy URL from it). Guard against malformed/missing output (no pathname → skip, do
    not throw — at-least-once webhook hygiene).
    In `lib/orchestration/webhook.ts` COMPLETED branch (after the updateMany), call
    `await deriveLayerFromResult(... )`. Because reconcile.ts replays through applyWebhookResult,
    the cron fallback gets layer creation for free — do NOT add a second code path.

    BACKFILL (W3 — pre-existing completed jobs have Job.result but no Layer row, so they are
    invisible in the gallery). Create `lib/orchestration/backfill-layers.ts` exporting a small,
    bounded `backfillCompletedLayers()`: query `prisma.job.findMany({ where: { status: "completed",
    layers: { none: {} }, result: { not: null } } })` and, for each, call
    `deriveLayerFromResult(job.id, job.result)`. This is SAFE and idempotent because the helper
    upserts on `{jobId}` — re-running creates no duplicates. Keep it bounded (no unbounded fan-out;
    process the matched set, log a count). It can be invoked once after deploy (or wired into the
    existing reconcile cron). Add a header comment that this future-proofs the gallery against
    completions that happened before the webhook hook existed.

    Make test/out-layer-derive.test.ts pass (mock prisma) — including the idempotency case
    (calling deriveLayerFromResult twice for the same jobId issues two upserts with the SAME
    where:{jobId}, never a second insert key).
  </action>
  <verify>
    <automated>npx vitest run out-layer-derive</automated>
  </verify>
  <done>deriveLayerFromResult maps output→Layer fields, upserts on {jobId} idempotently, is wired into the completed branch, stores pathname (not public URL); backfillCompletedLayers replays pre-existing completed jobs through the same idempotent upsert; test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod worker output → deriveLayerFromResult | Untrusted JSON from the worker becomes Layer fields |
| Webhook (at-least-once) → Layer write | Duplicate completion callbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | duplicate webhook → duplicate Layer | mitigate | upsert on `Layer.jobId @unique` (idempotent); guarded by the existing terminal-status check upstream; backfill reuses the same upsert (no duplicate risk) |
| T-05-02 | Tampering / Injection | worker output → Layer.url | mitigate | store only the blob PATHNAME, never the worker-supplied public `image_url`; skip on missing pathname (no throw) |
| T-05-03 | Information Disclosure | leaking public blob URL as delivery href | mitigate | Layer.url is the private pathname; delivery is via the /api/file proxy only (SEC-02) |
</threat_model>

<verification>
- `npx vitest run out-stone-transparency out-layer-derive` both GREEN.
- `npx vitest run` full suite stays green (webhook/reconcile tests not regressed).
</verification>

<success_criteria>
Stone-pass recipes are true single-group transparent holdouts (asserted per group);
completed jobs create exactly one Layer row idempotently via the shared completion
writer, and pre-existing completed jobs are backfilled the same idempotent way.
</success_criteria>

<output>
Create `.planning/phases/05-outputs-gallery-layered-passes/05-02-SUMMARY.md` when done.
</output>
