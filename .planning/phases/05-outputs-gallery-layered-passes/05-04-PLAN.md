---
phase: 05-outputs-gallery-layered-passes
plan: 04
type: execute
wave: 2
depends_on: ["05-02", "05-03"]
files_modified:
  - lib/gallery/group.ts
  - lib/gallery/query.ts
  - app/(app)/batches/[id]/gallery/page.tsx
  - app/(app)/batches/[id]/gallery/layer-card.tsx
  - app/(app)/batches/[id]/gallery/gallery-controls.tsx
  - app/(app)/batches/[id]/gallery/preview-lightbox.tsx
  - app/(app)/batches/[id]/page.tsx
  - app/(app)/batches/[id]/jobs-monitor.tsx
  - test/out-gallery-group.test.ts
  - test/out-gallery-query.test.ts
  - test/orch-db-only.test.ts
autonomous: true
requirements: [OUT-02, OUT-03]

must_haves:
  truths:
    - "An operator can browse a batch's completed outputs grouped by metal/angle/pass/variant"
    - "Stone (PNG) layers show on a transparency checkerboard; metal layers on a solid card"
    - "An operator can preview any layer full-quality and download a layer or the full set"
    - "The 'View in gallery' link from the job monitor opens the new gallery"
    - "The gallery never imports lib/runpod (DB-only)"
  artifacts:
    - path: "app/(app)/batches/[id]/gallery/page.tsx"
      provides: "DB-only Server Component gallery (requireSession + IDOR)"
      contains: "requireSession"
    - path: "lib/gallery/group.ts"
      provides: "Pure grouping by correct combo keys (angleKey/metalKey/stoneGroup/pass)"
      exports: ["groupLayers"]
    - path: "lib/gallery/query.ts"
      provides: "loadBatchGallery — completed-jobs-only DB read"
      exports: ["loadBatchGallery"]
    - path: "app/(app)/batches/[id]/gallery/preview-lightbox.tsx"
      provides: "shadcn dialog lightbox (client)"
  key_links:
    - from: "gallery layer-card / lightbox <img src>"
      to: "/api/file?pathname=..."
      via: "privateUrl(layer.url) proxy"
      pattern: "api/file"
    - from: "jobs-monitor 'View in gallery'"
      to: "/batches/[id]/gallery"
      via: "Link href"
      pattern: "/gallery"
---

<objective>
Deliver OUT-02 (gallery) and OUT-03's browse/preview surface: a DB-only Server
Component that reads completed-job Layer rows, groups them by the CORRECT combo
keys, renders metal/stone cards (checkerboard on PNG), opens a preview lightbox,
wires per-layer + full-set downloads to Plan-03's routes, and connects the
Phase-4 "View in gallery" link. Also fix the wrong-combo-keys bug in the monitor.

Purpose: the operator finally sees and downloads their layered outputs the way
they think about catalog work.
Output: the gallery route + client subcomponents + grouping/query libs, the
monitor combo-key fix and wiring, all Wave-0 gallery tests GREEN, the DB-only
guard extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-outputs-gallery-layered-passes/05-RESEARCH.md
@.planning/phases/05-outputs-gallery-layered-passes/05-UI-SPEC.md

<read_first>
@app/(app)/batches/[id]/page.tsx
@app/(app)/batches/[id]/jobs-monitor.tsx
@lib/blob.ts
@lib/auth/rbac.ts
@lib/batches/expand.ts
@lib/orchestration/batch-status.ts
@test/orch-db-only.test.ts
</read_first>

<interfaces>
<!-- Combo (expand.ts): { angleKey, metalKey, pass:"metal"|"stone", stoneGroup? } — the CORRECT keys. -->
<!-- BUG to fix: batches/[id]/page.tsx comboLabel reads c.angle/c.metal/c.stone (wrong) → falls back to "render". -->
<!-- Layer row: { id, jobId, pass, format, url, metadataUrl, isFlattened } — url is a PRIVATE pathname. -->
<!-- privateUrl(pathname) = `/api/file?pathname=...` ; download = `&download=1&name=...` (Plan 03). -->
<!-- Zip route: GET /batches/[id]/download?scope=... (Plan 03). -->
<!-- summarizeJobs/deriveBatchStatus from lib/orchestration/batch-status.ts for the partial banner. -->
<!-- UI-SPEC: PNG(stone)→checkerboard backing; JPEG/opaque(metal)→solid --card; group-by Metal default; -->
<!--          shadcn dialog lightbox; thumbnails 160×160; format badge; group chips (diamond=accent...). -->
<!-- "View in gallery" link currently at jobs-monitor.tsx:400 href=`/batches/${batchId}?job=${jobId}` — repoint to gallery. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gallery query + grouping libs + monitor combo-key fix (OUT-02)</name>
  <files>lib/gallery/group.ts, lib/gallery/query.ts, app/(app)/batches/[id]/page.tsx, app/(app)/batches/[id]/jobs-monitor.tsx, test/out-gallery-group.test.ts, test/out-gallery-query.test.ts</files>
  <behavior>
    - groupLayers groups Layer+combo rows by metalKey/angleKey/pass/stoneGroup using the CORRECT keys.
    - loadBatchGallery reads the batch by id with jobs filtered to status:"completed" and their layers.
    - The monitor's comboLabel reads angleKey/metalKey/stoneGroup/pass (no longer collapses to "render").
  </behavior>
  <action>
    Create `lib/gallery/group.ts` exporting a pure `groupLayers(rows, groupBy)` that groups by
    the CORRECT combo keys (angleKey/metalKey/stoneGroup/pass) — supporting group-by Metal (default),
    Angle, Pass, Variant per UI-SPEC. Create `lib/gallery/query.ts` exporting `loadBatchGallery(id)`
    that does `prisma.batch.findUnique({ where:{id}, include:{ product:{select:{name:true}},
    jobs:{ where:{status:"completed"}, include:{layers:true} } } })` — DB-only, NO lib/runpod import.
    FIX the existing combo-key bug: in batches/[id]/page.tsx `comboLabel` and in jobs-monitor.tsx,
    read `angleKey/metalKey/stoneGroup/pass` (not `angle/metal/stone`) so the monitor stops falling
    back to "render". Make test/out-gallery-group.test.ts and test/out-gallery-query.test.ts pass.
  </action>
  <verify>
    <automated>npx vitest run out-gallery-group out-gallery-query</automated>
  </verify>
  <done>Grouping uses correct keys; loadBatchGallery is completed-only + DB-only; monitor combo labels render real combos; tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Gallery Server Component + card + controls + lightbox + DB-only guard (OUT-02/03)</name>
  <files>app/(app)/batches/[id]/gallery/page.tsx, app/(app)/batches/[id]/gallery/layer-card.tsx, app/(app)/batches/[id]/gallery/gallery-controls.tsx, app/(app)/batches/[id]/gallery/preview-lightbox.tsx, test/orch-db-only.test.ts</files>
  <action>
    Create the gallery Server Component `app/(app)/batches/[id]/gallery/page.tsx`:
    `runtime="nodejs"`, `dynamic="force-dynamic"`, `requireSession()` FIRST line, await params,
    call `loadBatchGallery(id)` (IDOR-scoped; calm inline "Couldn't load these outputs" + Retry on
    null/throw, per UI-SPEC copy). Group via `groupLayers`. Render per UI-SPEC: header
    "{product} · {batch id} — Outputs" + layer count + a primary "Download full set" linking to
    the Plan-03 zip route; grouping/filter controls; grouped sections of layer cards. States:
    empty ("No finished outputs yet" → link back to monitor), partial (`info` banner "{c} of {N}
    renders done…" derived via summarizeJobs/deriveBatchStatus — NO RunPod poll), no-matches.
    Client subcomponents ("use client"):
    - layer-card.tsx: 160×160 `aspect-ratio` thumbnail, `<img src>`=`privateUrl(layer.url)`; PNG
      (stone) on the transparency checkerboard (first-party CSS pattern, neutral greys per UI-SPEC),
      JPEG/opaque (metal) on solid `--card`; format badge; group chip (diamond=accent/stone2=info/
      stone3=warning); hover Download ghost-button → `privateUrl(...)&download=1&name=...`; loading/
      error/downloading states. Whole card opens the lightbox.
    - gallery-controls.tsx: group-by toggle (Metal·Angle·Pass·Variant, default Metal) + filter
      chips read from the present Layer rows / Batch.matrix (never hardcoded); showing-count + Clear.
    - preview-lightbox.tsx: shadcn `dialog`; full-quality `<img src>`=`privateUrl(layer.url)`;
      checkerboard/solid per pass; combo title + format/pass/group chips; mono metadata panel;
      Download layer + quiet Download full set; ESC close, ←/→ prev/next within the current order,
      focus-trap; honors prefers-reduced-motion. NO third-party lightbox.
    Use ONLY official shadcn (dialog, aspect-ratio reused/added) + first-party — no new registry.
    DB-only: the gallery page must NOT import @/lib/runpod. Extend test/orch-db-only.test.ts
    DB_ONLY_FILES with `app/(app)/batches/[id]/gallery/page.tsx` (and the lightbox/card if they
    are server-imported) so the source guard covers the gallery.
  </action>
  <verify>
    <automated>npx vitest run orch-db-only && npx tsc --noEmit</automated>
  </verify>
  <done>Gallery renders grouped completed layers (checkerboard on PNG), lightbox previews + downloads work, full-set download links the zip route, gallery in DB_ONLY_FILES guard, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the "View in gallery" entry (D-5)</name>
  <files>app/(app)/batches/[id]/jobs-monitor.tsx</files>
  <action>
    Repoint the reserved Phase-4 "View in gallery" link (jobs-monitor.tsx CompletedPreview, currently
    `/batches/${batchId}?job=${jobId}`) to the new gallery route `/batches/${batchId}/gallery`
    (optionally `#${jobId}` or `?layer=` to deep-link). Keep the 160×160 completed-job thumbnail.
    This is the operator's entry from a completed job/batch into the layered browser (locked decision 5).
  </action>
  <verify>
    <automated>npx vitest run orch-db-only && npx next build</automated>
  </verify>
  <done>"View in gallery" navigates to /batches/[id]/gallery; build succeeds; monitor still DB-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → gallery SC | Authed-but-untrusted params.id (IDOR) |
| gallery → image bytes | Every `<img src>` resolves through the auth-gated /api/file proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | unauthed gallery view | mitigate | `requireSession()` first line of the gallery Server Component |
| T-05-06 | Elevation / IDOR | viewing another batch's outputs | mitigate | batch loaded by params.id; single-tenant posture (A5) documented |
| T-05-07 | Information Disclosure | rendering a raw public Blob URL | mitigate | `<img src>` always `privateUrl(pathname)` → /api/file proxy; never the worker `image_url` |
| T-05-09 | Tampering | gallery re-fetching terminal jobs from RunPod | mitigate | DB-only Server Component; gallery added to the orch-db-only source guard |
</threat_model>

<verification>
- `npx vitest run out-gallery-group out-gallery-query orch-db-only` GREEN.
- `npx vitest run` full suite green; `npx tsc --noEmit` exit 0; `npx next build` succeeds.
- Manual (05-VALIDATION): open a completed batch's gallery — PNG-on-checkerboard, lightbox, layer + full-set download.
</verification>

<success_criteria>
The operator browses a batch's completed layered outputs grouped by product/metal/
angle/pass, previews any layer full-quality, downloads a single layer or the full
set, and reaches the gallery from the monitor — all DB-only and private-blob-gated.
</success_criteria>

<output>
Create `.planning/phases/05-outputs-gallery-layered-passes/05-04-SUMMARY.md` when done.
</output>
