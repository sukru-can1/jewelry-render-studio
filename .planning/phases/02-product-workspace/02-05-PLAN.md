---
phase: 02-product-workspace
plan: 05
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - lib/settings/actions.ts
  - app/(app)/admin/settings/page.tsx
  - app/(app)/admin/settings/settings-forms.tsx
  - test/settings-edit.test.ts
autonomous: true
requirements: [DATA-04]
user_setup: []

must_haves:
  truths:
    - "An Admin can view and edit camera views, metals, stone types, and quality presets and save them"
    - "An Operator is blocked from saving settings by a server-side requireRole('Admin') check, not just hidden UI"
    - "Invalid edits (focal<=0, bad hex, fstop out of range) are rejected with the UI-SPEC copy"
    - "Saved changes persist and a note states they apply to new batches only"
  artifacts:
    - path: "lib/settings/actions.ts"
      provides: "saveCameraViews/saveMetals/saveStoneTypes/saveQualityPresets Server Actions (requireRole Admin)"
      exports: ["saveCameraViews", "saveMetals", "saveStoneTypes", "saveQualityPresets"]
    - path: "app/(app)/admin/settings/page.tsx"
      provides: "editable domain settings surface (upgrades Phase 1 read-only)"
    - path: "app/(app)/admin/settings/settings-forms.tsx"
      provides: "client editable grids (react-hook-form + zod) with sticky save"
  key_links:
    - from: "lib/settings/actions.ts"
      to: "requireRole('Admin')"
      via: "first line of every settings action"
      pattern: "requireRole\\(\\s*[\"']Admin[\"']\\s*\\)"
    - from: "lib/settings/actions.ts"
      to: "prisma.cameraView/metal/stoneType/qualityPreset"
      via: "validated update/upsert"
      pattern: "(cameraView|metal|stoneType|qualityPreset)\\.(update|upsert)"
---

<objective>
Vertical slice DATA-04: an Admin views and edits the seeded domain settings (camera views, metals, stone types, quality presets) and the changes persist for new batches. Upgrades the Phase 1 read-only settings view to editable forms, including the StoneType CRUD the StoneType seed (Plan 02-01) made non-empty.

Purpose: Closes DATA-04 and is fully independent of the product-upload slice, so it runs in parallel in Wave 1.
Output: settings Server Actions (Admin-gated, zod-validated), the editable settings page, and the client form grids.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-product-workspace/02-RESEARCH.md
@.planning/phases/02-product-workspace/02-UI-SPEC.md

@app/(app)/admin/settings/page.tsx
@app/api/admin/users/route.ts
@lib/auth/rbac.ts
@prisma/seed.ts

<interfaces>
<!-- FROM PLAN 02-01 (Wave 0): -->
<!-- lib/validation/settings.ts: cameraViewSchema (az -180..180, el -90..90, focalMm>0, fStop 0.7..32), metalSchema (hex 6-digit), stoneTypeSchema (key,label,preset?), qualityPresetSchema (samples/width/height int>0) -->
<!-- StoneType rows now seeded by prisma/seed.ts (diamond + gemstones) -->
<!-- Domain models: CameraView {key,label,azimuth,elevation,focalMm,fStop}, Metal {key,label,hex?}, StoneType {key,label,preset Json?}, QualityPreset {key,label,samples,width,height} -->
<!-- requireRole("Admin") from @/lib/auth/rbac throws 403 Response for Operators -->
<!-- Phase 1 settings page already does requireRole("Admin") then redirect("/forbidden") on 403, and findMany reads for all four tables -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings Server Actions (Admin-gated, zod-validated)</name>
  <read_first>lib/validation/settings.ts (from Plan 02-01), app/api/admin/users/route.ts, lib/auth/rbac.ts, .planning/phases/02-product-workspace/02-RESEARCH.md (Pattern 2, Settings validation)</read_first>
  <files>lib/settings/actions.ts, test/settings-edit.test.ts</files>
  <behavior>
    Each save action (test mocks @/lib/db/prisma + @/lib/auth/rbac like user-admin.test.ts):
    - Test: Admin session + valid payload → calls the matching prisma update/upsert and returns { ok:true }; then revalidatePath('/admin/settings').
    - Test: Operator session (requireRole('Admin') mock throws 403 Response) → action fails closed (returns { ok:false, forbidden:true } or rethrows the 403) and NO prisma write occurs. This is the AUTH-05 server-boundary assertion.
    - Test: invalid payload — cameraView focalMm <= 0, fStop 40 (out of 0.7..32), metal hex "xyz" — returns { ok:false, issues } and NO write; assert the exact copy strings are surfaced (focal/fstop/hex).
    - Test: saveStoneTypes can add a new row (upsert by key) and remove omitted rows (deleteMany of keys not in the payload) — round-trips the editable list.
  </behavior>
  <action>
    Create lib/settings/actions.ts with "use server". Implement saveCameraViews(input), saveMetals(input), saveStoneTypes(input), saveQualityPresets(input). EACH begins with requireRole("Admin") as its FIRST line (decision: server boundary is the action, not the page redirect — AUTH-05 defense-in-depth). Wrap requireRole in the same Response-catch pattern the admin users route uses; map a 403 to a returned { ok:false, forbidden:true }. Validate each row with the corresponding schema from lib/validation/settings.ts (array of rows → validate each; collect issues). On success, upsert each row by its unique key (prisma.cameraView.upsert / metal.upsert / qualityPreset.upsert) inside a $transaction; for stone types, upsert provided rows AND deleteMany rows whose key is not in the payload (the editor can remove). revalidatePath("/admin/settings") and return { ok:true }. Changes apply to NEW batches only — do NOT propagate retroactively (Pitfall 7).
  </action>
  <verify>
    <automated>npx vitest run test/settings-edit.test.ts</automated>
  </verify>
  <done>each action requireRole('Admin') first; Admin valid → persists; Operator → forbidden no-write; invalid → issues no-write; stoneTypes add/remove round-trips; test green.</done>
  <acceptance_criteria>
    - `npx vitest run test/settings-edit.test.ts` passes
    - Operator session produces no prisma write (403 fail-closed)
    - focalMm<=0 / fStop=40 / bad hex each rejected with the UI-SPEC copy
    - saveStoneTypes deletes omitted keys and upserts present keys
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Editable settings surface (upgrade Phase 1 view)</name>
  <read_first>app/(app)/admin/settings/page.tsx, .planning/phases/02-product-workspace/02-UI-SPEC.md (§5 Domain settings EDIT, Number-field grid, Hex + swatch editor, Copywriting Contract)</read_first>
  <files>app/(app)/admin/settings/page.tsx, app/(app)/admin/settings/settings-forms.tsx</files>
  <action>
    Add the required shadcn blocks: `npx shadcn@latest add tabs popover` (official registry only). (Input/Button/Table/Form/Tooltip/Separator already seeded in Phase 1.)

    Upgrade app/(app)/admin/settings/page.tsx: keep its requireRole("Admin")-then-redirect("/forbidden") guard and the four findMany reads UNCHANGED, but render the data into the editable <SettingsForms/> client component instead of the static read-only tables. Add the StoneType findMany read (prisma.stoneType.findMany) to the existing Promise.all. Keep runtime nodejs + dynamic force-dynamic.

    Create settings-forms.tsx ("use client"): four sections (tabs or stacked per UI-SPEC §5) each with a react-hook-form + zod resolver editable grid:
    - Camera views: rows view1–4, each a Number-field grid (az / el / focalMm / fStop), mono inputs; inline errors using the UI-SPEC copy.
    - Metals: white/yellow/red — editable label + Hex + swatch editor (24px swatch live preview + mono hex input + popover); invalid hex error copy.
    - Stone types: editable list — add/edit/remove rows (key + label), mono identifiers; uses saveStoneTypes.
    - Quality presets: editable mono sample counts; default 1920×1920 shown mono.
    A sticky save bar appears when any section is dirty: "Save changes" primary + "Discard" ghost. On save call the matching Server Action; on { ok:true } toast "Changes saved." (sonner) + a quiet note "Changes apply to new batches, not to renders already created."; on failure toast "Couldn't save changes. Try again."; on { forbidden } show the calm 403 message (defense-in-depth — page already gates).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx next build 2>&amp;1 | Select-String -Pattern "Compiled|error" | Select-Object -First 5</automated>
  </verify>
  <done>settings page renders editable grids for all four domain tables incl. StoneType add/remove; sticky save bar; validation copy inline; applies-to-new-batches note; Admin-gated; build + tsc clean.</done>
  <acceptance_criteria>
    - settings page Promise.all now includes prisma.stoneType.findMany
    - settings-forms.tsx imports the four save actions and renders editable inputs for az/el/focal/fstop, metal hex, stone type rows, quality samples
    - "Changes apply to new batches, not to renders already created." present
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → settings actions | Operator may attempt privilege escalation to edit domain settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Elevation of Privilege | lib/settings/actions.ts | mitigate | requireRole("Admin") as the FIRST line of every save action (server boundary, not page redirect — AUTH-05) |
| T-02-10 | Tampering | settings payloads | mitigate | zod per-row validation (focal/fstop/el/az ranges, 6-digit hex, positive ints) before any write |
| T-02-11 | Tampering | stone type key (stored XSS) | mitigate | React escapes; keys/labels rendered as text; no dangerouslySetInnerHTML |
| T-02-SC | Tampering | shadcn tabs/popover add | mitigate | official shadcn registry only (first-party source copy) |
</threat_model>

<verification>
- `npx vitest run test/settings-edit.test.ts` green (Admin saves; Operator 403 no-write; invalid rejected)
- `npx tsc --noEmit` exits 0; `npx next build` compiles
- Manual (phase gate): Operator deep-links /admin/settings → /forbidden; Admin edits a camera view + saves → persists
</verification>

<success_criteria>
- Admin can edit all four domain tables incl. StoneType add/remove and save
- Operator blocked server-side (AUTH-05)
- Invalid edits rejected with UI-SPEC copy; changes apply to new batches only
</success_criteria>

<output>
Create `.planning/phases/02-product-workspace/02-05-SUMMARY.md` when done
</output>
