---
phase: 02-product-workspace
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - lib/products/actions.ts
  - app/(app)/products/new/page.tsx
  - app/(app)/products/model-dropzone.tsx
  - app/(app)/products/create-product-form.tsx
  - test/product-create.test.ts
autonomous: true
requirements: [PROD-01]
user_setup: []

must_haves:
  truths:
    - "An operator can enter a product name, drag a model file, and watch it upload directly to private Blob"
    - "On a successful upload the product persists with its model blob pathname, format, and status needs_inspection"
    - "The model is uploaded with access:'private' on both the client upload() and the token route"
  artifacts:
    - path: "lib/products/actions.ts"
      provides: "createProduct Server Action (requireSession + zod + prisma.product.create)"
      exports: ["createProduct"]
    - path: "app/(app)/products/model-dropzone.tsx"
      provides: "client dropzone using @vercel/blob/client upload() with access:'private' + progress"
    - path: "app/(app)/products/new/page.tsx"
      provides: "new-product surface (name field + dropzone + Create product)"
  key_links:
    - from: "app/(app)/products/model-dropzone.tsx"
      to: "/api/blob/upload"
      via: "upload(file.name, file, {access:'private', handleUploadUrl})"
      pattern: "access:\\s*[\"']private[\"']"
    - from: "lib/products/actions.ts"
      to: "prisma.product"
      via: "create with modelUrl=pathname, status='needs_inspection'"
      pattern: "product\\.create"
---

<objective>
Vertical slice PROD-01: an operator creates a product and uploads its 3D model via authenticated direct-to-Blob client upload. After this slice an operator can do something new end-to-end — turn an empty workspace into a product with a private model attached, ready for inspection.

Purpose: This is the entry point of the whole workspace. It proves the private-upload path (the load-bearing SEC-02 + 4.5MB-bypass pattern) works end-to-end.
Output: createProduct Server Action, the model dropzone client component, and the /products/new surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-product-workspace/02-RESEARCH.md
@.planning/phases/02-product-workspace/02-UI-SPEC.md

@app/api/blob/upload/route.ts
@app/(app)/admin/users/create-user-dialog.tsx
@app/(app)/layout.tsx
@lib/auth/rbac.ts

<interfaces>
<!-- FROM PLAN 02-01 (Wave 0): -->
<!-- lib/validation/product.ts: createProductSchema { name, modelPathname, modelFormat in glb|fbx|blend|obj|stl } -->
<!-- app/api/blob/upload/route.ts now returns access:'private' in onBeforeGenerateToken -->
<!-- @vercel/blob/client upload(pathname, file, {access:'private', handleUploadUrl:'/api/blob/upload', contentType, onUploadProgress}) -> {url, pathname, contentType} -->
<!-- Product model: { id, name, modelUrl?, modelFormat?, inspectionUrl?, status @default("draft"), assignments[] } -->
<!-- requireSession() from @/lib/auth/rbac throws a 401 Response when unauthenticated -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createProduct Server Action</name>
  <read_first>lib/validation/product.ts (from Plan 02-01), app/api/admin/users/route.ts, lib/auth/rbac.ts, .planning/phases/02-product-workspace/02-RESEARCH.md (Pattern 2)</read_first>
  <files>lib/products/actions.ts, test/product-create.test.ts</files>
  <behavior>
    createProduct(input) (test mocks @/lib/db/prisma + @/lib/auth/rbac exactly like user-admin.test.ts):
    - Test: zod-valid input { name, modelPathname, modelFormat:'glb' } calls prisma.product.create with data.modelUrl === modelPathname, data.modelFormat === 'glb', data.status === 'needs_inspection'.
    - Test: invalid input (empty name, or modelFormat not in the allowed set) returns { ok:false, issues } and does NOT call prisma.product.create.
    - Test: unauthenticated (requireSession mock throws 401 Response) propagates fail-closed (the action throws / returns the denial; product.create not called).
  </behavior>
  <action>
    Create lib/products/actions.ts with "use server". Implement createProduct(input: unknown): requireSession() FIRST line (fail-closed, AUTH boundary), safeParse with createProductSchema, on failure return { ok:false, issues: parsed.error.flatten() }, on success prisma.product.create({ data: { name, modelUrl: modelPathname, modelFormat, status: "needs_inspection" } }), then revalidatePath("/products") and return { ok:true, id }. (The redirect to /products/[id] is performed by the calling client form after success so the action stays unit-testable; if you redirect() inside the action instead, the test must account for the thrown NEXT_REDIRECT — prefer returning the id.) Store result.pathname only; never store result.url (the non-public URL). Node runtime by default for Server Actions.
  </action>
  <verify>
    <automated>npx vitest run test/product-create.test.ts</automated>
  </verify>
  <done>createProduct persists modelUrl/modelFormat/status='needs_inspection' on valid input, rejects invalid, fails closed when unauth; test green.</done>
  <acceptance_criteria>
    - `npx vitest run test/product-create.test.ts` passes
    - Asserts prisma.product.create called with status:'needs_inspection' and modelUrl===modelPathname
    - Invalid payload → no product.create call
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Model dropzone + new-product surface</name>
  <read_first>.planning/phases/02-product-workspace/02-UI-SPEC.md (§2 New product + model upload, Model dropzone states, Copywriting Contract), app/(app)/admin/users/create-user-dialog.tsx</read_first>
  <files>app/(app)/products/model-dropzone.tsx, app/(app)/products/create-product-form.tsx, app/(app)/products/new/page.tsx</files>
  <action>
    Build the model upload UI per UI-SPEC §2. First add the required shadcn block: `npx shadcn@latest add progress` (official registry only).

    model-dropzone.tsx ("use client"): native drag-and-drop zone (no Radix) accepting GLB/FBX/BLEND/OBJ/STL. On file selected/dropped, validate extension client-side (reject others with the exact copy "That file type isn't supported. Use GLB, FBX, BLEND, OBJ, or STL.") and size <= 50MB ("That model is larger than the 50 MB limit."). Then call upload(file.name, file, { access: "private", handleUploadUrl: "/api/blob/upload", contentType: file.type || "application/octet-stream", onUploadProgress: ({percentage}) => setProgress(percentage) }) from @vercel/blob/client. access:'private' is REQUIRED on the client side too (decision #2 — both sides). Render the five states: idle (dashed border, cloud-upload icon, the idle + size-hint copy), drag-over (2px teal dashed + faint teal tint), uploading (filename + Progress bar + mono % + Cancel ghost), success (check + filename + mono size + Replace ghost), error ("Couldn't upload that model. Check your connection and try again." + Try again). On success surface { pathname, format } up to the parent form via a callback; persist NOTHING here and never render result.url.

    create-product-form.tsx ("use client"): single-column form with a required Product name text field (inline-validated) and the dropzone. The primary button "Create product" is disabled until a non-empty name AND a successful upload exist. On click, call the createProduct Server Action with { name, modelPathname: pathname, modelFormat: format }; on { ok:true, id } redirect (router.push) to /products/${id}; on { ok:false } show the field issues.

    new/page.tsx: an async Server Component that calls requireSession() then renders the heading "New product" + <CreateProductForm/>. runtime nodejs; this is the /products/new route.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx next build 2>&amp;1 | Select-String -Pattern "Compiled|error" | Select-Object -First 5</automated>
  </verify>
  <done>/products/new renders the name field + dropzone; upload uses access:'private'; Create product disabled until name+upload; success redirects to product detail; all five dropzone states present; build + tsc clean.</done>
  <acceptance_criteria>
    - Grep `access:\s*["']private["']` in app/(app)/products/model-dropzone.tsx returns a match
    - Grep `handleUploadUrl` and `onUploadProgress` present in the dropzone
    - The Create product button has a disabled condition gated on name + successful upload
    - Exact wrong-type and too-large copy strings present (match UI-SPEC)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → createProduct action | untrusted product name + model ref |
| browser → /api/blob/upload | direct-to-Blob token exchange |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | createProduct action | mitigate | requireSession() first line (fail-closed); zod-validated payload |
| T-02-06 | Info Disclosure | model upload | mitigate | access:'private' on BOTH client upload() and token route; store pathname not url |
| T-02-07 | Tampering/DoS | oversized/wrong-type upload | mitigate | client extension allowlist + 50MB cap; token route allowedContentTypes; addRandomSuffix |
| T-02-08 | Tampering | product name (stored XSS) | mitigate | React escapes by default; name rendered as text only |
| T-02-SC | Tampering | shadcn progress add | mitigate | official shadcn registry only (first-party source copy, not an npm dep) |
</threat_model>

<verification>
- `npx vitest run test/product-create.test.ts` green
- `npx tsc --noEmit` exits 0; `npx next build` compiles
- Manual (phase gate): upload a real ~50MB model in dev → product created with status needs_inspection; incognito fetch of the blob URL is NOT readable (private)
</verification>

<success_criteria>
- Operator can create a product + upload a private model end-to-end
- Product persists with modelUrl (pathname), modelFormat, status='needs_inspection'
- Both client and token-route sides set access:'private'
</success_criteria>

<output>
Create `.planning/phases/02-product-workspace/02-02-SUMMARY.md` when done
</output>
