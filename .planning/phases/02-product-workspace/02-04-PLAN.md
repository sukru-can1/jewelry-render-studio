---
phase: 02-product-workspace
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - lib/products/assignments.ts
  - app/(app)/products/[id]/group-assignment.tsx
  - app/(app)/products/[id]/page.tsx
  - app/(app)/products/page.tsx
  - app/(app)/products/product-card.tsx
  - test/assignment-save.test.ts
  - test/product-list.test.ts
autonomous: true
requirements: [PROD-03, PROD-04, PROD-05]
user_setup: []

must_haves:
  truths:
    - "An operator assigns each detected object to alloycolour/diamond/stone2/stone3 and saves it to the product"
    - "Saved assignments persist as one ObjectGroupAssignment row per group with objectTokens being object signatures"
    - "The saved token lists are the exact shape the Phase-3 recipe builder consumes for holdout passes"
    - "An operator browses a product-card grid sorted by recency and reopens any product"
    - "Saving recomputes product status to ready (required groups covered) or needs_groups"
  assumptions:
    - "The 'ready' rule (alloycolour >=1 AND no clearly-stone mesh left unassigned) is an ASSUMPTION carried from RESEARCH Open Q4. Phase 3 is expected to revisit/confirm this heuristic when it consumes the token shape for holdout passes. Record this assumption explicitly in 02-04-SUMMARY so Phase-3 planning re-examines it."
  artifacts:
    - path: "lib/products/assignments.ts"
      provides: "saveAssignments + loadAssignments + token-assist suggestion wiring"
      exports: ["saveAssignments"]
    - path: "app/(app)/products/[id]/group-assignment.tsx"
      provides: "per-object group picker + token-assist + sticky save"
    - path: "app/(app)/products/page.tsx"
      provides: "products list (recency-sorted card grid + reopen)"
    - path: "app/(app)/products/product-card.tsx"
      provides: "product card (thumbnail/name/status/updated-at, whole-card reopen)"
  key_links:
    - from: "lib/products/assignments.ts"
      to: "prisma.objectGroupAssignment"
      via: "delete-and-recreate one row per group in a transaction"
      pattern: "objectGroupAssignment\\.(createMany|deleteMany)"
    - from: "app/(app)/products/page.tsx"
      to: "prisma.product"
      via: "findMany orderBy updatedAt/createdAt desc → product-card grid"
      pattern: "product\\.findMany"
    - from: "app/(app)/products/product-card.tsx"
      to: "/products/[id]"
      via: "whole-card link to reopen"
      pattern: "/products/"
---

<objective>
Vertical slices PROD-03/04 (assign objects to groups + persist the shape that drives holdout passes) and PROD-05 (browse + reopen products). After this slice the operator can complete the full workspace loop: create → upload → inspect → group → reopen — every PROD requirement closed.

Purpose: Assignment is the core deliverable of Phase 2 and the data bridge to Phase 3's batch builder. The products list makes the workspace persistent and re-enterable.
Output: saveAssignments Server Action, the group-assignment client surface (in the detail Groups tab), the products list page, and the product card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-product-workspace/02-RESEARCH.md
@.planning/phases/02-product-workspace/02-UI-SPEC.md

@prisma/schema.prisma
@lib/auth/rbac.ts
@app/(app)/admin/settings/page.tsx

<interfaces>
<!-- FROM PLAN 02-01: lib/tokens.ts suggestGroup(signature); lib/validation/product.ts assignmentSchema { productId, groups: { alloycolour|diamond|stone2|stone3: string[] } } -->
<!-- FROM PLAN 02-01: lib/inventory.ts parseInventory output objects carry .signature (lowercased name + material slots) -->
<!-- FROM PLAN 02-03: app/(app)/products/[id]/page.tsx exists with Overview|Materials|Groups tabs and a Groups mount point; latest inspection.inventory is available -->
<!-- prisma ObjectGroupAssignment { id, productId, group String, objectTokens String[] } -->
<!-- PROD-04 (Phase 2 scope): persist the token shape ONLY. Recipe generation (objectTokens → material_map/holdout contains) is Phase 3 (deferred). -->
<!-- Product has createdAt; there is NO updatedAt column — sort by createdAt desc unless an updatedAt is added (do NOT add a migration here; use createdAt). -->
<!-- "ready" rule (RESEARCH A5 / Open Q4 — ASSUMPTION): ready when alloycolour has >=1 object AND no clearly-stone mesh left unassigned; else needs_groups. Phase 3 revisits. -->
<!-- Product.status @default("draft") — the status-pill mapping MUST include 'draft' (treat as neutral / needs_inspection) so freshly-created products render a pill. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: saveAssignments + load + tokens test (PROD-03/04)</name>
  <read_first>prisma/schema.prisma (ObjectGroupAssignment), lib/tokens.ts + lib/validation/product.ts (from Plan 02-01), .planning/phases/02-product-workspace/02-RESEARCH.md (Pattern 5 + 6, Open Q4)</read_first>
  <files>lib/products/assignments.ts, test/assignment-save.test.ts</files>
  <behavior>
    saveAssignments(productId, groups) (test mocks @/lib/db/prisma + @/lib/auth/rbac):
    - Test: requireSession() first; given groups { alloycolour:["band_metal gold"], diamond:["center_diamond"], stone2:[], stone3:[] } runs a $transaction that deleteMany existing rows for productId then createMany one row PER non-empty group with objectTokens set to the provided signatures (alloycolour + diamond rows created; empty groups skipped).
    - Test: PROD-04 shape assertion — the persisted objectTokens are object SIGNATURES (lowercased name+material strings), NOT row ids — i.e. exactly the `contains` tokens Phase-3 holdout will match. Assert a token equals "center_diamond" style string, never a cuid.
    - Test: invalid group key (not in the enum) → zod reject, no write.
    - Test: recomputes product.status — alloycolour has >=1 and no stone-typed mesh unassigned → 'ready'; otherwise 'needs_groups' (assert both branches with a small inventory input).
    loadAssignments(productId): returns the saved rows as a group→tokens map (round-trips a save).
  </behavior>
  <action>
    Create lib/products/assignments.ts ("use server"). saveAssignments(productId, groups): requireSession() FIRST; validate with assignmentSchema; run prisma.$transaction([ deleteMany({where:{productId}}), createMany({ data: Object.entries(groups).filter(([,t])=>t.length).map(([group,objectTokens])=>({productId,group,objectTokens})) }) ]) — delete-and-recreate per Pattern 5. Then recompute Product.status: 'ready' when alloycolour has >=1 token and every detected stone-ish mesh is grouped (use the latest inspection inventory passed in or loaded), else 'needs_groups'; update product.status; revalidatePath(`/products/${productId}`) and `/products`. Add loadAssignments(productId) returning a { group: tokens } map. Export suggestGroup re-use is in the UI (Task 2), not here.

    IMPORTANT (PROD-04): objectTokens MUST be the object signatures (matching render_scene.py object_signature), because Phase 3's lib/enterprise-recipes.ts consumes them directly as holdout `contains` tokens. Do NOT store ids. Phase 2 persists the shape ONLY — do NOT generate recipes here (deferred to Phase 3).

    ASSUMPTION (RESEARCH Open Q4 — surface in 02-04-SUMMARY): the 'ready' rule (alloycolour >=1 AND no clearly-stone mesh unassigned) is an assumed heuristic, NOT a confirmed contract. Keep it as the recompute rule for Phase 2, but explicitly note in the SUMMARY that Phase 3 must revisit/confirm it once it consumes the token shape for holdout passes (so a later phase does not silently inherit an unvalidated readiness definition).
  </action>
  <verify>
    <automated>npx vitest run test/assignment-save.test.ts</automated>
  </verify>
  <done>saveAssignments upserts one row per non-empty group with signature tokens, recomputes ready/needs_groups, rejects bad group; load round-trips; PROD-04 token-shape asserted; 'ready' rule flagged as Open-Q4 assumption in SUMMARY guidance; test green.</done>
  <acceptance_criteria>
    - `npx vitest run test/assignment-save.test.ts` passes
    - Transaction does deleteMany then createMany one row per non-empty group
    - objectTokens asserted to be signatures (not ids)
    - status recompute covers both 'ready' and 'needs_groups' branches
    - SUMMARY records the 'ready' rule as a RESEARCH Open-Q4 assumption for Phase 3 to revisit
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Group-assignment surface (Groups tab) + token-assist</name>
  <read_first>.planning/phases/02-product-workspace/02-UI-SPEC.md (§4 Object→group assignment, Group-assignment row, Token-assist bar, Group-meaning helper, Copywriting Contract), lib/tokens.ts (from Plan 02-01), app/(app)/products/[id]/page.tsx (Groups mount point from Plan 02-03)</read_first>
  <files>app/(app)/products/[id]/group-assignment.tsx, app/(app)/products/[id]/page.tsx</files>
  <action>
    Add the required shadcn blocks: `npx shadcn@latest add radio-group checkbox command` (official registry only; skip any already present).

    group-assignment.tsx ("use client"): the core P2 surface per UI-SPEC §4. For each MESH object from the inspection inventory render a Group-assignment row: object name (mono) + a 5-option group picker (alloycolour / diamond / stone2 / stone3 / unassigned) via radio-group, with group-colored chips (alloycolour=neutral, diamond=teal accent, stone2=info, stone3=warning, unassigned=dashed neutral). Show a dotted-teal "Suggested: {group} → Accept" hint computed via suggestGroup(object.signature) — never auto-applied. Add the Token-assist bar (bulk "Assign all matching *metal* → alloycolour" with a preview count + toast undo) and the Group-meaning helper popover/tooltip ("alloycolour = the metal pass · diamond = the center stone · stone2 / stone3 = side stones. Unassigned objects won't appear in any pass."). A sticky save bar appears when dirty: "Save groups" primary + "Discard" ghost. On save call saveAssignments(productId, groups); on success toast "Groups saved." and reflect the new status pill. Incomplete-on-save → non-blocking inline note "{n} objects are still unassigned and won't appear in any pass." (operator may intentionally leave decorative objects unassigned). Use the intro hint "Assign each object to a group, then save. Groups decide which parts are rendered or held out in each pass."

    Wire the Groups tab in [id]/page.tsx to mount <GroupAssignment/> with the inventory objects + existing assignments (from loadAssignments) as initial state. Empty/partial state: unassigned rows with dashed neutral chips + the top hint.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx next build 2>&amp;1 | Select-String -Pattern "Compiled|error" | Select-Object -First 5</automated>
  </verify>
  <done>Groups tab lists every MESH object with a group picker + token-assist suggestions + bulk action + group-meaning helper + sticky save; save persists + updates status pill; copy matches UI-SPEC; build + tsc clean.</done>
  <acceptance_criteria>
    - group-assignment renders a radio-group per object with the five options and group-colored chips
    - suggestGroup is used to render "Suggested: {group}" hints (not auto-applied)
    - sticky save bar calls saveAssignments; "Groups saved." toast on success
    - incomplete note + group-meaning helper copy present (match UI-SPEC)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Products list + product card (PROD-05)</name>
  <read_first>.planning/phases/02-product-workspace/02-UI-SPEC.md (§1 Products list, Product card, Copywriting Contract), app/(app)/products/page.tsx (current placeholder), app/(app)/admin/settings/page.tsx (server-component read pattern), prisma/schema.prisma (Product.status @default("draft"))</read_first>
  <files>app/(app)/products/page.tsx, app/(app)/products/product-card.tsx, test/product-list.test.ts</files>
  <action>
    Replace the placeholder app/(app)/products/page.tsx with the real list per UI-SPEC §1: async Server Component, runtime nodejs, dynamic force-dynamic, requireSession() first. prisma.product.findMany({ orderBy:{ createdAt:"desc" }, include:{ _count:{ select:{ assignments:true } } } }) (sort by createdAt — there is no updatedAt column; do NOT add a migration). Header: Heading "Products" + count (mono) on the left, primary "New product" button (links to /products/new) on the right. Body: responsive grid (3-up >=1024, 2-up >=640, 1-up below) of <ProductCard/>. States: loading (6 skeleton cards), empty (centered "No products yet" + body "Upload your first jewelry model to inspect it and assign its parts to render groups." + [New product]), error (inline card + Retry).

    product-card.tsx: a card built on the Phase 1 interactive Card — 160×160 thumbnail (or placeholder glyph when no model thumbnail; deliver any thumbnail via /api/file, never a public url) · Body product name · mono model filename · status pill from product.status · relative created-at (mono). The whole card is a link to /products/[id] (reopen). Hover lift + focus-within per UI-SPEC.

    Status-pill mapping (MUST cover the schema default so no value renders unmapped): draft=neutral (treat as "needs inspection" — Product.status @default("draft"), so a freshly-created product still shows a pill), needs_inspection=neutral, inspecting=info, needs_groups=warning, ready=success, inspection_failed=destructive. Use a default/neutral fallback for any unknown value rather than rendering blank.

    test/product-list.test.ts: mock @/lib/db/prisma + @/lib/auth/rbac; assert the page's findMany is called with orderBy createdAt desc and includes the assignment count, and that products render with their status. (Integration-style, mirroring user-admin.test.ts mocking.) Include a product with status 'draft' in the mock data and assert it renders a (neutral) pill rather than an empty/unmapped one.
  </action>
  <verify>
    <automated>npx vitest run test/product-list.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>/products shows a recency-sorted product-card grid with status pills (draft mapped to neutral); whole card reopens to detail; empty + loading + error states; New product CTA; list test green; tsc clean.</done>
  <acceptance_criteria>
    - `npx vitest run test/product-list.test.ts` passes (orderBy createdAt desc asserted)
    - product-card whole-card links to /products/[id] and renders the correct status pill mapping
    - 'draft' status maps to a neutral pill (not unmapped/blank) — asserted in test
    - empty-state copy "No products yet" + body present (match UI-SPEC)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → saveAssignments | untrusted productId + group payload |
| operator → /products/[id] | reopening any product by id (IDOR surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Spoofing | saveAssignments | mitigate | requireSession() first line; assignmentSchema (group enum) before any write |
| T-02-18 | Tampering | injected group key / object token | mitigate | zod group enum rejects unknown groups; objectTokens stored as opaque strings, React-escaped on render |
| T-02-19 | IDOR | /products/[id] reopen + product list | accept | single-tenant internal team (REQUIREMENTS out-of-scope: multi-tenant isolation); requireSession() still required; per-workspace ownership is a v2 concern |
| T-02-20 | Tampering | product/object names (stored XSS) | mitigate | React escapes; mono text render; no dangerouslySetInnerHTML |
| T-02-SC | Tampering | shadcn radio-group/checkbox/command | mitigate | official shadcn registry only (first-party source copy) |
</threat_model>

<verification>
- `npx vitest run test/assignment-save.test.ts test/product-list.test.ts` green
- `npx tsc --noEmit` exits 0; `npx next build` compiles
- Full suite: `npm test` green at wave merge
- Manual (phase gate): assign groups on a real inspected product + save → status pill becomes ready; reopen from /products grid restores the saved assignment
</verification>

<success_criteria>
- Operator assigns objects to the four groups + saves (PROD-03)
- Saved objectTokens are signatures = the Phase-3 holdout shape (PROD-04, persist-only)
- Operator browses + reopens products from a recency-sorted grid (PROD-05)
- Status pills cover the schema default ('draft') with no unmapped value
</success_criteria>

<output>
Create `.planning/phases/02-product-workspace/02-04-SUMMARY.md` when done
</output>
</output>
