---
phase: 06-compositing-deliverable
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - app/(app)/batches/[id]/compositing/page.tsx
  - app/(app)/batches/[id]/compositing/compositor.tsx
  - app/(app)/batches/[id]/compositing/flatten-action.tsx
  - app/(app)/batches/[id]/segment-switcher.tsx
  - test/comp-page-db-only.test.ts
files_created_shared:
  - app/(app)/batches/[id]/compositing/page.tsx
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03]
must_haves:
  truths:
    - "An operator can open /batches/[id]/compositing and see one LayerCompositor per (angle × metal) variant"
    - "Each compositor stacks the metal layer (z=0, floor) under each stone PNG (z=1+) via absolutely-positioned <img> on the gallery checkerboard"
    - "Every layer defaults ON; toggling a layer flips its <img> visibility/opacity 1↔0"
    - "Every <img src> resolves through privateUrl() → /api/file (auth-gated proxy) — never a raw public Blob URL"
    - "The compositing page requires a session, IDOR-loads the batch by params.id, and is DB-only (never imports the GPU dispatch client)"
    - "A Monitor · Gallery · Compositing segment switcher lets an operator move between the three batch surfaces"
    - "The per-variant Flatten & download action calls the Plan-01 flatten route and surfaces WARN (not silent) on a gate failure"
  artifacts:
    - path: "app/(app)/batches/[id]/compositing/page.tsx"
      provides: "DB-only Server Component compositing route (auth + IDOR + force-dynamic)"
      contains: "requireSession"
    - path: "app/(app)/batches/[id]/compositing/compositor.tsx"
      provides: "COMP-01 client LayerCompositor (stacked <img> + per-layer toggles)"
      contains: "privateUrl"
    - path: "app/(app)/batches/[id]/segment-switcher.tsx"
      provides: "Monitor · Gallery · Compositing navigation"
      contains: "Compositing"
  key_links:
    - from: "app/(app)/batches/[id]/compositing/page.tsx"
      to: "loadBatchGallery + groupVariantsForCompositing"
      via: "DB layers grouped into (angle×metal) variants for the compositor"
      pattern: "groupVariantsForCompositing"
    - from: "app/(app)/batches/[id]/compositing/compositor.tsx"
      to: "/api/file"
      via: "privateUrl(layer.url) on every stacked <img>"
      pattern: "privateUrl"
    - from: "app/(app)/batches/[id]/compositing/flatten-action.tsx"
      to: "POST /batches/[id]/flatten"
      via: "fetch the Plan-01 flatten route, render WARN on ok:false"
      pattern: "flatten"
---

<objective>
Build the COMP-01 surface: a `compositing/` route segment alongside `gallery/`
that DB-only-loads the batch, groups its completed layers into (angle × metal)
variants, and renders one LayerCompositor per variant — stacked, toggle-able
`<img>` layers on the inherited checkerboard. Wire the per-variant Flatten &
download action to the Plan-01 route (showing WARN, not silent), and add the
Monitor · Gallery · Compositing segment switcher.

Purpose: give operators the in-browser preview (COMP-01) and the entry point to
flatten (COMP-02→03) on a dedicated, write-capable surface that mirrors the
gallery page's auth/load/force-dynamic discipline exactly.
Output: the compositing page + 3 client components + segment switcher + a
DB-only source guard for the new page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compositing-deliverable/06-UI-SPEC.md
@.planning/phases/06-compositing-deliverable/06-RESEARCH.md

<read_first>
@app/(app)/batches/[id]/gallery/page.tsx
@app/(app)/batches/[id]/gallery/layer-card.tsx
@lib/gallery/query.ts
@lib/compositing/variants.ts
@lib/blob.ts
@test/orch-db-only.test.ts
</read_first>

<interfaces>
<!-- Reuse from Plan 01: groupVariantsForCompositing(layers) → Variant[]
     ({key, angleKey, metalKey, base?, overlays[]}), CompositingLayer{pass,stoneGroup,url,format}. -->
<!-- Reuse from gallery: loadBatchGallery(id) → {productName, jobCounts, totalJobs, layers[]}; layer.url is a PATHNAME. -->
<!-- Persistence is BLOB-ONLY (per 06-01 Task-1 decision): a flatten is written to a deterministic private
     blob path `renders/<batchId>/deliverables/<angle>_<metal>.png` via putPrivate(allowOverwrite:true) and
     discovered by `list({prefix:'renders/<batchId>/deliverables/'})` — NOT a Layer row with isFlattened=true
     (Layer.jobId is @unique + a required FK to Job.id, so a synthetic-jobId row is infeasible). -->
<!-- Reuse helpers (gallery/layer-card.tsx): comboLabel(combo), downloadName(layer), GROUP_CHIP map
     (diamond=accent, stone2=info, stone3=warning). privateUrl(pathname) from lib/blob.ts. -->
<!-- Inherited UI: gallery-checkerboard (app/globals.css), shadcn alert/toggle/button/skeleton/sonner/aspect-ratio. -->
<!-- Flatten route (Plan 01): POST /batches/[id]/flatten?angle=&metal=[&force=1]
     → 200 {ok:true, deliverable:{url,format,width,height}} | 200 {ok:false, warnings:FlattenWarning[]}. -->
<!-- FlattenWarning.code ∈ missing-base|dimension-mismatch|empty-layer|no-overlays; .detail carries the mono numbers. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compositing page (DB-only Server Component) + segment switcher + DB-only guard</name>
  <files>app/(app)/batches/[id]/compositing/page.tsx, app/(app)/batches/[id]/segment-switcher.tsx, test/comp-page-db-only.test.ts</files>
  <action>
    Create the compositing page mirroring gallery/page.tsx (per UI-SPEC §Layout decision):
    `export const runtime = "nodejs"; export const dynamic = "force-dynamic";` — requireSession()
    FIRST, then IDOR-load via loadBatchGallery(params.id); a null result renders the calm
    "Couldn't load this batch for compositing. … [Retry]" card (reuse the gallery fallback shape).
    Group `gallery.layers` with `groupVariantsForCompositing` (Plan 01), then group those variants
    by metal for the section layout (per UI-SPEC: reuse the angle×metal idea — render variants
    grouped under each metalKey). Header = `{productName} · {id} — Compositing` (mono id) +
    `{v} variants · {d} flattened` summary. The `{d} flattened` count is BLOB-DERIVED: under the
    blob-only persistence (06-01), `loadBatchGallery().layers[].isFlattened` stays all-false, so do
    NOT count from it. Instead derive `{d}` from a blob `list({prefix:'renders/<batchId>/deliverables/'})`
    in the Server Component (count the discovered deliverable blobs, optionally matched against the
    enumerated variant keys). Render the "Download all deliverables" primary action slot
    (the action component itself ships in Plan 03 — render a disabled placeholder or import it
    only if 06-03 has landed; keep this page compiling standalone by rendering the switcher +
    compositor list now and leaving a clearly-marked slot for the batch download button).
    States per UI-SPEC §1: loading skeleton, empty ("No composable layers yet" → View batch
    progress), error (DB read failed → Retry), in-progress info banner (DB counts only, NO RunPod).
    Create segment-switcher.tsx: a small client (or link) Monitor · Gallery · Compositing nav
    pointing at `/batches/[id]`, `/batches/[id]/gallery`, `/batches/[id]/compositing`
    (inherited toggle-group or plain links); render it at the top of this page. (Adding it to the
    gallery/monitor pages is a nice-to-have; this plan only owns rendering it on the compositing page.)
    Create test/comp-page-db-only.test.ts: a source guard (readFileSync, like orch-db-only) asserting
    compositing/page.tsx imports no @/lib/runpod / submitRunPod / getRunPodStatus / cancelRunPod.
    ALSO add the compositing page path to test/orch-db-only.test.ts DB_ONLY_FILES (now that the file
    exists this wave) so the shared guard covers it as a hard gate.
  </action>
  <verify>
    <automated>npx vitest run comp-page-db-only orch-db-only --reporter=dot</automated>
  </verify>
  <done>compositing/page.tsx exists (Node, force-dynamic, requireSession first, IDOR by params.id, DB-only); segment switcher renders; both DB-only guards pass GREEN; `npx tsc --noEmit` clean for the new files.</done>
</task>

<task type="auto">
  <name>Task 2: LayerCompositor + Flatten-and-download client components (COMP-01)</name>
  <files>app/(app)/batches/[id]/compositing/compositor.tsx, app/(app)/batches/[id]/compositing/flatten-action.tsx</files>
  <action>
    compositor.tsx ("use client") — one LayerCompositor per Variant (UI-SPEC New Component
    Inventory + §2). A square `aspect-ratio` preview frame on `gallery-checkerboard` containing
    absolutely-positioned `<img>` layers: base (metal) at z=0 (the floor, default-on), each stone
    overlay stacked by ascending z (default-on). Every `src = privateUrl(layer.url)`. Beside/under
    the frame, a toggle column — one row per layer using shadcn `toggle`: an `eye`/`eye-off` glyph,
    a mono label (`metal · alloycolour`, `stone · diamond`), the inherited GROUP_CHIP, and an
    optional mono dimension. Toggling flips that layer's opacity 1↔0 (visibility:hidden), label
    dimmed when off; the metal base is visually the floor (present-but-base). States: loading
    (skeleton frame + skeleton rows), composed (default-on stack), per-layer toggled-off, per-layer
    image-load error ("Couldn't load this layer." + Retry; the rest still composes), WARN banner
    above the frame (rendered when the flatten-action reports warnings — best-effort stack still shown).
    Reuse comboLabel/GROUP_CHIP from gallery/layer-card.tsx (import them; if not already exported,
    export them there without changing behavior). 44px hit-area on every toggle (inherited rule).
    flatten-action.tsx ("use client") — the per-variant COMP-02→03 action. A secondary `Button`
    "Flatten & download". On click: POST `/batches/${id}/flatten?angle=${angleKey}&metal=${metalKey}`.
    - 200 {ok:true}: kick a download of the deliverable via `privateUrl(deliverable.url)&download=1&name=${...}`
      (an `<a download>` or programmatic anchor — reuse the /api/file attachment pattern), sonner
      toast "Downloading {filename}.", show the inline `check` "Deliverable ready" badge.
    - 200 {ok:false, warnings}: render the Composite WARN banner (shadcn `alert` + `warning` token)
      with the mono detail line per warning code (dimension-mismatch / missing-metal / empty-alpha
      copy from UI-SPEC Copywriting Contract); the button STAYS enabled (never silently blocks);
      `no-overlays` advisory may offer a "Flatten metal only" that re-POSTs with `&force=1`.
    - non-2xx / network: destructive error toast "Couldn't flatten this variant. Try again." + Retry.
    States: idle / flattening (`loader-2` "Flattening…") / ready (badge + download) / warn-present /
    error / empty-scope (disabled with reason "No metal base for this variant." when base is absent).
    Use the exact NET-NEW copy strings from UI-SPEC §Copywriting Contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | findstr /C:"compositor" /C:"flatten-action" ; echo TSC_DONE</automated>
  </verify>
  <done>LayerCompositor stacks default-on metal+stone <img> via privateUrl on the checkerboard with working per-layer toggles; flatten-action POSTs the Plan-01 route and renders ok:true (download+badge) vs ok:false (WARN banner, button still enabled) vs error (Retry); no public Blob URL constructed; tsc clean for both files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → compositing page | Untrusted batch id in the URL; page must IDOR-scope by params.id |
| browser → /api/file | Every preview <img> + download crosses the auth-gated proxy (private only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing | unauth view of compositing page | mitigate | requireSession() first line of the Server Component |
| T-06-07 | Elevation/IDOR | view another project's batch | mitigate | loadBatchGallery(params.id) scopes to the route batch; null → calm fallback |
| T-06-08 | Information Disclosure | raw public Blob URL in an <img> | mitigate | every src via privateUrl() → /api/file; DB-only source guard forbids RunPod, blob-guard posture inherited |
</threat_model>

<verification>
- `npx vitest run comp-page-db-only orch-db-only` GREEN.
- `npx tsc --noEmit` clean; the page renders the loading/empty/error/in-progress states.
- Manual: open a completed batch's /compositing, confirm stacked checkerboard preview, per-layer toggles, and a Flatten & download click round-trips to the route.
</verification>

<success_criteria>
An operator can open the compositing page for a batch, see per-(angle×metal)
variants with stacked, toggle-able metal+stone layers on the checkerboard, move
between Monitor · Gallery · Compositing, and trigger a per-variant flatten that
either downloads the deliverable or shows a calm WARN — all DB-only, auth-gated,
and via the private-blob proxy.
</success_criteria>

<output>
Create `.planning/phases/06-compositing-deliverable/06-02-SUMMARY.md` when done.
</output>
