---
phase: 01-secure-foundation-secrets-db-auth
plan: 05
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - app/(auth)/login/page.tsx
  - app/(auth)/login/login-form.tsx
  - app/(app)/layout.tsx
  - app/components/app-shell/sidebar.tsx
  - app/components/app-shell/topbar.tsx
  - app/components/app-shell/user-menu.tsx
  - app/components/app-shell/theme-toggle.tsx
  - app/forbidden/page.tsx
  - app/page.tsx
  - app/actions/auth.ts
autonomous: false
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "A team member can sign in on /login with seeded credentials and is redirected to the app (Products)"
    - "The session persists across a browser refresh (JWT HTTP-only cookie)"
    - "A user can log out from the user menu on any page and is returned to /login"
    - "The login error never reveals whether email vs password was wrong"
    - "Operators never see the ADMIN nav section; an Operator deep-linking an admin route gets a calm 403 surface"
    - "The app shell (sidebar + topbar + user menu) hosts authenticated content and matches the UI-SPEC tokens"
  artifacts:
    - path: "app/(auth)/login/page.tsx"
      provides: "AUTH-01 login surface (UI-SPEC §2)"
    - path: "app/(app)/layout.tsx"
      provides: "app shell hosting every authenticated route (UI-SPEC §1)"
    - path: "app/components/app-shell/user-menu.tsx"
      provides: "AUTH-02 logout from any page (UI-SPEC §1)"
      contains: "signOut"
    - path: "app/forbidden/page.tsx"
      provides: "calm 403 surface (UI-SPEC §5)"
  key_links:
    - from: "app/(auth)/login/login-form.tsx"
      to: "lib/auth/auth signIn"
      via: "Credentials sign-in"
      pattern: "signIn"
    - from: "app/components/app-shell/sidebar.tsx"
      to: "session.user.role"
      via: "Admin-only nav gating"
      pattern: "role"
---

<objective>
Build the visible Walking-Skeleton login interaction and the global app shell every later phase reuses: the login
page (UI-SPEC §2), the sidebar + topbar + user-menu shell (UI-SPEC §1) with logout, and the calm 403 surface
(UI-SPEC §5). After this plan a real person completes the end-to-end thread — denied by default → /login → sign in
with the seeded Admin → authenticated app shell → log out — proving secrets→DB→auth→UI as one slice.

Purpose: AUTH-01 (login UI + persistent cookie, the visible side), AUTH-02 (logout from any page). The Admin-only
nav gating mirrors the AUTH-05 server boundary (enforced in Plan 06); here it is the UI convenience layer.
Output: login page + form, app shell (sidebar/topbar/user-menu/theme-toggle), forbidden page, root redirect, auth server actions.

NOTE: NOT autonomous — Task 3 is a manual UI/UX verification (login flow, teal accent, error/loading states,
Operator-sees-no-Admin-nav) per 01-VALIDATION.md Manual-Only Verifications and `workflow.human_verify_mode` default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-secure-foundation-secrets-db-auth/01-UI-SPEC.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md
@.planning/phases/01-secure-foundation-secrets-db-auth/SKELETON.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-03-SUMMARY.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-01-SUMMARY.md

<interfaces>
From 01-03: lib/auth/auth.ts exports `auth`, `signIn`, `signOut`; session shape `{ user: { id, email, role: "Admin"|"Operator" } }`.
From 01-01: shadcn components under app/components/ui (button, input, label, card, dropdown-menu, avatar, sonner, separator,
tooltip, skeleton); globals.css teal token layer; Geist fonts + dark theme on <html>.
From 01-UI-SPEC.md §1-§5 + Copywriting Contract: EXACT copy — "Sign in", login error "We couldn't sign you in.
Check your email and password and try again.", forbidden "You don't have access to this area. This section is
Admin-only." → [Back to Products], "Log out". Nav: MAIN = Products(box)/Batches(layers)/Jobs(activity)/Gallery(image),
ADMIN (Admin only) = Domain Settings(sliders-horizontal)/Users(users), bottom = Settings(settings). Sidebar 240/56px,
topbar 56px, active item = left 2px teal bar + tinted bg + teal icon. Login card max-width 400px, centered, Display-size wordmark.
NOTE: existing app/page.tsx currently renders <EnterpriseApp/>; this plan repurposes the root route to redirect
authenticated users into the app shell (the EnterpriseApp render workflow is a later-phase concern and is preserved
under its own route by reuse, not deleted here — keep enterprise-app.tsx file intact).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page + Credentials sign-in form (AUTH-01)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-UI-SPEC.md (§2 Login + Copywriting), lib/auth/auth.ts</read_first>
  <files>app/(auth)/login/page.tsx, app/(auth)/login/login-form.tsx, app/actions/auth.ts</files>
  <action>
    Create `app/(auth)/login/page.tsx`: centered single Card (max-width 400px) on `--background`, Display-size
    wordmark "Jewelry Render Studio" above. It reads `?from=` to preserve the redirect target.
    Create `app/(auth)/login/login-form.tsx` (client component): Email + Password (with show/hide toggle) shadcn
    inputs, one primary teal Button "Sign in". On submit call the `signInWithCredentials` server action; show the
    loading state (button spinner + "Signing in…", fields disabled) and on failure a single calm banner above the
    form with the EXACT copy "We couldn't sign you in. Check your email and password and try again." — NEVER reveal
    email-vs-password. On success redirect to `from || "/"`.
    Create `app/actions/auth.ts`: `signInWithCredentials(formData)` server action wrapping `signIn("credentials", {...})`
    with `redirect:false`, returning a generic error flag on failure; and `signOutAction()` wrapping `signOut({ redirectTo: "/login" })`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=fs.readFileSync('app/(auth)/login/login-form.tsx','utf8');const a=fs.readFileSync('app/actions/auth.ts','utf8');if(!/Sign in/.test(f))throw new Error('missing Sign in CTA');if(!/couldn't sign you in/i.test(f))throw new Error('missing generic error copy');if(!/signIn/.test(a)||!/signOut/.test(a))throw new Error('actions missing');console.log('OK')"</automated>
  </verify>
  <done>/login renders the UI-SPEC §2 card with Sign in CTA, show/hide password, loading + generic-error states; signInWithCredentials/signOutAction wrap Auth.js signIn/signOut; success redirects to from||"/".</done>
</task>

<task type="auto">
  <name>Task 2: App shell (sidebar + topbar + user menu + logout) and 403 surface (AUTH-02, UI-SPEC §1/§5)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-UI-SPEC.md (§1 App Shell, §5 Access, Copywriting), app/actions/auth.ts</read_first>
  <files>app/(app)/layout.tsx, app/components/app-shell/sidebar.tsx, app/components/app-shell/topbar.tsx, app/components/app-shell/user-menu.tsx, app/components/app-shell/theme-toggle.tsx, app/forbidden/page.tsx, app/page.tsx</files>
  <action>
    Create `app/(app)/layout.tsx` (server component): `await auth()`; if no session redirect to /login; render the
    shell — `<Topbar/>` (56px: wordmark left, theme toggle + user menu right) and `<Sidebar role={session.user.role}/>`
    (240/56px) wrapping `{children}` in a 1280px max-width content column with 24px gutters and a route-level skeleton fallback.
    Create `sidebar.tsx`: MAIN nav (Products/Batches/Jobs/Gallery with lucide icons) always; ADMIN section
    (Domain Settings/Users) rendered ONLY when `role === "Admin"`; bottom-pinned Settings. Active item = left 2px
    teal indicator + `--accent` tint + teal icon. 44px min hit area on icon-only collapsed targets.
    Create `topbar.tsx` + `theme-toggle.tsx` (dark default, toggles `.dark` on <html>).
    Create `user-menu.tsx` (client): avatar (initials fallback) dropdown showing identity + role badge + Settings +
    **Log out** (calls `signOutAction`) — available on every page (AUTH-02).
    Create `app/forbidden/page.tsx`: calm 403 surface with EXACT copy "You don't have access to this area. This
    section is Admin-only." + [Back to Products] link (UI-SPEC §5) — mirrors the server 403 (AUTH-05).
    Update `app/page.tsx`: move it under the (app) group as the Products landing placeholder (authenticated) — a simple
    "Nothing here yet" empty-state per Copywriting Contract; keep `app/enterprise-app.tsx` file untouched for later reuse.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/components/app-shell/sidebar.tsx','utf8');const u=fs.readFileSync('app/components/app-shell/user-menu.tsx','utf8');const fb=fs.readFileSync('app/forbidden/page.tsx','utf8');if(!/role/.test(s)||!/Admin/.test(s))throw new Error('admin nav not gated');if(!/signOut|Log out/.test(u))throw new Error('logout missing');if(!/don't have access/i.test(fb))throw new Error('403 copy missing');console.log('OK')"</automated>
  </verify>
  <done>(app)/layout.tsx gates on auth() and renders the shell; sidebar hides ADMIN section for Operators; user menu has Log out on every page; forbidden page shows the calm Admin-only 403 with Back to Products.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the end-to-end login slice + UI-SPEC fidelity (AUTH-01/02, UI-02)</name>
  <what-built>The full Walking-Skeleton thread: deny-by-default → /login → sign in with seeded Admin → app shell → log out, styled per UI-SPEC.</what-built>
  <how-to-verify>
    1. `npm run dev`. Visit `/` unauthenticated → confirm redirect to `/login?from=…` (visible SEC-03).
    2. Sign in with the seeded Admin (SEED_ADMIN_EMAIL/PASSWORD) → confirm redirect to the app shell (Products).
    3. Confirm the teal accent (NOT purple), dark default theme, Sign in button loading state, and that a wrong
       password shows the single calm banner "We couldn't sign you in…" (never says which field was wrong).
    4. Refresh the browser → confirm you stay logged in (JWT HTTP-only cookie persists — AUTH-01).
    5. Open the user menu → Log out → confirm return to /login (AUTH-02).
    6. (If an Operator account exists) log in as Operator → confirm the ADMIN nav section is absent and deep-linking
       /admin/users shows the calm 403 surface (the server 403 is verified in Plan 06).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what doesn't match the UI-SPEC / flow.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → login | Untrusted credentials; generic error prevents user enumeration |
| Operator UI → Admin nav | UI hides ADMIN section but is NOT the security boundary (server enforces in Plan 06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-AUTH | Spoofing/Info | login form | mitigate | Generic error copy (no email-vs-password leak); credentials posted to Auth.js signIn; HTTP-only cookie persistence |
| T-1-RBAC | Elevation | Admin nav visibility | accept-as-UI | Nav gating is convenience only; authoritative `requireRole` server gate lives in Plan 06 (AUTH-05) |
| T-1-SESSION | Tampering | logout | mitigate | signOut clears the session cookie; reachable from every page |
</threat_model>

<verification>
- Login form has the Sign in CTA + generic error copy; auth actions wrap signIn/signOut.
- Sidebar gates the ADMIN section on role; user menu exposes Log out everywhere; forbidden page shows the Admin-only copy.
- Manual checkpoint (Task 3): end-to-end login→shell→logout works, persists across refresh, teal/dark theme, no enumeration.
</verification>

<success_criteria>
The visible Walking-Skeleton login slice works end-to-end: deny-by-default redirect, sign in with the seeded Admin,
persistent session, logout from any page (AUTH-01/02); app shell + sidebar + 403 surface match the approved UI-SPEC
with teal-not-purple tokens; Operators see no ADMIN nav.
</success_criteria>

<output>
Create `.planning/phases/01-secure-foundation-secrets-db-auth/01-05-SUMMARY.md` when done.
</output>
