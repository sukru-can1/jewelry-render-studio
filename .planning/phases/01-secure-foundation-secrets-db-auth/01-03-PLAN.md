---
phase: 01-secure-foundation-secrets-db-auth
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - lib/auth/auth.config.ts
  - lib/auth/auth.ts
  - lib/auth/rbac.ts
  - app/api/auth/[...nextauth]/route.ts
  - middleware.ts
  - app/api/webhooks/runpod/route.ts
  - types/next-auth.d.ts
  - test/require-role.test.ts
  - test/deny-default.test.ts
  - test/webhook-auth.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, SEC-03, SEC-04]
must_haves:
  truths:
    - "Valid credentials authenticate against the seeded User and issue an HTTP-only JWT cookie that survives refresh"
    - "session.user.role is populated server-side from the JWT (Admin or Operator)"
    - "requireRole('Admin') rejects an Operator session with 403 server-side"
    - "An unauthenticated request to any protected route is denied by default (redirect to /login)"
    - "Only /login, /api/auth/*, and the RunPod webhook are public"
    - "The RunPod webhook rejects a request without the correct shared secret (401) and accepts with it"
    - "Middleware never imports Prisma or bcrypt (edge-safe split config)"
  artifacts:
    - path: "lib/auth/auth.config.ts"
      provides: "edge-safe Auth.js config (authorized/jwt/session callbacks)"
      contains: "authorized"
    - path: "lib/auth/auth.ts"
      provides: "AUTH-01 Credentials provider + NextAuth exports"
      contains: "Credentials"
    - path: "lib/auth/rbac.ts"
      provides: "AUTH-03/05 requireSession + requireRole"
      contains: "requireRole"
    - path: "middleware.ts"
      provides: "SEC-03 deny-by-default gate"
      contains: "matcher"
    - path: "app/api/webhooks/runpod/route.ts"
      provides: "SEC-04 shared-secret webhook"
      contains: "timingSafeEqual"
  key_links:
    - from: "lib/auth/auth.ts"
      to: "lib/db/prisma.ts + bcryptjs"
      via: "authorize() user lookup + compare"
      pattern: "bcrypt\\.compare"
    - from: "middleware.ts"
      to: "lib/auth/auth.config.ts"
      via: "NextAuth(authConfig) edge gate"
      pattern: "authConfig"
---

<objective>
Build the auth + RBAC + route-gating core: the split edge-safe `auth.config.ts` + Node `auth.ts` (Credentials
provider that bcrypt-checks the seeded User and issues a JWT cookie), the `requireRole()` server-side boundary,
the NextAuth route handler, deny-by-default middleware, and the shared-secret RunPod webhook scaffold. This is
the auth half of the Walking Skeleton — after this, the seeded Admin can authenticate and every route is locked.

Purpose: AUTH-01 (login + persistent cookie), AUTH-02 (logout via signOut), AUTH-03 (role in session),
SEC-03 (deny-by-default), SEC-04 (webhook secret). AUTH-04/05 enforcement is exercised in Plan 06 (admin routes);
the `requireRole` boundary itself is built and unit-tested here.
Output: lib/auth/{auth.config.ts,auth.ts,rbac.ts}, app/api/auth/[...nextauth]/route.ts, middleware.ts,
app/api/webhooks/runpod/route.ts, types/next-auth.d.ts, RBAC/deny/webhook tests.

NOTE: This plan depends only on 01-01 (it needs the generated Prisma client + User model + typed env, not a live
DB). Unit tests stub the session and Prisma per 01-VALIDATION.md; live-login E2E is a manual check in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-VALIDATION.md
@.planning/phases/01-secure-foundation-secrets-db-auth/SKELETON.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-01-SUMMARY.md

<interfaces>
From 01-01: lib/db/prisma.ts (singleton `prisma`), lib/env.ts (typed env incl. AUTH_SECRET, RUNPOD_WEBHOOK_SECRET),
generated `@prisma/client` with `User { id, email, passwordHash, role, disabled }` and `Role { Admin Operator }`,
Vitest harness (test/setup.ts fakeSession, test/factories.ts adminUser/operatorUser).
From RESEARCH Pattern 1/2/5 (copy faithfully, verify against installed beta.31 .d.ts):
  - auth.config.ts: `satisfies NextAuthConfig`; pages.signIn="/login"; session.strategy="jwt"; providers:[];
    callbacks.authorized (deny-by-default — public only /login), callbacks.jwt (token.role=user.role),
    callbacks.session (session.user.role=token.role).
  - auth.ts: NextAuth({...authConfig, providers:[Credentials({ authorize })]}); authorize() does
    prisma.user.findUnique → reject if !user||user.disabled → bcrypt.compare → return {id,email,role}.
    Exports { handlers, auth, signIn, signOut }.
  - rbac.ts: requireSession() throws 401 Response if no session; requireRole("Admin"|"Operator") throws 403 if Operator hits Admin.
  - middleware.ts (ROOT): NextAuth(authConfig) → export { auth as middleware }; matcher excludes
    api/auth, login, _next/static, _next/image, favicon.ico, api/webhooks/runpod.
  - webhook: timingSafeEqual against RUNPOD_WEBHOOK_SECRET; 401 on mismatch.
From CONCERNS: there is currently NO auth on any route — this plan is what closes SEC-03 for the whole app.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split auth config + Credentials provider + RBAC boundary (AUTH-01/02/03)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md (Pattern 1 + Pattern 2 + Anti-Patterns), test/setup.ts, test/factories.ts</read_first>
  <files>lib/auth/auth.config.ts, lib/auth/auth.ts, lib/auth/rbac.ts, app/api/auth/[...nextauth]/route.ts, types/next-auth.d.ts</files>
  <behavior>
    - `requireRole("Admin")` with an Operator session throws a 403 Response (status 403).
    - `requireRole("Admin")` with an Admin session returns the session.
    - `requireSession()` with no session throws a 401 Response.
    - jwt callback copies `user.role` onto `token.role`; session callback copies `token.role` onto `session.user.role`.
  </behavior>
  <action>
    Create `lib/auth/auth.config.ts` EDGE-SAFE (RESEARCH Pattern 1): NO prisma/bcrypt imports; `satisfies NextAuthConfig`;
    `pages.signIn="/login"`, `session.strategy="jwt"`, `providers:[]`, and the `authorized` (deny-by-default: public
    only when pathname starts with `/login`), `jwt`, and `session` callbacks. Read AUTH_SECRET from env (Auth.js
    picks up `AUTH_SECRET` automatically).
    Create `lib/auth/auth.ts` (Node runtime): `NextAuth({ ...authConfig, providers:[Credentials({ authorize })] })`;
    `authorize()` validates the `{email,password}` payload with zod, looks up `prisma.user.findUnique`, returns null
    if missing/disabled, `bcrypt.compare`s the password, returns `{id,email,role}` on success. Export
    `{ handlers, auth, signIn, signOut }`. Generic failure (return null) — never reveal email-vs-password (V2/UI-SPEC).
    Create `lib/auth/rbac.ts`: `requireSession()` and `requireRole(role)` per RESEARCH Pattern 2 (throw 401/403 Responses).
    Create `app/api/auth/[...nextauth]/route.ts`: `export const { GET, POST } = handlers` from lib/auth/auth.
    Create `types/next-auth.d.ts`: augment `Session["user"]` and `JWT` with `role: "Admin" | "Operator"` so role is typed.
    Write `test/require-role.test.ts` covering the four behaviors using test/setup.ts `fakeSession` (stub `auth()`).
  </action>
  <verify>
    <automated>npx vitest run require-role</automated>
  </verify>
  <done>Split config in place (auth.config.ts has no prisma/bcrypt import); Credentials.authorize does findUnique + bcrypt.compare + disabled check; requireRole/requireSession throw 403/401 correctly; role is typed in session; require-role test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deny-by-default middleware + edge-safety guard (SEC-03)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md (Pattern 1 middleware snippet, Pitfall 1)</read_first>
  <files>middleware.ts, test/deny-default.test.ts</files>
  <behavior>
    - The `authorized` callback returns true for an unauthenticated request to `/login` and false for `/admin/users`.
    - The `authorized` callback returns true for any path when a session/user is present.
    - middleware.ts source contains NO import of `@/lib/db/prisma`, `@prisma/client`, `bcryptjs`, or `lib/auth/auth` (edge-safety).
  </behavior>
  <action>
    Create ROOT `middleware.ts` (RESEARCH Pattern 1): `import NextAuth from "next-auth"; import { authConfig } from "@/lib/auth/auth.config"; export const { auth: middleware } = NextAuth(authConfig);`
    and `export const config = { matcher: ["/((?!api/auth|login|_next/static|_next/image|favicon.ico|api/webhooks/runpod).*)"] }`
    so EVERY route is gated except login, /api/auth/*, static assets, and the RunPod webhook (SEC-03 allowlist).
    Import ONLY auth.config.ts — never auth.ts/prisma/bcrypt (Pitfall 1: edge crash).
    Write `test/deny-default.test.ts`: import the `authorized` callback (export it from auth.config or test via a thin
    wrapper) and assert deny-by-default for /admin/users, allow for /login, allow when authed; plus a source-text
    assertion that middleware.ts imports no Prisma/bcrypt/auth.ts.
  </action>
  <verify>
    <automated>npx vitest run deny-default</automated>
  </verify>
  <done>middleware.ts gates all routes except the allowlist, imports only auth.config.ts (edge-safe), and the deny-default test proves unauth→deny / authed→allow / login→public with no Prisma in middleware.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RunPod webhook shared-secret scaffold (SEC-04)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md (Pattern 5 webhook), lib/env.ts</read_first>
  <files>app/api/webhooks/runpod/route.ts, test/webhook-auth.test.ts</files>
  <behavior>
    - POST without the `x-webhook-secret` header → 401.
    - POST with a wrong secret → 401.
    - POST with the correct `RUNPOD_WEBHOOK_SECRET` → 200 `{ ok: true }`.
    - Secret comparison uses constant-time `crypto.timingSafeEqual` (length-guarded), not `===`.
  </behavior>
  <action>
    Create `app/api/webhooks/runpod/route.ts` (Node runtime; `export const runtime="nodejs"`) per RESEARCH Pattern 5:
    read `x-webhook-secret`, compare against `process.env.RUNPOD_WEBHOOK_SECRET` with a length-guarded
    `crypto.timingSafeEqual`; return 401 on missing/mismatch, else `Response.json({ ok: true })`. Phase-4 reconcile
    logic is a TODO comment — do NOT implement status handling here; this plan only closes the auth gap so the
    endpoint never ships unauthenticated. It is allowlisted in middleware (Task 2) but verified IN the handler (SEC-04).
    Write `test/webhook-auth.test.ts` covering the four behaviors (set RUNPOD_WEBHOOK_SECRET in the test env, call POST).
  </action>
  <verify>
    <automated>npx vitest run webhook-auth</automated>
  </verify>
  <done>Webhook returns 401 without/with wrong secret and 200 with the correct secret via timingSafeEqual; allowlisted in middleware but verified in-handler; webhook-auth test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → app/API | Untrusted requests; deny-by-default unless authenticated session present |
| Operator session → Admin action | Privilege boundary enforced server-side, not by UI |
| RunPod → webhook | Machine-to-machine; no user session, authenticated by shared secret |
| edge middleware → Node handlers | Prisma/bcrypt must not cross into the edge runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-AUTH | Spoofing | Credentials login / session | mitigate | Auth.js v5 Credentials + bcrypt(12) compare; encrypted JWT HTTP-only cookie; generic error (no user enumeration) |
| T-1-RBAC | Elevation | Operator→Admin escalation | mitigate | `requireRole("Admin")` server-side boundary (403); unit-tested; UI hiding is not the gate |
| T-1-AUTHZ | Elevation | deny-by-default bypass | mitigate | middleware matcher denies all but allowlist (/login, /api/auth, webhook); authorized callback returns false by default |
| T-1-WEBHOOK | Spoofing/Tampering | RunPod webhook | mitigate | `crypto.timingSafeEqual` against `RUNPOD_WEBHOOK_SECRET`; verified in handler not middleware |
| T-1-EDGE | Tampering | middleware edge runtime | mitigate | Split config — middleware imports only auth.config.ts; source-text test asserts no Prisma/bcrypt import |
</threat_model>

<verification>
- `npx vitest run require-role deny-default webhook-auth` all green.
- middleware.ts contains no Prisma/bcrypt/auth.ts import (edge-safe).
- `app/api/auth/[...nextauth]/route.ts` re-exports GET/POST from the NextAuth handlers.
</verification>

<success_criteria>
Credentials login issues a JWT HTTP-only cookie with role in the session (AUTH-01/03); signOut is exported for
logout (AUTH-02); every route is deny-by-default with the correct allowlist (SEC-03); the RunPod webhook is
secret-gated (SEC-04); the split config keeps Prisma/bcrypt off the edge. requireRole is the tested RBAC boundary.
</success_criteria>

<output>
Create `.planning/phases/01-secure-foundation-secrets-db-auth/01-03-SUMMARY.md` when done.
</output>
