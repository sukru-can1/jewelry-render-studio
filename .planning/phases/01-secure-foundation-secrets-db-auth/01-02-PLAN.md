---
phase: 01-secure-foundation-secrets-db-auth
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - prisma/seed.ts
  - test/seed-domain.test.ts
  - test/prisma-pool.test.ts
  - docs/DB_SETUP.md
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "The migration applies cleanly against Railway Postgres via DIRECT_URL and User/Role + domain + core tables exist"
    - "The seed creates EXACTLY 4 camera views, 3 metals, 4 object groups, 4 quality presets with the real values"
    - "Default render resolution is 1920x1920 in every quality preset"
    - "A first Admin user is created from SEED_ADMIN_EMAIL/SEED_ADMIN_PASSWORD (never hardcoded)"
    - "Concurrent Prisma queries through the pooled singleton do not exhaust the pool (no P2024)"
  artifacts:
    - path: "prisma/seed.ts"
      provides: "DATA-03 domain seed + first Admin"
      contains: "cameraView.upsert"
    - path: "prisma/migrations"
      provides: "DATA-01 applied migration"
    - path: "test/seed-domain.test.ts"
      provides: "post-seed count + value assertions"
  key_links:
    - from: "prisma/seed.ts"
      to: "lib/db/prisma.ts / @prisma/client"
      via: "PrismaClient upserts"
      pattern: "upsert"
    - from: "migrate"
      to: "DIRECT_URL"
      via: "prisma migrate against direct connection"
      pattern: "DIRECT_URL"
---

<objective>
Bring the database to life: apply the Phase-1 migration against Railway Postgres and seed the real domain
settings (DATA-03) plus the first Admin user. This is the second half of the Walking-Skeleton DB slice — after
this plan, a real query returns the seeded 4 views / 3 metals / 4 groups / quality presets at 1920x1920 from
Postgres, and the seeded Admin can be authenticated by the auth slice (Plan 03).

Purpose: DATA-01 (tables exist), DATA-02 (pool stays healthy under concurrency), DATA-03 (exact seeded values).
Output: prisma/seed.ts, applied migration, seed-domain + pool Vitest tests, docs/DB_SETUP.md.

NOTE: This plan is NOT autonomous. The migrate task is [BLOCKING] and requires a live `DATABASE_URL`/`DIRECT_URL`.
Railway Postgres is being provisioned in parallel; the executor MUST NOT fabricate a connection string. If the
DB is not yet reachable, pause at the checkpoint and resume once the operator confirms the URLs are set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-VALIDATION.md
@.planning/phases/01-secure-foundation-secrets-db-auth/SKELETON.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-01-SUMMARY.md

<interfaces>
From 01-01 (Plan 01): prisma/schema.prisma (Role/JobStatus enums + 12 models), lib/db/prisma.ts (singleton),
lib/env.ts (typed env incl. SEED_ADMIN_EMAIL/SEED_ADMIN_PASSWORD optional), test harness (vitest.config.ts, test/setup.ts).
From RESEARCH "Seed script for DATA-03": EXACT values — cameraViews (view1 az30/el25/187.5/f2.8, view2 az180/el15/187.5/f2.8,
view3 az-30/el10/50/f2.8, view4 az0/el75/187.5/f2.8), metals (white/yellow/red labels), objectGroups
(alloycolour/diamond/stone2/stone3), qualityPresets (preview 64 / medium 256 / high 512 / ultra 2048, all 1920x1920),
first Admin via bcrypt.hash(pw,12). UI-SPEC also lists metal hex swatches and ultra 2048-4096 (pick 2048 default).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply the Phase-1 migration against Railway Postgres</name>
  <what-built>Plan 01 wrote prisma/schema.prisma (validated, no migration yet). This task creates and applies the initial migration.</what-built>
  <how-to-verify>
    PREREQUISITE: `DATABASE_URL` (pooled, ...?pgbouncer=true&connection_limit=1) and `DIRECT_URL` (direct 5432)
    for the provisioned Railway Postgres 16 must be present in `.env.local`. If they are not yet set (DB still
    provisioning), STOP and tell the operator — do NOT fabricate a connection string.
    Once URLs are set, run (non-TTY hint included):
      1. `npx prisma migrate dev --name init`   (dev — creates prisma/migrations + applies via DIRECT_URL)
      2. `npx prisma migrate status`             (expect: up to date, exit 0)
    Document for prod (do not run here): the build step runs `npx prisma migrate deploy` (already in package.json build script).
    Migrations MUST run against DIRECT_URL, never the pooled transaction-mode URL (RESEARCH PITFALL #6).
  </how-to-verify>
  <resume-signal>Type "migrated" once `prisma migrate status` reports up to date, or report the DB blocker.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Domain + Admin seed with exact values (DATA-03)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md (Seed script for DATA-03), .planning/phases/01-secure-foundation-secrets-db-auth/01-UI-SPEC.md (Domain Settings §4 for metal hex + resolution)</read_first>
  <files>prisma/seed.ts, test/seed-domain.test.ts</files>
  <behavior>
    - After seed: `cameraView` count == 4 with view1 az=30/el=25/focalMm=187.5/fStop=2.8, view3 az=-30/el=10/focalMm=50.
    - After seed: `metal` count == 3 (white/yellow/red), `objectGroup` count == 4 (alloycolour/diamond/stone2/stone3 with sortOrder 0..3).
    - After seed: `qualityPreset` count == 4 (preview 64 / medium 256 / high 512 / ultra 2048), every preset width==1920 && height==1920.
    - After seed with SEED_ADMIN_EMAIL/PASSWORD set: one User with role Admin whose passwordHash verifies via bcrypt.compare.
    - Seed is idempotent: running twice yields the same counts (upsert by `key`/`email`).
  </behavior>
  <action>
    Create `prisma/seed.ts` exactly per RESEARCH "Seed script for DATA-03": upsert cameraViews/metals/objectGroups/
    qualityPresets by `key`, and upsert the first Admin from `process.env.SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD`
    with `passwordHash = await bcrypt.hash(pw, 12)`, `role: "Admin"`. Pull metal hex values from UI-SPEC color
    swatches where the schema's `Metal.hex` is set. Use ultra samples 2048 (Admin-editable later). Use the
    `prisma db seed` wiring already added to package.json in Plan 01.
    Write `test/seed-domain.test.ts` asserting the behaviors above (run against the live/test Postgres; if the
    suite must isolate, query after invoking the seed or seed within the test's setup).
  </action>
  <verify>
    <automated>npx prisma db seed && npx vitest run seed-domain</automated>
  </verify>
  <done>Seed produces exactly 4 views / 3 metals / 4 groups / 4 presets with the exact az/el/focal/fStop and 1920x1920 values; first Admin created from env with a verifiable bcrypt hash; idempotent on re-run; seed-domain test green.</done>
</task>

<task type="auto">
  <name>Task 3: Pool-health test + DB setup docs (DATA-02)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md (Pattern 3, Pitfall 2), .planning/phases/01-secure-foundation-secrets-db-auth/01-VALIDATION.md</read_first>
  <files>test/prisma-pool.test.ts, docs/DB_SETUP.md</files>
  <action>
    Write `test/prisma-pool.test.ts`: fire N concurrent (e.g. 25) trivial queries through the `lib/db/prisma.ts`
    singleton (`Promise.all` of `prisma.$queryRaw\`SELECT 1\`` or `prisma.cameraView.count()`) and assert all
    resolve with no `P2024`/"too many connections" error — proving the singleton + pooled URL topology holds (DATA-02).
    Write `docs/DB_SETUP.md`: document the pooled vs direct URL split (`DATABASE_URL` pooled with
    `pgbouncer=true&connection_limit=1`, `DIRECT_URL` for migrations), the `migrate dev`/`migrate deploy` commands,
    the `prisma db seed` + SEED_ADMIN_* env, and the rule never to migrate over the pooled URL. No secrets in the doc.
  </action>
  <verify>
    <automated>npx vitest run prisma-pool</automated>
  </verify>
  <done>Concurrent-query pool test passes with no P2024; docs/DB_SETUP.md documents the pooled/direct topology, migrate/seed commands, and the migration-URL rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serverless runtime → Postgres | Many concurrent function invocations share one pooled connection budget |
| seed env → DB | First Admin credentials come from env, never committed defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-DATA-02 | DoS | Prisma connection pool | mitigate | Singleton + pooled `DATABASE_URL` (`connection_limit=1`) + `DIRECT_URL` migrations only; pool test proves no P2024 |
| T-1-SEED | Spoofing | first Admin bootstrap | mitigate | Admin seeded from `SEED_ADMIN_EMAIL/PASSWORD` env, bcrypt(12); no hardcoded credentials |
| T-1-MIGRATE | Tampering | migration over pooled URL | mitigate | `directUrl` enforced; [BLOCKING] task runs migrate only against DIRECT_URL |
</threat_model>

<verification>
- `npx prisma migrate status` reports up to date (Task 1, against live DB).
- `npx prisma db seed` then `npx vitest run seed-domain` green with exact counts/values.
- `npx vitest run prisma-pool` green (no P2024 under concurrency).
</verification>

<success_criteria>
Migration applied to Railway Postgres; domain seeded with the exact DATA-03 values at 1920x1920; first Admin
created from env; pool stays healthy under concurrent load (DATA-02). The DB half of the Walking Skeleton is live.
</success_criteria>

<output>
Create `.planning/phases/01-secure-foundation-secrets-db-auth/01-02-SUMMARY.md` when done.
</output>
