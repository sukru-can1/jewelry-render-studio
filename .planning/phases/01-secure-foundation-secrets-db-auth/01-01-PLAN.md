---
phase: 01-secure-foundation-secrets-db-auth
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - .env.example
  - vitest.config.ts
  - test/setup.ts
  - test/factories.ts
  - prisma/schema.prisma
  - lib/db/prisma.ts
  - lib/env.ts
  - components.json
  - app/globals.css
  - app/layout.tsx
  - postcss.config.mjs
  - lib/utils.ts
  - test/prisma-singleton.test.ts
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "All Phase-1 dependencies install at the exact pinned versions (next-auth beta.31, Prisma 6.19.2, @vercel/blob 2.4.x)"
    - "Prisma schema defines User/Role + JobStatus enum + the domain + core relational tables and validates"
    - "PrismaClient is a globalThis singleton reading the pooled DATABASE_URL with DIRECT_URL reserved for migrations"
    - "Typed env (@t3-oss/env-nextjs) fails fast when a required secret is missing"
    - "Vitest runs and a singleton test passes"
    - "shadcn/Tailwind v4 token layer + Geist fonts load through a single reconciled global stylesheet"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "DATA-01 relational schema + Role/JobStatus enums + pooled datasource"
      contains: "directUrl = env(\"DIRECT_URL\")"
    - path: "lib/db/prisma.ts"
      provides: "DATA-02 PrismaClient globalThis singleton"
      contains: "globalThis"
    - path: "lib/env.ts"
      provides: "fail-fast typed env"
      contains: "createEnv"
    - path: "vitest.config.ts"
      provides: "test harness for all later waves"
    - path: "app/globals.css"
      provides: "UI-SPEC token CSS variables (:root + .dark)"
      contains: "--primary"
  key_links:
    - from: "lib/db/prisma.ts"
      to: "@prisma/client"
      via: "PrismaClient import"
      pattern: "new PrismaClient"
    - from: "prisma/schema.prisma"
      to: "DATABASE_URL / DIRECT_URL"
      via: "datasource env()"
      pattern: "env\\(\"DIRECT_URL\"\\)"
---

<objective>
Stand up the entire Phase-1 foundation scaffold in one pass: pinned dependency install, the Prisma
schema + serverless-safe singleton + typed fail-fast env (DATA-01/DATA-02), the Vitest harness every
later wave verifies against (Wave 0 per 01-VALIDATION.md), and the shadcn/Tailwind v4 + Geist design-token
layer from the approved UI-SPEC. This is the Walking-Skeleton substrate — nothing downstream compiles
without it, so it ships first and alone.

Purpose: Every later plan (DB seed, auth, hardening, UI surfaces) imports from `lib/db/prisma.ts`,
`lib/env.ts`, the generated Prisma client, the token stylesheet, and the test harness. Defining these
contracts first prevents downstream scavenger hunts.
Output: package.json (deps + scripts), prisma/schema.prisma, lib/db/prisma.ts, lib/env.ts, Vitest config
+ harness, shadcn config + token globals.css + Geist fonts in layout.tsx, lib/utils.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-UI-SPEC.md
@.planning/phases/01-secure-foundation-secrets-db-auth/01-VALIDATION.md
@.planning/phases/01-secure-foundation-secrets-db-auth/SKELETON.md

<interfaces>
<!-- Existing app contracts the executor must NOT break. Extend, do not restructure. -->
From tsconfig.json: path alias `@/*` → `./*` (baseUrl "."). moduleResolution "bundler", jsx "preserve", strict true.
From app/layout.tsx: currently imports "./styles.css" and renders <html lang="en"><body>{children}</body>. This plan reconciles styles.css → globals.css and adds Geist fonts + theme class.
From package.json: existing deps @vercel/blob ^1.0.2, lucide-react ^0.468.0, next ^15.1.4, react ^19. Scripts: dev/build/start/lint.
From RESEARCH "Minimal Prisma schema" and "Seed script" sections: exact model/enum shapes are specified — copy them faithfully (Role { Admin Operator }, JobStatus { queued submitted in_queue in_progress completed failed cancelled }, User/CameraView/Metal/StoneType/ObjectGroup/QualityPreset/Project/Product/ObjectGroupAssignment/Batch/Job/Layer).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned dependencies and wire build/test scripts</name>
  <read_first>package.json, .env.example, tsconfig.json, .planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md (Installation + Version verification table)</read_first>
  <files>package.json, .env.example, .gitignore</files>
  <action>
    Install EXACT pinned versions (RESEARCH is explicit that `@latest` is wrong for next-auth and Prisma):
    runtime — `next-auth@5.0.0-beta.31` (pin exact, do NOT float `@beta`), `@prisma/client@6.19.2`,
    `bcryptjs@^3.0.3`, `@vercel/blob@^2.4.0` (upgrade from ^1.0.2), `zod@^3.25.0` (honor STACK lock — do NOT
    take zod 4), `@t3-oss/env-nextjs@^0.13.11`, `geist` (latest);
    dev — `prisma@6.19.2`, `@types/bcryptjs`, `tsx`, `vitest`, `@vitejs/plugin-react`, `vite-tsconfig-paths`.
    Tailwind v4 + shadcn deps are added by Task 4's `shadcn init`; do not hand-add them here.
    Add to package.json `scripts`: `"build": "prisma generate && prisma migrate deploy && next build"`,
    `"postinstall": "prisma generate"`, `"test": "vitest run"`, `"test:dot": "vitest run --reporter=dot"`,
    `"db:seed": "tsx prisma/seed.ts"`. Add top-level `"prisma": { "seed": "tsx prisma/seed.ts" }`.
    Add new placeholder vars to `.env.example` (placeholders only, NEVER real values): `DATABASE_URL=`,
    `DIRECT_URL=`, `AUTH_SECRET=`, `RUNPOD_WEBHOOK_SECRET=`, `SEED_ADMIN_EMAIL=`, `SEED_ADMIN_PASSWORD=`.
    In `.gitignore` ensure `.env`, `.env.local`, `.env*.local`, `next-dev.*.log`, `outputs/*.log` are all present
    (CONCERNS flagged the .env*.local glob and stray dev logs).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};const need={'next-auth':'5.0.0-beta.31','@prisma/client':'6.19.2','prisma':'6.19.2'};for(const k in need){if(!d[k]||!d[k].includes(need[k])){throw new Error(k+' wrong: '+d[k])}}if(!p.scripts.build.includes('prisma generate')||!p.prisma||!p.prisma.seed)throw new Error('scripts/prisma seed missing');console.log('OK')"</automated>
  </verify>
  <done>package.json pins next-auth 5.0.0-beta.31, @prisma/client 6.19.2, prisma 6.19.2; build/postinstall/test/db:seed scripts + prisma.seed present; .env.example has the 6 new placeholder vars; .gitignore covers .env*.local and dev logs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prisma schema + serverless-safe singleton + typed fail-fast env (DATA-01/DATA-02)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-RESEARCH.md (Pattern 3, "Minimal Prisma schema", schema datasource), tsconfig.json</read_first>
  <files>prisma/schema.prisma, lib/db/prisma.ts, lib/env.ts, test/prisma-singleton.test.ts</files>
  <behavior>
    - Test: importing `prisma` twice from `lib/db/prisma.ts` returns the SAME instance (singleton via globalThis).
    - Test: `lib/env.ts` throws (createEnv validation) when a required server var is absent at import time.
    - Test: `prisma/schema.prisma` text contains `directUrl = env("DIRECT_URL")` and `enum JobStatus` and `model User`.
  </behavior>
  <action>
    Run `npx prisma init --datasource-provider postgresql` only if prisma/ absent, then OVERWRITE schema.prisma.
    Write the datasource with `provider="postgresql"`, `url=env("DATABASE_URL")` (pooled, `connection_limit=1` set
    in the URL value at deploy time), `directUrl=env("DIRECT_URL")` (migrations only). Write `generator client`.
    Copy the exact enums and models from RESEARCH "Minimal Prisma schema": `enum Role { Admin Operator }`,
    `enum JobStatus { queued submitted in_queue in_progress completed failed cancelled }`, and models
    User, CameraView, Metal, StoneType, ObjectGroup, QualityPreset, Project, Product, ObjectGroupAssignment,
    Batch, Job (with `@@index([batchId, status])` and `runpodJobId String?` for Phase-8 backfill), Layer.
    Create `lib/db/prisma.ts` as the globalThis singleton (RESEARCH Pattern 3): one PrismaClient cached on
    `globalThis.prisma`, only assigned to globalThis when `NODE_ENV !== "production"`.
    Create `lib/env.ts` using `@t3-oss/env-nextjs` `createEnv` + zod: server vars `DATABASE_URL`, `DIRECT_URL`,
    `AUTH_SECRET`, `RUNPOD_API_KEY`, `RUNPOD_ENDPOINT_ID`, `BLOB_READ_WRITE_TOKEN`, `RUNPOD_WEBHOOK_SECRET`
    (all `z.string().min(1)`); `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` optional. Fail-fast on missing.
    Run `npx prisma generate` so `@prisma/client` types exist for downstream waves. Do NOT run `migrate` here
    (no live DB yet — that is Plan 02's [BLOCKING] task).
    Write `test/prisma-singleton.test.ts` covering the three behaviors above (read schema.prisma as text for the
    string assertions; for the env throw test, import a tiny isolated check or assert createEnv presence).
  </action>
  <verify>
    <automated>npx prisma validate && npx vitest run prisma-singleton</automated>
  </verify>
  <done>`prisma validate` passes; schema has Role + JobStatus enums, directUrl, and all 12 models; `lib/db/prisma.ts` exports a globalThis singleton; `lib/env.ts` exports a fail-fast typed env; singleton test green; `node_modules/.prisma/client` generated.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest harness + test fixtures (Wave 0 per 01-VALIDATION.md)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-VALIDATION.md (Wave 0 Requirements), tsconfig.json</read_first>
  <files>vitest.config.ts, test/setup.ts, test/factories.ts</files>
  <action>
    Create `vitest.config.ts` using `vite-tsconfig-paths` (so `@/*` resolves) and `@vitejs/plugin-react`,
    `test.environment="node"`, `test.setupFiles=["./test/setup.ts"]`, no watch flags.
    Create `test/setup.ts`: shared fixtures — a session stub helper shape (`fakeSession(role)` returning
    `{ user: { id, email, role } }`) and a Prisma test-client accessor (re-export the singleton; integration
    tests that need a live DB are gated in Plan 02). Keep this import-safe with no live DB connection at module load.
    Create `test/factories.ts`: `adminUser()` and `operatorUser()` factory objects (email/role/passwordHash
    placeholder via bcryptjs hash) consumed by RBAC/admin tests in later waves.
  </action>
  <verify>
    <automated>npx vitest run prisma-singleton --reporter=dot</automated>
  </verify>
  <done>`vitest run` executes with path aliases resolving and setup.ts loaded; factories export adminUser/operatorUser; no watch-mode flags present.</done>
</task>

<task type="auto">
  <name>Task 4: shadcn/Tailwind v4 init + UI-SPEC token layer + Geist fonts (reconcile single stylesheet)</name>
  <read_first>.planning/phases/01-secure-foundation-secrets-db-auth/01-UI-SPEC.md (shadcn Setup, Token Architecture, Color, Typography), app/layout.tsx, app/styles.css</read_first>
  <files>components.json, app/globals.css, app/layout.tsx, postcss.config.mjs, lib/utils.ts</files>
  <action>
    Run `npx shadcn@latest init` with: style `new-york`, base color `neutral`, CSS variables `yes`, RSC `yes`,
    aliases `@/components` and `@/lib/utils`. This adds Tailwind v4, `tailwind-merge`/`clsx`, `lib/utils.ts`,
    `components.json`, and a `globals.css`. Add the shadcn component set the later UI plans consume:
    `npx shadcn@latest add button input label table badge dialog card dropdown-menu sonner form select switch skeleton avatar tooltip separator`.
    RECONCILE the two stylesheets (RESEARCH PITFALL #7): migrate any needed rules from existing `app/styles.css`
    into `app/globals.css`, point `components.json` `tailwind.css` at `app/globals.css`, import only `globals.css`
    in layout.tsx, and stop importing `styles.css`. Do NOT leave two competing global stylesheets.
    Seed the EXACT semantic CSS-variable token layer from UI-SPEC into `globals.css` `:root` (light) and `.dark`:
    map `--background`, `--foreground`, `--card`, `--muted(-foreground)`, `--border`, `--ring`, `--primary`
    (teal — dark `#14B8A6`, light `#0D9488`), `--destructive`, plus `--success`, `--warning`, `--info`,
    `--accent` per the dark/light tables and the status-pill colors. NO purple anywhere.
    In `layout.tsx`: load Geist Sans + Geist Mono via `next/font` (or the `geist` package's `GeistSans`/`GeistMono`),
    apply their CSS variables + `className="dark"` on `<html>` (dark is the default ops-console theme), keep `lang="en"`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/globals.css','utf8');if(!/--primary/.test(c)||!/\.dark/.test(c))throw new Error('tokens missing');if(/#8b5cf6|#a855f7|purple|indigo/i.test(c))throw new Error('purple found');if(fs.existsSync('app/styles.css')&&fs.readFileSync('app/layout.tsx','utf8').includes('styles.css'))throw new Error('two stylesheets');console.log('OK')"</automated>
  </verify>
  <done>components.json present (new-york/neutral/cssVars), globals.css holds the semantic teal token layer for :root + .dark with no purple, layout.tsx imports only globals.css and loads Geist + dark theme, the 16-component shadcn set exists under app/components/ui.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/runtime → env | Secrets read only via typed `lib/env.ts`; missing secret must fail-fast, not run degraded |
| repo → version control | `.env`/`.env.local` must never be staged (CONCERNS: live keys in working tree) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-CONFIG-01 | Tampering/Info | env handling | mitigate | `@t3-oss/env-nextjs` fail-fast; no secret literals committed; UI never prints env var names (UI-SPEC copy) |
| T-1-DATA-02 | DoS | Prisma pooling | mitigate | globalThis singleton + pooled DATABASE_URL (`connection_limit=1`) + DIRECT_URL for migrations only |
| T-1-SC | Tampering | npm installs | mitigate | All packages official-scope/de-facto-standard, audited in RESEARCH "Package Legitimacy Audit" (no [SLOP]/[SUS]); pin next-auth beta EXACT |
</threat_model>

<verification>
- `npx prisma validate` exits 0.
- `npx vitest run` green (singleton + schema-text + env-presence tests).
- `app/globals.css` is the only global stylesheet imported and contains the teal token layer with no purple.
- package.json pins next-auth `5.0.0-beta.31`, Prisma `6.19.2`.
</verification>

<success_criteria>
All deps installed at pinned versions; Prisma schema validates with the full DATA-01 model set + JobStatus enum;
PrismaClient singleton + typed fail-fast env in place (DATA-02); Vitest harness runs; shadcn/Tailwind v4 token
layer + Geist load through one reconciled stylesheet. Downstream waves can import every contract this plan defines.
</success_criteria>

<output>
Create `.planning/phases/01-secure-foundation-secrets-db-auth/01-01-SUMMARY.md` when done.
</output>
