---
phase: 09-adaptive-render-intelligence
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/enterprise-recipes.ts
  - lib/intelligence/knobs.ts
  - lib/intelligence/verdict.ts
  - lib/intelligence/loop.ts
  - lib/validation/batch.ts
  - prisma/schema.prisma
  - test/intel-overrides.test.ts
  - test/intel-knobs.test.ts
  - test/intel-verdict.test.ts
  - test/intel-loop-decision.test.ts
  - test/intel-loop-guardrails.test.ts
  - test/intel-migration-additive.test.ts
autonomous: false
requirements: [INTEL-01, INTEL-03]
must_haves:
  truths:
    - "buildEnterpriseRecipe with NO profileOverrides produces byte-identical output to today (backward-compatible); absent overrides = current behavior"
    - "buildEnterpriseRecipe with profileOverrides applies worldStrength→world.strength, exposure→render.exposure, cardDarkness→reflection_cards[].color (scaled), contactShadowStrength→contact_shadows[].alpha, cameraPreset→ANGLES key — all CLAMPED to KNOB_RANGES before they reach the recipe (G2)"
    - "applyDeltas adds the model's signed relative deltas to the CURRENT recipe knob values then clamp()s to KNOB_RANGES — a hallucinated out-of-range value can never reach the recipe"
    - "visionVerdictSchema validates the full 8-dimension scores + 5 flags + 4 bounded deltas + cameraPresetSuggestion + overallScore + rationale; out-of-range deltas are rejected at parse time"
    - "decideLoop returns escalate→accept→autoCorrect→freeze-best in that priority order; hard flags (milky/wrongMetal/brokenHoldout) and emptyOrBroken force escalate; MAX_ITERATIONS=2 and stop-on-no-improvement freeze the best-scoring override set"
    - "the forbidden-move guard (G5) zeroes a positive exposureDelta when flags.milky is true, and zeroes a positive worldStrengthDelta when fixing flat metal — the DOMAIN iron law holds even if the model regresses"
    - "the pass-type knob gate (G7) drops a non-zero contactShadowDelta on a stone (transparent) pass"
    - "Job gains nullable intelState String? + intel Json? via an ADDITIVE migration; non-intelligence jobs leave both null and behave exactly as today"
  artifacts:
    - path: "lib/intelligence/knobs.ts"
      provides: "ProfileOverrides type, KNOB_RANGES, clamp(), applyDeltas()"
      contains: "KNOB_RANGES"
    - path: "lib/intelligence/verdict.ts"
      provides: "visionVerdictSchema + VisionVerdict type (8-dim scores, flags, deltas)"
      contains: "visionVerdictSchema"
    - path: "lib/intelligence/loop.ts"
      provides: "decideLoop() pure decision + guardrail helpers (forbidden-move, pass-gate, caps)"
      contains: "decideLoop"
    - path: "lib/enterprise-recipes.ts"
      provides: "buildEnterpriseRecipe extended with optional profileOverrides (clamped)"
      contains: "profileOverrides"
    - path: "prisma/schema.prisma"
      provides: "Job.intelState String? + Job.intel Json? additive columns"
      contains: "intelState"
  key_links:
    - from: "lib/intelligence/loop.ts"
      to: "lib/intelligence/knobs.ts"
      via: "applyDeltas clamps every delta to KNOB_RANGES"
      pattern: "clamp"
    - from: "lib/enterprise-recipes.ts"
      to: "ProfileOverrides"
      via: "buildEnterpriseRecipe applies clamped overrides at the end of recipe assembly"
      pattern: "profileOverrides"
---

<objective>
Build the PURE, heavily-unit-tested foundation of the adaptive render loop and the
single additive Prisma migration the whole phase rides on. Four pure modules + one
schema change:

1. Extend `buildEnterpriseRecipe` with an optional `profileOverrides` named-knob input
   (worldStrength, exposure, cardDarkness, contactShadowStrength, cameraPreset),
   clamped to `KNOB_RANGES` before they touch the recipe. Backward-compatible:
   absent overrides = today's behavior byte-for-byte (INTEL-01).
2. `lib/intelligence/knobs.ts` — the `ProfileOverrides` contract, `KNOB_RANGES`,
   `clamp()`, and `applyDeltas()` (current value + signed delta → clamp → override).
3. `lib/intelligence/verdict.ts` — the full 8-dimension `visionVerdictSchema` (the
   structured-output contract the vision model fills) per 09-AI-SPEC §5.2.
4. `lib/intelligence/loop.ts` — `decideLoop()` (escalate→accept→autoCorrect→freeze-best)
   plus the online guardrails G3/G4/G5/G7 (caps, stop-on-no-improvement, forbidden-move,
   pass-type gate) as pure functions (INTEL-03).
5. The additive `Job.intelState String? + Job.intel Json?` migration (the loop's trace
   + state machine), plus the `optimizeWithAi` field on the createBatch selection schema.

Purpose: lock every contract and clamp BEFORE any vision call, cron sweep, or UI exists,
so the impure plans (09-02 vision/orchestration, 09-03 UI, 09-04 calibration) are pure
wiring over tested primitives — no scavenger hunt for the schema, the clamp ranges, or
the decision priority. The single-quality-source rule (G10) is enforced here by TYPES:
the loop only ever emits a `ProfileOverrides` object; it never hand-builds recipe JSON.
Output: 4 pure libs (1 extended), 1 additive migration, 1 schema field, 5 RED test
files driven GREEN, and 1 additive-migration assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-adaptive-render-intelligence/09-DOMAIN.md
@.planning/phases/09-adaptive-render-intelligence/09-AI-RESEARCH.md
@.planning/phases/09-adaptive-render-intelligence/09-AI-SPEC.md

<read_first>
@lib/enterprise-recipes.ts
@lib/batches/expand.ts
@lib/validation/batch.ts
@prisma/schema.prisma
@test/setup.ts
@test/batch-expand.test.ts
</read_first>

<interfaces>
<!-- The named-knob override contract (09-AI-RESEARCH §6). buildEnterpriseRecipe
     applies these at the END of recipe assembly; an undefined knob leaves the
     default untouched (backward-compatible). -->
<!--
  type ProfileOverrides = {
    worldStrength?: number;          // world.strength            (recipe default 0.105)
    exposure?: number;               // render.exposure           (recipe default -0.58)
    cardDarkness?: number;           // scales reflection_cards[].color (0=black .. 0.5)
    contactShadowStrength?: number;  // contact_shadows[].alpha   (recipe default 0.115)
    cameraPreset?: "hero" | "front" | "top" | "profile"; // selects ANGLES key
  };

  // SAFE ranges — clamp BEFORE the recipe ever sees a value (G2). 09-AI-SPEC §5.2.
  const KNOB_RANGES = {
    worldStrength:         [0.04, 0.20],
    exposure:              [-1.5, 0.3],
    cardDarkness:          [0.0, 0.5],
    contactShadowStrength: [0.04, 0.22],
  } as const;
  function clamp(n, [lo, hi]): number;       // Math.min(hi, Math.max(lo, n))
  // applyDeltas: current knob values + the verdict's signed deltas → clamped overrides.
  function applyDeltas(current: ProfileOverrides, verdict: VisionVerdict): ProfileOverrides;

  // EnterpriseRecipeRequest gains ONE optional field:
  //   profileOverrides?: ProfileOverrides
-->
<!-- The structured verdict the vision model fills (09-AI-SPEC §5.2 SUPERSEDES the
     5-field placeholder in 09-AI-RESEARCH §1). 8 quality dims, 1=reject .. 5=catalog-ready. -->
<!--
  const score = z.number().int().min(1).max(5);
  visionVerdictSchema = z.object({
    scores: { diamondBrilliance, metalHighlight, metalBelievability, exposureTonal,
              stoneSymmetry, contactShadow, framing, backgroundHoldout },  // each `score`
    flags:  { milky, wrongMetal, brokenHoldout, blownHighlights, emptyOrBroken },  // booleans
    adjust: { worldStrengthDelta: z.number().min(-0.05).max(0.05),
              exposureDelta:      z.number().min(-1).max(1),
              cardDarknessDelta:  z.number().min(-0.4).max(0.4),
              contactShadowDelta: z.number().min(-0.1).max(0.1) },
    cameraPresetSuggestion: z.enum(["hero","front","top","profile"]).nullable(),
    overallScore: score,
    rationale: z.string().max(600),
  });
-->
<!-- decideLoop contract (09-AI-SPEC §5.5). GOOD_ENOUGH=4, MAX_ITERATIONS=2.
     Decision priority (evaluated in order): escalate → accept → autoCorrect → freeze-best.
     accept = D1>=4 && D3>=4 && D2>=3 && D8>=3 && noDimensionEqualsOne && overallScore>=4.
     escalate = brokenHoldout || emptyOrBroken || (iteration>=MAX && !accept).
     autoCorrect = !accept && !escalate && hasNonZeroDelta && iteration<MAX && improvedOverPrevious.
     else → freeze-best (proceed to FINAL with the best-scoring override set seen so far). -->
<!-- Job.combo Json carries { angleKey, metalKey, pass, stoneGroup? } (lib/batches/expand.ts Combo).
     pass==="stone" ⇒ transparent ⇒ G7 drops the contactShadow delta. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the new Phase-9 requirement IDs + the additive Job.intel migration scope</name>
  <decision>Confirm the six new INTEL-* requirement IDs and that the loop state persists as two additive nullable Job columns (no new table).</decision>
  <context>
    Phase 9 is NET-NEW work not present in REQUIREMENTS.md/ROADMAP.md (the v1
    milestone closed at 41 reqs). This plan introduces requirement IDs INTEL-01..06
    (this plan covers INTEL-01 recipe overrides + INTEL-03 loop control/guardrails;
    09-02 covers INTEL-02 vision scorer + INTEL-04 orchestration; 09-03 covers
    INTEL-05 UI; 09-04 covers INTEL-06 calibration). 09-AI-RESEARCH §5 specifies the
    loop state as a nullable `intelState String?` + `intel Json?` on the EXISTING Job
    row (additive, schemaless-friendly, no new table) — consistent with the repo's
    "additive columns, no breaking migrations" stance (Phase 4/5 both added columns
    this way). The alternative (a separate `RenderIntel` table with an FK) is heavier
    and buys nothing at this volume.
  </context>
  <options>
    <option id="additive-columns">
      <title>Option A — two additive nullable Job columns (recommended, 09-AI-RESEARCH §5)</title>
      <pros>Smallest possible change; backward-compatible (existing jobs stay null and behave exactly as today); the `intel` Json IS the audit trace (09-AI-SPEC §7.1) — verdicts, iteration, bestOverrides, guardrailHits, cost, operatorAction all live in one replayable column; mirrors the Phase-4 `result Json?`/`startedAt` additive pattern; one `prisma migrate` against Railway adds two nullable columns with zero backfill.</pros>
      <cons>Querying intel sub-fields is JSON-path, not first-class SQL columns — acceptable at this volume (the operator monitor reads the row, not aggregate SQL).</cons>
    </option>
    <option id="new-table">
      <title>Option B — a separate RenderIntel table (1:1 with Job)</title>
      <pros>First-class queryable iteration/score columns.</pros>
      <cons>New table + FK + join on every monitor read; heavier migration; over-engineered for the volume; diverges from the repo's additive-column convention.</cons>
    </option>
  </options>
  <resume-signal>Select: additive-columns (default — the plan tasks below assume it) or new-table. If new-table is chosen the executor adds the model + FK and the orchestration plan (09-02) writes the row instead of the column.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: profileOverrides + KNOB_RANGES in buildEnterpriseRecipe (pure, backward-compatible)</name>
  <files>lib/enterprise-recipes.ts, lib/intelligence/knobs.ts, test/intel-knobs.test.ts, test/intel-overrides.test.ts</files>
  <behavior>
    knobs.ts (PURE — no prisma/react/sharp/ai imports):
    - Export `ProfileOverrides` (the named-knob type from &lt;interfaces&gt;), `KNOB_RANGES`,
      `clamp(n, [lo,hi])` = Math.min(hi, Math.max(lo, n)), and `applyDeltas(current, verdict)`.
    - applyDeltas: for each numeric knob, new = clamp((current[knob] ?? recipeDefault) + verdict.adjust.*Delta, KNOB_RANGES[knob]); cameraPreset = verdict.cameraPresetSuggestion ?? current.cameraPreset. Only emit a knob key when it actually changed/was-set (keep the override object minimal). Recipe defaults referenced: worldStrength 0.105, exposure -0.58, cardDarkness scale 1.0 baseline, contactShadowStrength 0.115.
    enterprise-recipes.ts:
    - Add optional `profileOverrides?: ProfileOverrides` to EnterpriseRecipeRequest.
    - cameraPreset override: when present, select ANGLES[cameraPreset] INSTEAD of ANGLES[request.angle] (symptom 6/9 — lower front camera). When absent, behavior is unchanged.
    - Apply at the END of recipe assembly, each CLAMPED via clamp(...,KNOB_RANGES[...]):
      worldStrength → world.strength; exposure → render.exposure; contactShadowStrength → every contact_shadows[].alpha; cardDarkness → scale every reflection_cards[].color RGB toward black (multiply the existing [r,g,b] by cardDarkness, leaving alpha; a LOWER cardDarkness = darker cards = readable facets per DOMAIN). Pick the exact scaling so cardDarkness===<the value that reproduces today's colors> is the identity — DOCUMENT the chosen identity baseline in a comment.
    - BACKWARD-COMPAT: when profileOverrides is undefined, EVERY field is byte-identical to today.
    Tests (RED first):
    - intel-knobs.test.ts: clamp() saturates at both bounds; applyDeltas adds delta to current+default then clamps (e.g. milky verdict worldStrengthDelta -0.05 from default 0.105 → 0.055, in range; a +999 hallucinated delta is impossible by schema but clamp still caps to 0.20); cameraPreset passthrough.
    - intel-overrides.test.ts: buildEnterpriseRecipe(req) with NO profileOverrides deep-equals the same call today (snapshot/structural — assert world.strength===0.105, render.exposure===-0.58, contact_shadows[0].alpha===0.115, reflection_cards colors unchanged); WITH profileOverrides {worldStrength:0.06, exposure:-1.0, contactShadowStrength:0.18, cardDarkness:<darker>} the four fields move to the clamped values and nothing else changes; cameraPreset:"front" selects the front ANGLES camera even when request.angle==="hero".
  </behavior>
  <action>
    Create lib/intelligence/knobs.ts (pure) and extend buildEnterpriseRecipe. Keep knobs.ts
    free of prisma/react/sharp/ai imports (unit-tests with zero mocks, mirrors lib/batches/expand.ts).
    Reference the recipe defaults from the EXISTING constants in enterprise-recipes.ts (world
    strength 0.105, exposure -0.58, contact alpha 0.115) — do NOT duplicate magic numbers; export
    them or read them locally so the identity baseline is provably correct. Write the two RED tests
    first, confirm RED, then implement to GREEN. The cardDarkness scaling identity MUST be chosen so
    the no-override path is unchanged — assert that in intel-overrides.test.ts.
  </action>
  <verify>
    <automated>npx vitest run intel-knobs intel-overrides --reporter=dot</automated>
  </verify>
  <done>knobs.ts is a pure module; buildEnterpriseRecipe accepts optional profileOverrides applied at the end, each clamped; the no-override path is byte-identical to today (asserted); both test files GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: visionVerdictSchema (verdict.ts) + decideLoop + guardrails (loop.ts)</name>
  <files>lib/intelligence/verdict.ts, lib/intelligence/loop.ts, test/intel-verdict.test.ts, test/intel-loop-decision.test.ts, test/intel-loop-guardrails.test.ts</files>
  <behavior>
    verdict.ts (PURE — zod only):
    - Export `visionVerdictSchema` (the full 8-dim contract from &lt;interfaces&gt; / 09-AI-SPEC §5.2) and `VisionVerdict` type. Re-`.parse()`-able; the four adjust deltas carry the exact .min()/.max() bounds (worldStrength ±0.05, exposure ±1, cardDarkness ±0.4, contactShadow ±0.1).
    loop.ts (PURE — imports knobs.ts + verdict.ts only):
    - Export GOOD_ENOUGH=4, MAX_ITERATIONS=2.
    - `decideLoop({ verdict, iteration, prevBestScore, pass }): { decision: "accept"|"autoCorrect"|"escalate"|"freeze-best"; reason: string; appliedDeltas?: VisionVerdict["adjust"]; guardrailHits: string[] }`.
      Priority order EXACTLY: escalate → accept → autoCorrect → freeze-best (09-AI-SPEC §5.5).
      accept = diamondBrilliance>=4 && metalBelievability>=4 && metalHighlight>=3 && backgroundHoldout>=3 && noDimensionEqualsOne(scores) && overallScore>=GOOD_ENOUGH.
      escalate = flags.brokenHoldout || flags.emptyOrBroken || (iteration>=MAX_ITERATIONS && !accept).
      autoCorrect = !accept && !escalate && hasNonZeroDelta(adjust) && iteration<MAX_ITERATIONS && overallScore>prevBestScore (stop-on-no-improvement: Δ<=0 ⇒ freeze-best).
    - Guardrail helpers (G5/G7) APPLIED inside decideLoop's autoCorrect path, returning the sanitized deltas + recording guardrailHits:
      G5 forbidden-move: if flags.milky && adjust.exposureDelta>0 → zero exposureDelta, push "forbidden_move:exposure"; if (flat-metal symptom: metalBelievability<=2 && !flags.milky) && adjust.worldStrengthDelta>0 → zero worldStrengthDelta, push "forbidden_move:worldStrength".
      G7 pass-type gate: if pass==="stone" && adjust.contactShadowDelta!==0 → zero contactShadowDelta, push "pass_gate:contactShadow".
      G3 cap: iteration>=MAX_ITERATIONS contributes to escalate/freeze; push "cost_cap"/"max_iterations" where it fires.
      G4 no-improvement: Δ<=0 ⇒ freeze-best, push "no_improvement".
    Tests (RED first):
    - intel-verdict.test.ts: a well-formed verdict parses; an out-of-range delta (exposureDelta:2) is REJECTED; a non-integer score (4.5) is rejected; flags default present.
    - intel-loop-decision.test.ts: clean high verdict → accept; brokenHoldout=true → escalate (even with great scores); emptyOrBroken=true → escalate; a 2–3 verdict with a real delta at iteration 0 improving → autoCorrect; iteration>=2 && !accept → escalate; a non-improving rescore (overallScore<=prevBest) → freeze-best.
    - intel-loop-guardrails.test.ts: milky + exposureDelta>0 → exposureDelta zeroed + "forbidden_move:exposure" hit; flat-metal + worldStrengthDelta>0 → zeroed + hit; stone pass + contactShadowDelta!=0 → zeroed + "pass_gate:contactShadow" hit.
  </behavior>
  <action>
    Create verdict.ts (zod schema only) and loop.ts (pure decision + guardrails). loop.ts imports
    ONLY knobs.ts + verdict.ts — no prisma/ai/sharp/react. Use the exact accept/escalate/autoCorrect
    predicates from 09-AI-SPEC §5.5 verbatim. Write all three RED tests first, confirm RED, implement
    to GREEN. decideLoop must be the SINGLE place the decision priority + G5/G7 guardrails live so the
    cron sweep (09-02) is pure wiring over it.
  </action>
  <verify>
    <automated>npx vitest run intel-verdict intel-loop-decision intel-loop-guardrails --reporter=dot</automated>
  </verify>
  <done>verdict.ts exports the validated 8-dim schema; loop.ts exports decideLoop with escalate→accept→autoCorrect→freeze-best priority + G3/G4/G5/G7 guardrails; all three test files GREEN; loop.ts imports no impure modules.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] additive Job.intelState/intel migration + optimizeWithAi selection field + additive-migration test</name>
  <files>prisma/schema.prisma, lib/validation/batch.ts, test/intel-migration-additive.test.ts</files>
  <action>
    Add to the Job model (per Task-1 decision = additive-columns): `intelState String?` and
    `intel Json?` — both NULLABLE, no default, additive. Run the migration against Railway via the
    DIRECT_URL (prisma migrate dev --name add_job_intel; the migration MUST be additive — two nullable
    columns, NO data transform, NO column drop/rename). This is the BLOCKING gate the rest of the phase
    rides on; existing Job rows stay null and behave exactly as today.
    Add an OPTIONAL `optimizeWithAi: z.boolean().optional().default(false)` to createBatchSchema in
    lib/validation/batch.ts (the per-batch kill-switch / opt-in toggle, default OFF — G9). Do NOT wire
    it into createBatch behavior yet (09-02 consumes it); this plan only lands the validated field so
    the UI (09-03) and orchestration (09-02) have the contract.
    Write test/intel-migration-additive.test.ts: a SOURCE-text assertion over the newest
    prisma/migrations/*/migration.sql proving it only ADDS columns to "Job" (matches /ALTER TABLE
    "Job" ADD COLUMN/ for intelState + intel) and contains NO `DROP COLUMN`, NO `DROP TABLE`, NO
    `ALTER COLUMN ... SET NOT NULL`, NO `RENAME` — i.e. the migration is provably additive and
    backward-compatible (mirrors the Phase 4/5 additive-migration discipline). Also assert
    createBatchSchema.parse({...valid, }) yields optimizeWithAi===false by default and accepts true.
  </action>
  <verify>
    <automated>npx vitest run intel-migration-additive --reporter=dot && npx prisma validate</automated>
  </verify>
  <done>Job has nullable intelState + intel columns via an applied additive migration (DIRECT_URL); createBatchSchema accepts optional optimizeWithAi defaulting false; the additive-migration source test is GREEN; `prisma validate` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vision model → recipe knobs | Untrusted/hallucinatable model deltas cross into recipe-shaping numbers; must be schema-bounded AND clamped before they reach Blender |
| operator selection → createBatch | The optimizeWithAi boolean crosses the Server-Action boundary (validated by createBatchSchema) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | hallucinated/out-of-range knob delta reaching the recipe | mitigate | TWO layers (G2): schema `.min()/.max()` rejects out-of-range deltas at parse; `clamp(value, KNOB_RANGES)` in applyDeltas is the belt-and-suspenders backstop — asserted in intel-knobs/intel-overrides |
| T-09-02 | Tampering | the loop hand-building recipe JSON (bypassing the single quality source) | mitigate | G10 enforced by TYPES — the loop emits ONLY a ProfileOverrides object; buildEnterpriseRecipe is the sole recipe builder; no JSON path crosses the boundary |
| T-09-03 | Elevation | a forbidden corrective move (raise brightness to fix milky) | mitigate | G5 forbidden-move guard zeroes the offending delta in decideLoop even if the model regresses — asserted in intel-loop-guardrails |
| T-09-04 | DoS | unbounded re-render loop burning GPU | mitigate | G3 MAX_ITERATIONS=2 + G4 stop-on-no-improvement in decideLoop freeze-best and stop — asserted in intel-loop-decision |
| T-09-MIG | Tampering | a non-additive migration breaking existing jobs | mitigate | intel-migration-additive asserts the SQL only ADDs nullable columns (no drop/rename/SET NOT NULL) |
</threat_model>

<verification>
- `npx vitest run intel-knobs intel-overrides intel-verdict intel-loop-decision intel-loop-guardrails intel-migration-additive` all GREEN.
- `npx tsc --noEmit` exit 0 (NOT `next build` — local .env.local is clobbered, per phase brief).
- `npx prisma validate` exit 0; migration applied to Railway via DIRECT_URL.
- knobs.ts + verdict.ts + loop.ts import no prisma/ai/sharp/react (pure).
- no-override buildEnterpriseRecipe path is byte-identical to today (asserted).
</verification>

<success_criteria>
The pure foundation + additive migration are in place and tested: buildEnterpriseRecipe
takes optional clamped profileOverrides (backward-compatible), KNOB_RANGES/clamp/applyDeltas
gate every knob, the full 8-dimension visionVerdictSchema is the validated structured-output
contract, decideLoop encodes escalate→accept→autoCorrect→freeze-best with the forbidden-move,
pass-type, cap, and no-improvement guardrails, and Job carries nullable intelState/intel.
</success_criteria>

<output>
Create `.planning/phases/09-adaptive-render-intelligence/09-01-SUMMARY.md` when done.
</output>
