---
phase: 4
slug: orchestration-status
status: approved
shadcn_initialized: true
preset: "inherits Phase 1 — shadcn/ui (new-york, base neutral, CSS-variable teal token layer); no new preset"
created: 2026-06-05
reviewed_at: 2026-06-05
inherits: ../01-secure-foundation-secrets-db-auth/01-UI-SPEC.md
also_reuses:
  - ../02-product-workspace/02-UI-SPEC.md
  - ../03-batch-builder-with-cost-guardrails/03-UI-SPEC.md
---

# Phase 4 — UI Design Contract (Orchestration & Status)

> Visual and interaction contract for Phase 4. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Inheritance note (READ FIRST):** Phase 1's UI-SPEC (`01-UI-SPEC.md`, approved) is the **global design system** — token architecture (primitive → semantic → component), color (teal accent, NO purple; dark default + light), Geist Sans/Mono typography, 8pt spacing, elevation/motion, the app shell (240/56px sidebar + 56px topbar with the **Batches** + **Jobs** nav items), and the seed component library (Button, Table, Badge/Status Pill, Card, Dialog, Toast, Skeleton, Tooltip, Separator, Progress). Crucially, Phase 1 already declared the **status-pill vocabulary this phase exists to render** — `success`/completed, `warning`/queued, `info`/running, `destructive`/failed+cancelled, `neutral`/idle — plus the `--chart-1..6` palette reserved for dashboards. Phase 2 added the **product card + status-pill lifecycle** and the `alloycolour/diamond/stone2/stone3` **group chips**. Phase 3 built the **Batch Builder**, created the `Batch` + N `Job` rows, defined the **batch-status pill vocabulary** and the **combo shape** (`angle · metal · stoneGroup · pass`), and reserved the link target `/batches/[id]` — **the surface THIS phase delivers**. **This Phase 4 spec does NOT redefine any of that.** It (1) specifies the THREE new Phase 4 surfaces (Batches list, Batch detail / jobs monitor, cancel/retry controls), (2) declares the NET-NEW components those surfaces require (aggregate progress bar + stat row, jobs status table, error-log viewer, freshness indicator, cancel/retry controls), and (3) reuses every inherited token, component, status-pill color, and copy tone unchanged. Where a value is "inherited," the executor reads it from Phase 1/2/3, not from here.
>
> **Scale-forward note:** the jobs monitor is the upstream of Gallery (P5). A **completed** job's per-job preview here is deliberately LIGHT (a single thumbnail + "View in gallery" link target reserved for P5); the full layered-output browser (metal JPEG + per-stone PNG, per-layer download) is P5 (`OUT-01..03`), NOT this phase. This phase reads `Layer` rows only to show a thumbnail; it does not build the gallery. Keep the batch-status and job-status vocabulary stable — P5 consumes the same terminal states.

---

## DB-only status model (executor-critical, not a redefinition)

The defining interaction constraint of this phase: **every page load and refresh in the monitor reads Postgres ONLY — never RunPod directly.** Status is written into the DB out-of-band by (a) the secret-verified RunPod **webhook** (the one public M2M route, SEC-04/ORCH-02) and (b) the **Vercel Cron reconciliation** fallback. The UI surfaces that DB state and communicates *freshness*, never liveness via per-request RunPod fan-out (ORCH-02 forbids it; the Vercel 60s cap makes it impossible at batch scale).

| Concept | What the UI does | What the UI must NEVER do |
|---------|------------------|---------------------------|
| Job/batch status | Read `Job.status` / derived batch status from the DB on load + on refresh | Call RunPod `/status/:id` per row on page load |
| "Live-ish" updates | Client **polls the DB** (lightweight `GET` that re-reads Postgres) on an interval, OR Next.js revalidation; surfaces an **"updated Ns ago"** freshness chip + manual **Refresh** | Open a socket to RunPod; fan out N RunPod calls |
| Terminal jobs (`completed`/`failed`/`cancelled`) | Treated as settled — excluded from refresh churn; never re-fetched | Re-poll a terminal job |
| Retry state | Render `Job.attempt` (e.g. "attempt 2 of 2") read straight from the DB | Trigger retries from the client; retries are server/cron-driven (ORCH-03) |

**Job-status → inherited status-pill mapping (DO NOT invent colors).** The DB enum (`prisma/schema.prisma`: `queued | submitted | in_queue | in_progress | completed | failed | cancelled`) collapses to the operator-facing 5-state vocabulary using Phase 1 status tokens:

| DB `JobStatus` value(s) | Operator-facing state | Inherited Phase 1 status token | Pill dot |
|--------------------------|-----------------------|--------------------------------|----------|
| `queued`, `submitted`, `in_queue` | **queued** | `warning` (amber) | amber |
| `in_progress` | **running** | `info` (cyan/blue) | blue, subtle pulse |
| `completed` | **completed** | `success` (green) | green |
| `failed` | **failed** | `destructive` (red) | red |
| `cancelled` | **cancelled** | `neutral` / idle (greyed) | grey |

**Derived batch status (computed from its jobs, not stored as a 6th color):** `queued` (all non-terminal, none running) · `running` (≥1 running, not all terminal) · `completed` (all completed) · `partly failed` (all terminal, ≥1 failed, ≥1 completed → `warning`) · `failed` (all terminal, all failed → `destructive`) · `cancelling`/`cancelled` (cancel requested/applied → `neutral`). The batch pill reuses the same inherited tokens; "partly failed" maps to `warning`, never a new hue.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui — **inherited from Phase 1** (no re-init; `components.json` already established) |
| Preset | Inherited — new-york style, base color neutral, CSS-variable teal token layer in `app/globals.css` |
| Component library | Radix UI primitives via shadcn/ui + Tailwind CSS v4 — **inherited** |
| Icon library | lucide-react `^0.468.0` — **inherited** (16px dense, 20px nav/buttons, stroke 1.75). P4 glyphs: `layers` (batch), `activity` (jobs/monitor), `loader-2` (running spinner), `clock` (queued / freshness "updated Ns ago"), `check-circle-2` (completed), `x-circle` (failed), `ban` / `octagon-x` (cancel/cancelled), `rotate-cw` (retry/attempt), `refresh-cw` (manual refresh), `terminal` / `file-text` (error-log viewer), `chevron-right`/`chevron-down` (row expand), `filter` (status filter) |
| Font (sans) | **Geist Sans** — inherited |
| Font (mono) | **Geist Mono** — inherited; in P4 used for ALL monitor numerics + machine values: job counts (completed/failed/running/queued/total), the combo (`view1 · white · diamond · stone`), `attempt N of 2`, durations (`1m 12s`), the RunPod request id, the freshness counter ("updated 8s ago"), and the **entire error-log body** (worker stdout/stderr tail) |

**New shadcn components to add this phase** (official registry only — see Registry Safety): `scroll-area` (the bounded, scrollable error-log viewer body), `toggle-group` (status filter: All / Queued / Running / Completed / Failed / Cancelled — reused from P3, no re-add if already present), `alert-dialog` (cancel confirmation — destructive-confirm; if `dialog` is preferred the inherited destructive-confirm `Dialog` variant is acceptable). Reused unchanged from Phase 1/2/3: `button badge card table dialog skeleton tooltip separator sonner progress collapsible tabs alert`. The **aggregate progress bar + stat row**, **jobs status table**, **error-log viewer**, **freshness indicator**, and **cancel/retry controls** are **first-party** components composed from these primitives (see New Component Inventory).

---

## Spacing Scale

**Inherited unchanged from Phase 1/2/3** (8-point scale: xs 4 / sm 8 / md 16 / lg 24 / xl 32 / 2xl 48 / 3xl 64; Tailwind `1/2/4/6/8/12/16`). Layout constants (240/56 sidebar, 56 topbar, 1280 content max-width, 24px gutters, 8px card radius, 6px pill radius) inherited.

| Token | Value | Phase-4 application |
|-------|-------|---------------------|
| xs | 4px | status-dot-to-label gap, attempt-badge inner padding, stat-number-to-caption gap |
| sm | 8px | jobs-table cell vertical padding (dense table), stat-row segment gaps, freshness-chip inner padding |
| md | 16px | card padding, gap between the aggregate panel and the jobs table, error-log viewer inner padding |
| lg | 24px | batch-detail section spacing, batches-list card/row padding, gap between page header and aggregate panel |
| xl | 32px | page-header → content gap (Batches list, batch detail) |
| 2xl | 48px | major break between the aggregate-progress block and the jobs table on batch detail |
| 3xl | 64px | empty-state vertical padding (no batches yet) |

**Exceptions (inherited rules applied):** the inherited **44px min hit-area** rule applies to every row action (`…` menu, cancel button, expand toggle), the status-filter toggle chips, and the manual Refresh button. 1px hairlines (table row dividers, error-log top border) exempt from the multiple-of-4 rule. The aggregate progress bar height is **8px** (`Progress` default, inherited). The completed-job thumbnail in a row is a fixed **40×40** media size (not a spacing token); the larger per-job preview (light) is **160×160** reusing Phase 2's product-thumbnail size — both declared media sizes, not new spacing tokens.

---

## Typography

**Inherited unchanged from Phase 1/2/3** — four roles, two weights (400 / 600), tabular-nums on all numeric fields. Geist Mono on all monitor numerics and the entire error-log body.

| Role | Size | Weight | Line Height | Phase-4 usage |
|------|------|--------|-------------|---------------|
| Body | 14px | 400 | 1.5 | job-row text, batch-list product names, dialog body copy, empty/error copy |
| Label | 12px | 600 | 1.4 | table column headers, stat captions ("COMPLETED", "FAILED", "RUNNING", "QUEUED", "TOTAL"), status-pill text (uppercase 0.04em), status-filter chip labels, freshness chip |
| Heading | 20px | 600 | 1.3 | page titles ("Batches", batch-detail title = product name + batch id), dialog titles ("Cancel this batch?") |
| Display | 28px | 600 | 1.2 | not used in P4 (reserved for login/hero) |

- **Aggregate stat numbers** render at **Heading-size mono (20px / 600, tabular-nums)** in the stat row (completed / failed / running / queued / total) — large enough to scan at a glance, but deliberately **NOT** the P3 estimate "big number" (40px). The 40px size remains reserved exclusively for the Batch Builder estimate panel; the monitor is a calm operational read-out, not an alarm, so it stays at Heading-size. (No new typographic exception is introduced this phase.)
- **Mono (Geist Mono, 400):** the combo string, attempt counter, durations, RunPod request id, freshness counter, all stat numbers (600), and the **error-log body** (the worker stdout/stderr tail) — mono signals machine-emitted text vs. operator-authored UI copy.

---

## Color

**Inherited unchanged from Phase 1/2/3** — dark default + light, teal accent (NOT purple), 60/30/10 discipline, the full semantic + status vocabulary. P4 introduces **no new hues**; the whole point of the phase is to render the **already-declared status vocabulary**. The aggregate progress bar is the one place multiple status colors appear together, and it uses the inherited `success`/`warning`/`info`/`neutral` tokens as stacked segments — not new colors, and ordered color-blind-safely per Phase 1.

| Role | Value (dark / light) | Phase-4 usage |
|------|----------------------|---------------|
| Dominant (60%) | `#0A0A0A` / `#FFFFFF` | monitor + batches-list background |
| Secondary (30%) | `#141414` card · `#1F1F1F` border / `#FAFAFA` card · `#E4E4E7` border | batch-list rows/cards, aggregate panel surface, jobs-table surface, error-log viewer surface (slightly inset) |
| Accent (10%) | `#14B8A6` / `#0D9488` | see "Accent reserved for" |
| Destructive | `#F87171` / `#DC2626` | failed jobs, the **Cancel batch / Cancel job** action + its confirm dialog primary; nothing else |
| Foreground | `#FAFAFA`/`#A1A1AA` muted · light `#18181B`/`#71717A` | text + secondary/freshness text |

### Aggregate progress bar — segment colors (inherited status tokens only)

The bar is a single horizontal track segmented by job outcome, segments ordered **completed → running → queued → failed** so green growth reads left-to-right and red failures pool at the right edge:

| Segment | Inherited token | Meaning |
|---------|-----------------|---------|
| completed | `success` (green) | jobs done |
| running | `info` (blue) | in progress (subtle indeterminate shimmer on this segment only, reduced-motion → static) |
| queued | `warning` (amber) | not started yet |
| failed | `destructive` (red) | failed (after retries exhausted) |
| cancelled | `neutral` (grey) | cancelled — shown as a hatched/greyed remainder, not a "loss" |

The numeric truth lives in the stat row beneath; the bar is the at-a-glance proportion. A 100%-complete bar is fully green; an all-failed bar is fully red; the common "mostly done, a couple failed" reads as a long green bar with a small red tail — calm, not alarming.

**Accent reserved for (P4 — extends Phase 1/2/3's list, does not broaden it):**
- The active sidebar **Batches** / **Jobs** nav item (inherited shell behavior).
- Keyboard focus rings (inherited 2px teal).
- The **manual Refresh** affordance and the freshness chip's "live" tick are **NOT** teal — they use `--muted-foreground`; refresh is a quiet utility, not a primary action.
- The **"Retry failed jobs"** button (if in scope, see surfaces) is a **secondary** button (neutral surface + border), NOT teal and NOT destructive — it is a corrective, non-primary action. The one place a primary teal button could appear on these surfaces is a forward CTA like **"View in gallery"** once a batch completes (reserved P5 link); even that is optional and may be a secondary link.

**Accent stays scarce:** the monitor is intentionally accent-light. Status meaning is carried entirely by the inherited semantic status colors (green/blue/amber/red/grey), never by teal. Teal would dilute the "instrument panel" reading where green=good and red=needs-attention must dominate. Failures are red but **calm** — non-blinking, paired with a clear next action, never a full-bleed alarm.

---

## New Component Inventory (Phase 4 only)

Net-new components this phase contributes. Each lists required states. Everything else is reused from Phase 1/2/3 unchanged.

| Component | Built on | Variants | Required states |
|-----------|----------|----------|-----------------|
| **Aggregate progress bar + stat row** | Phase 1 `Progress` + first-party stat row | batch-detail header / batch-list-row mini | A segmented `success/info/warning/destructive/neutral` bar (see Color) above a row of mono stat cells: **completed · failed · running · queued · total** (Label caption + Heading-size mono number, tabular). States: **all-queued** (full amber bar, 0 completed), **in-progress** (mixed segments + running shimmer), **mixed** (green + red tail), **all-done** (full green), **all-failed** (full red), **cancelling/cancelled** (grey hatched), **loading** (skeleton bar + skeleton numbers). A compact single-line variant (just the thin bar + `12/36`) is embedded per row in the Batches list. |
| **Jobs status table** | Phase 1 `Table` (dense) + status pill + first-party | batch-detail centerpiece | Columns: **Combo** (`view1 · white · diamond · stone`, mono) · **Status** (inherited pill) · **Attempt** (mono `1 / 2`, with `rotate-cw` glyph when >1) · **Duration** (mono `1m 12s`, or `—` if not started, live-ticking for running) · **Actions** (`…`: Cancel job when queued/running; expand for log/preview). States: **loading** (skeleton rows, inherited), **empty** (shouldn't normally happen — a batch always has jobs; defensive "No jobs in this batch."), row **expanded** (reveals error-log viewer for failed / light preview for completed), row **hover/focus**, **cancelling** (row pill → cancelling, action disabled). **Group/filter by status** via the status-filter toggle group above the table; sticky header on scroll (inherited Table behavior). Right-aligned numeric (attempt/duration) columns, mono. |
| **Error-log viewer** | new (first-party) + `scroll-area` + `collapsible` | per failed job | Collapsible, **monospace**, bounded-height (`scroll-area`, ~ 240–320px max) viewer showing the worker **stdout/stderr tail** (`Job.error`, which by repo convention holds `status.error || status.output`, last ~4000 chars). Header: "Render log" Label + a **Copy** ghost button (copies the raw text; toast "Log copied"). Body: pre-formatted mono, 12–13px, `--muted-foreground` on the inset secondary surface, preserves whitespace, horizontal scroll for long lines (no wrap-mangling of stack traces). A short **summary line** above the fold restates the failure in one plain sentence (calm, non-alarming) before the raw tail. States: **collapsed** (default — "View render log" affordance with `terminal` glyph), **expanded**, **empty** ("No log was captured for this failure." when `error` is null), **copy-success** (toast). NEVER renders secret/env names to the operator (inherited Phase 1 config-error rule). |
| **Freshness indicator** | new (first-party) + `clock` glyph | header chip | A quiet `--muted-foreground` chip: **"updated {N}s ago"** (mono counter), auto-incrementing client-side; next to it a **Refresh** ghost icon-button (`refresh-cw`). On a poll/revalidate it resets to "updated just now". States: **fresh** (just now / Ns ago), **refreshing** (spinner on the glyph, "updating…"), **stale** (after a long gap or a failed poll → "couldn't refresh — retrying" or a manual Refresh prompt), **auto-refresh on/off** (a tiny toggle or it's implicit; default ON at a sensible interval while the batch is non-terminal, **auto-stops when the batch is terminal** so a finished batch doesn't poll forever). The interval is a single config value (recommend ~5s while running), read from config, not hardcoded in the component. |
| **Cancel control + confirm** | Phase 1 `Dialog` (destructive-confirm) / `alert-dialog` | batch-level + job-level | **Cancel batch** (header, destructive ghost/secondary) and **Cancel job** (row `…`, only enabled for `queued`/`running` jobs — terminal jobs show no cancel). Opens an inherited destructive-confirm dialog; on confirm the affected jobs move to a **cancelling** then **cancelled** state (optimistic pill update, server reconciles). States: enabled (cancelable jobs exist), disabled (nothing cancelable / already terminal), **cancelling** (in-flight, button loading), **cancelled** (reflected in pills + grey bar segment). Copy is calm and states what stops (see Copywriting). |
| **Retry indicator + (optional) retry-failed control** | Phase 1 `Badge` + `Button` (secondary) | per-job auto-retry surface + batch-level manual | **Auto-retry is read-only surface (ORCH-03):** the Attempt column shows `attempt 2 of 2` with a `rotate-cw` glyph; a job that auto-retried but then succeeded shows `completed` with a subtle "retried" marker; a job that exhausted retries shows `failed`. **Manual "Retry failed jobs" (if in scope this phase):** a **secondary** batch-level button shown only when the batch is terminal-with-failures; re-queues just the failed jobs **idempotently** (server checks existing RunPod request id — no duplicate successful renders, ORCH-03). States: hidden (no failures / batch not terminal), enabled, **retrying** (button loading, affected rows → queued), disabled (retry in flight). If manual retry is descoped to a later phase, the component degrades to the read-only Attempt indicator only — the auto-retry surface is the required part; manual retry is the optional part. |

---

## Phase 4 Surfaces

Each surface names its loading / empty / all-queued / in-progress / mixed / all-done / all-failed / cancelling states (state-completeness is the inherited bar; this phase is *about* states).

### 1. Batches list — `/batches` (the inherited **Batches** nav item, currently a placeholder)

- **Header:** Heading **"Batches"** + a mono count on the left. (Optional, lightweight) a status filter / product filter — not required for MVP; the list defaults to the current operator's batches, most-recent first. No primary "create" button here — batches are created from a product (Phase 3); an optional secondary "Build a batch" link may point back to Products.
- **Body:** a list (table or stacked cards) of batches, each row showing: **product name** (Body) + small thumbnail (40×40, reused), the **combo summary** (mono, e.g. `3 metals × 4 angles × 3 passes = 36 jobs` — read back from `Batch.matrix` / `jobCount`), the **compact aggregate progress bar + `12 / 36`** mini variant, the **batch status pill** (derived, inherited tokens), **created** time (mono, relative), and the whole row is the **reopen** affordance → `/batches/[id]`.
- **States:** **loading** = 6 skeleton rows (inherited Skeleton); **empty** = centered empty-state ("No batches yet" + body pointing to building one from a product → [Go to products]); **error** = inline error card + Retry (inherited data-load copy); per-row the mini bar + pill communicate progress at a glance (all-queued amber / in-progress mixed / done green / failed red / cancelled grey). This list reads the DB only; it does not poll RunPod.

### 2. Batch detail / jobs monitor — `/batches/[id]` (THE centerpiece; the reserved Phase 3 link target)

- **Header:** Heading = **product name** + a mono batch id; the **derived batch status pill**; the **freshness indicator** ("updated Ns ago" + Refresh) on the right; the **Cancel batch** control (destructive secondary, enabled only while cancelable jobs exist); and — when the batch is terminal-with-failures — the optional **"Retry failed jobs"** secondary button. A context subline shows the combo summary (mono, from `Batch.matrix`) and quality.
- **Aggregate block:** the **aggregate progress bar + stat row** (segmented bar + completed / failed / running / queued / total). This is the operator's "how's it going" glance.
- **Jobs section:** the **status filter toggle group** (All / Queued / Running / Completed / Failed / Cancelled, with mono counts per tab) above the **jobs status table**. Each row = combo · status pill · attempt · duration · actions. Rows expand:
  - **Failed job → error-log viewer** (collapsible mono stdout/stderr tail + one-line plain summary + Copy).
  - **Completed job → light preview** (a single 160×160 thumbnail of the rendered pass + a **"View in gallery"** link — the link target is the P5 Gallery surface, reserved here; this phase does NOT build the layered browser or per-layer download).
  - **Queued/running job →** no expansion body beyond a "waiting…/rendering…" line + (if running) live duration; the **Cancel job** action lives in the row `…`.
- **Refresh model (DB-only):** while the batch is non-terminal, the client **polls the DB** at the configured interval (recommend ~5s) and/or relies on revalidation; the webhook + cron are what actually move the numbers (the UI just re-reads). The freshness chip shows recency; manual Refresh forces a re-read. When the batch reaches a terminal state, **auto-refresh stops** and the chip reads a final "updated …". The page must function with auto-refresh off (manual Refresh always available) for users who prefer it / reduced-motion contexts.
- **States:**
  - **loading** = skeleton aggregate bar + skeleton stat numbers + skeleton table rows.
  - **all-queued** = full amber bar, stat row `0 / 0 / 0 / N / N`, every job pill = queued, a calm "Queued — waiting for GPU workers to pick these up." note; Cancel batch enabled.
  - **in-progress** = mixed segmented bar with running shimmer, some jobs running (blue, live duration) / some queued; freshness ticking.
  - **mixed (some failed, still running)** = bar shows green + blue + amber + a red tail; failed rows expandable to logs; non-alarming.
  - **all-done** = full green bar, `N / 0 / 0 / 0 / N`, batch pill = completed, freshness frozen, optional "View in gallery" forward link.
  - **all-failed** = full red bar, batch pill = failed, each row expandable to its log, the **"Retry failed jobs"** secondary action (if in scope) prominent-but-calm; copy explains next steps without blame.
  - **partly-failed (terminal)** = green bar + red tail, batch pill = `warning` "partly failed", retry-failed available.
  - **cancelling** = affected pills → cancelling, bar segments shifting to grey, Cancel button → loading; settles to **cancelled** (grey) for the cancelled jobs (completed ones stay green).
  - **error (page-level DB read failed)** = inline error card + Retry (the DB read failed — distinct from a *job* failing); never surfaces a RunPod error to the operator as a page crash.

### 3. Cancel & retry — controls within surfaces 1–2 (ORCH-05 / ORCH-03)

- **Cancel batch:** header destructive-secondary → inherited destructive-confirm dialog → cancels all still-cancelable (`queued`/`running`) jobs; already-`completed` jobs are kept (their outputs aren't thrown away); pills + bar reflect `cancelling → cancelled`. Calm copy names exactly what stops and what's kept.
- **Cancel job:** row `…` action, enabled only for `queued`/`running` → confirm → that single job cancels. Terminal jobs expose no cancel.
- **Auto-retry surface (required):** the Attempt column + "retried" marker make the automatic ~2× idempotent retry **visible** (ORCH-03) — the operator sees `attempt 2 of 2` and understands a transient failure is being handled, without any action.
- **Manual "Retry failed jobs" (optional this phase):** batch-level secondary, only on terminal-with-failures, re-queues failed jobs idempotently. If descoped, the auto-retry surface alone satisfies the "see the retry state" requirement and this control moves to a later phase.

---

## Copywriting Contract

**Tone inherited from Phase 1/2/3:** plain, calm, operational, second-person, no exclamation marks, no blame ("Couldn't…" not "You failed to…"). **Failures must read as clear-but-calm with a next action** — this is an instrument panel, not an alarm. Below are the NET-NEW P4 strings; inherited strings (logout, generic data-load error, 403, generic toasts) are reused as-is.

| Element | Copy |
|---------|------|
| Page title (list) | **Batches** |
| Page title (detail) | {product name} · {batch id} (mono id) |
| Empty — no batches (heading) | **No batches yet** |
| Empty — no batches (body) | Build a batch from a product to start rendering. It'll show up here with live progress. → [Go to products] |
| Batch combo summary | {m} metals × {a} angles × {p} passes = {N} jobs |
| Stat captions | COMPLETED · FAILED · RUNNING · QUEUED · TOTAL |
| Batch status — queued | Queued — waiting for GPU workers to pick these up. |
| Batch status — running | Rendering — {c} of {N} done. |
| Batch status — partly failed | Done with {f} failed — review the logs or retry the failed jobs. |
| Batch status — all failed | All {N} jobs failed. Open a job's log to see what happened, then retry. |
| Batch status — completed | All {N} jobs completed. |
| Job — duration not started | — |
| Job — running | Rendering… |
| Job — attempt | attempt {n} of {max} |
| Job — retried-then-ok marker | retried |
| Error-log — collapsed affordance | View render log |
| Error-log — one-line summary | This render failed on the worker. The log below has the details. |
| Error-log — empty | No log was captured for this failure. |
| Error-log — copy toast | Log copied. |
| Completed-job preview link | View in gallery |
| Freshness — fresh | updated {N}s ago |
| Freshness — just refreshed | updated just now |
| Freshness — refreshing | updating… |
| Freshness — stale / poll failed | Couldn't refresh — retrying. Or [Refresh] now. |
| Refresh button (aria/label) | Refresh |
| Destructive — cancel batch | **Cancel this batch?** "This stops the {q} jobs still queued or running. Renders already completed are kept. This can't be undone." → [Keep rendering] [Cancel batch] (destructive) |
| Destructive — cancel job | **Cancel this job?** "This stops the {combo} render. It can't be undone — you can rebuild the batch later." → [Keep rendering] [Cancel job] (destructive) |
| Cancelling (in-flight) | Cancelling… |
| Cancelled (toast) | Batch cancelled — {q} jobs stopped, {c} completed kept. |
| Retry failed (button) | Retry failed jobs |
| Retry failed (in-flight) | Retrying {f} jobs… |
| Retry failed (toast) | Re-queued {f} failed jobs. |
| Error — page DB read failed | Couldn't load this batch. Check your connection and try again. → [Retry] |

---

## Registry Safety

No third-party shadcn registries. All P4 components are official shadcn/ui blocks or first-party components built on Radix + Tailwind (aggregate progress bar + stat row, jobs status table, error-log viewer, freshness indicator, cancel/retry controls are first-party). No registry vetting gate required.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | scroll-area, alert-dialog (new this phase) + reused Phase 1/2/3 blocks (button, badge, card, table, dialog, skeleton, tooltip, separator, sonner, progress, collapsible, tabs, alert, toggle-group) | not required |
| first-party (built in-repo) | aggregate progress bar + stat row, jobs status table, error-log viewer, freshness indicator, cancel/retry controls | not applicable (no external source) |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved
