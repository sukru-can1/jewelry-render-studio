---
phase: 04-orchestration-status
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - lib/orchestration/batch-status.ts
  - app/(app)/batches/page.tsx
  - app/(app)/batches/status-pill.tsx
  - app/(app)/batches/aggregate-bar.tsx
  - app/api/batches/[id]/status/route.ts
autonomous: true
requirements: [ORCH-04]

must_haves:
  truths:
    - "Batch progress (completed / failed / running / queued / total) is computed from job counts via a single DB groupBy, never from RunPod"
    - "Derived batch status maps to the inherited Phase-1 status tokens (queued/running/completed/partly failed/failed/cancelling/cancelled) with no new color"
    - "Under the immediate-cancel model (04-04, option A), 'cancelling' is only the transient display when cancelRequestedAt is set AND non-terminal jobs still remain; once all cancelable jobs are 'cancelled' the derived status is 'cancelled' — the persisted truth is the job statuses"
    - "The Batches list shows every batch with a product name, combo summary, a compact aggregate bar + n/total, a derived status pill, and reopen → /batches/[id]; DB-only read"
    - "A lightweight GET /api/batches/[id]/status re-reads Postgres (no RunPod) for the client freshness poll"
  artifacts:
    - path: "lib/orchestration/batch-status.ts"
      provides: "groupBy counts + derived batch status (DB-only, shared by list/detail/route)"
      exports: ["batchProgress", "deriveBatchStatus", "type BatchProgress"]
    - path: "app/(app)/batches/page.tsx"
      provides: "Batches list Server Component (requireSession, DB-only, force-dynamic)"
      exports: ["default"]
    - path: "app/(app)/batches/status-pill.tsx"
      provides: "job + batch status → inherited Badge token mapping"
      exports: ["JobStatusPill", "BatchStatusPill"]
    - path: "app/api/batches/[id]/status/route.ts"
      provides: "DB-only freshness JSON (counts + derived status + per-job statuses)"
      exports: ["GET"]
  key_links:
    - from: "app/(app)/batches/page.tsx"
      to: "lib/orchestration/batch-status batchProgress"
      via: "per-batch counts for the mini bar"
      pattern: "batchProgress|groupBy"
    - from: "app/api/batches/[id]/status/route.ts"
      to: "prisma"
      via: "DB-only read (NO lib/runpod import)"
      pattern: "prisma"
---

<objective>
Build the shared DB-only progress engine and the first operator surface: the Batches list. This plan owns the batch-status derivation (reused by the detail monitor in 04-06 and the freshness route), the inherited status-pill mapping, the aggregate bar component, the `/batches` list page, and the lightweight DB-only freshness endpoint.

Purpose: ORCH-04 progress must be DB-derived (RESEARCH Pattern 5) and ORCH-02 forbids any RunPod fan-out on a user path — the freshness route and list page read Postgres only. Centralizing batch-status here prevents the list, detail, and route from diverging.
Output: `lib/orchestration/batch-status.ts`, `status-pill.tsx`, `aggregate-bar.tsx`, `/batches/page.tsx`, `/api/batches/[id]/status/route.ts`. Turns test/orch-progress.test.ts GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-orchestration-status/04-UI-SPEC.md
@.planning/phases/04-orchestration-status/04-01-SUMMARY.md
@.planning/phases/04-orchestration-status/04-04-SUMMARY.md

<read_first>
@lib/orchestration/status-map.ts
@app/(app)/products/page.tsx
@app/components/ui/badge.tsx
@app/components/ui/progress.tsx
@app/components/ui/skeleton.tsx
@lib/auth/rbac.ts
@test/orch-progress.test.ts
@prisma/schema.prisma
</read_first>

<interfaces>
From lib/orchestration/status-map.ts: TERMINAL_STATUSES, isTerminal.
From lib/auth/rbac.ts: requireSession.
Server Component pattern (app/(app)/products/page.tsx): export const runtime="nodejs"; export const dynamic="force-dynamic"; requireSession() first; try/catch DB load → inline error card + Retry; empty-state; map rows.
Batch: id, productId, product.name, matrix Json? (carries angleViewKeys/metalKeys/passes for the combo summary), jobCount Int, status String, cancelRequestedAt, createdAt, jobs Job[]. Job.status JobStatus.
DB JobStatus → operator-facing 5-state pill (UI-SPEC table): queued|submitted|in_queue → "queued" (warning); in_progress → "running" (info); completed → "completed" (success); failed → "failed" (destructive); cancelled → "cancelled" (neutral).
Derived batch status (UI-SPEC): all completed → completed; ≥1 running, not all terminal → running; all non-terminal none running → queued; all terminal + mix completed/failed → "partly failed" (warning); all terminal all failed → "failed". Cancel (immediate model A per 04-04): 'cancelling' is the TRANSIENT display only — derive it when cancelRequestedAt is set AND at least one non-terminal job still remains; once all cancelable jobs are 'cancelled' (the immediate-cancel write usually makes this instantaneous) derive 'cancelled'. cancelRequestedAt is an audit timestamp, not a separate persisted job phase — the persisted truth is the job statuses (both 'cancelling' and 'cancelled' render with the neutral token).
Badge variants available (verify in badge.tsx); status colors must reuse the inherited semantic tokens — NO new hue, NO purple, teal stays scarce.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: batch-status derivation engine (ORCH-04, DB-only)</name>
  <files>lib/orchestration/batch-status.ts</files>
  <read_first>@test/orch-progress.test.ts @lib/orchestration/status-map.ts</read_first>
  <behavior>
    - batchProgress(batchId): prisma.job.groupBy({ by:['status'], where:{ batchId }, _count:{ _all:true } }) → { completed, failed, running, queued, cancelled, total } where running=in_progress and queued=queued+submitted+in_queue.
    - deriveBatchStatus(counts, { cancelRequestedAt }): returns one of 'queued'|'running'|'completed'|'partly_failed'|'failed'|'cancelling'|'cancelled' per the UI-SPEC mapping. Cancel (immediate model A): when cancelRequestedAt is set AND there are still non-terminal jobs (queued+running > 0) → 'cancelling' (transient display); when cancelRequestedAt is set AND all jobs are terminal with cancelled present → 'cancelled'. (In practice the immediate-cancel write makes 'cancelling' rare/brief, but the derivation still handles the window deterministically.)
    - Never imports lib/runpod (DB-only).
  </behavior>
  <action>
Create `lib/orchestration/batch-status.ts` exporting `type BatchProgress`, `batchProgress(batchId: string): Promise<BatchProgress>`, and `deriveBatchStatus(counts: BatchProgress, opts?: { cancelRequestedAt?: Date | null }): BatchStatus`. Implement the count collapse + derivation exactly per the interfaces table, treating 'cancelling' as the transient display when cancelRequestedAt is set and non-terminal jobs remain (immediate-cancel model A — see 04-04). This module is imported by the list page, the detail page (04-06), and the freshness route — it is the single DB-only progress source. Do NOT import lib/runpod here (enforced by test/orch-db-only.test.ts indirectly and by Pattern 6).
  </action>
  <verify>
    <automated>npx vitest run test/orch-progress.test.ts --reporter=dot</automated>
  </verify>
  <done>test/orch-progress.test.ts GREEN: counts via groupBy; derived status covers all-completed/running/queued/partly-failed/all-failed/cancelling/cancelled (cancelling only while cancelRequestedAt set + non-terminal jobs remain).</done>
</task>

<task type="auto">
  <name>Task 2: status-pill + aggregate-bar components (inherited tokens only)</name>
  <files>app/(app)/batches/status-pill.tsx, app/(app)/batches/aggregate-bar.tsx</files>
  <read_first>@app/components/ui/badge.tsx @app/components/ui/progress.tsx @.planning/phases/04-orchestration-status/04-UI-SPEC.md</read_first>
  <action>
Create `status-pill.tsx` exporting `JobStatusPill({ status })` and `BatchStatusPill({ status })` — pure presentational, mapping each DB JobStatus / derived batch status to the inherited Badge semantic variant + label + lucide glyph per the UI-SPEC "Job-status → status-pill" and "Derived batch status" tables (success/warning/info/destructive/neutral; "partly failed" → warning; running → subtle pulse, reduced-motion static). Both 'cancelling' and 'cancelled' use the neutral token (cancelling = transient display per model A). Uppercase Label type, mono where the spec calls for it. NO new color, NO teal for status.
Create `aggregate-bar.tsx` exporting a segmented progress bar + stat row built on the inherited `Progress`: segments ordered completed→running→queued→failed (+ cancelled grey hatched remainder) using success/info/warning/destructive/neutral tokens; below it the mono stat cells COMPLETED · FAILED · RUNNING · QUEUED · TOTAL (Heading-size mono, tabular-nums) per UI-SPEC §New Component Inventory. Provide a `compact` variant (thin bar + `n / total`) for the list rows and a full variant for the detail header. Accept a `BatchProgress` prop. Include loading (skeleton) handling via a prop.
Do not add a new shadcn registry; these are first-party components on existing primitives.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Both components compile; status colors come only from inherited semantic Badge/token variants; compact + full aggregate variants exist; cancelling/cancelled both render neutral.</done>
</task>

<task type="auto">
  <name>Task 3: Batches list page + DB-only freshness route (ORCH-04/02)</name>
  <files>app/(app)/batches/page.tsx, app/api/batches/[id]/status/route.ts</files>
  <read_first>@app/(app)/products/page.tsx @test/orch-progress.test.ts @lib/orchestration/batch-status.ts</read_first>
  <action>
Create `app/(app)/batches/page.tsx` as an async Server Component (`runtime="nodejs"`, `dynamic="force-dynamic"`): requireSession() first; try/catch load batches (orderBy createdAt desc, include product + per-batch status counts via batchProgress or a grouped query) → on error the inherited inline error card + Retry; empty-state "No batches yet" + body + [Go to products] per the UI-SPEC Copywriting table; otherwise render rows: product name + 40×40 thumbnail, combo summary from Batch.matrix (`{m} metals × {a} angles × {p} passes = {N} jobs`, mono), the compact aggregate bar + `n/total`, the derived BatchStatusPill, relative created time (mono), whole row links to `/batches/[id]`. This page reads the DB only — it MUST NOT import lib/runpod (enforced by test/orch-db-only.test.ts in 04-06).
Create `app/api/batches/[id]/status/route.ts`: `runtime="nodejs"`, `export async function GET(req, { params })`. requireSession() first (fail-closed). IDOR-load the batch by id (404 JSON if missing, shape `{ error }`). Return JSON: derived batch status + BatchProgress counts + a per-job `{ id, status, attempt, startedAt, finishedAt }` array — all from Postgres via batchProgress + a prisma.job.findMany. This is the client freshness poll source; it NEVER calls RunPod. Auto-poll cadence is read client-side from config in 04-06; this route just serves a fresh DB snapshot.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run test/orch-progress.test.ts --reporter=dot</automated>
  </verify>
  <done>/batches list renders DB-only with mini bar + derived pill + reopen link and an empty-state; GET /api/batches/[id]/status returns DB-derived counts/status (no RunPod import); requireSession-gated, IDOR 404 on unknown id; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → /batches + /api/batches/[id]/status | Untrusted session/batchId; must be auth-gated + IDOR-checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Spoofing | list page + status route | mitigate | requireSession() first on both (deny-by-default already gates the route tree; defense-in-depth here) |
| T-04-11 | Tampering/EoP (IDOR) | status route | mitigate | Load batch by id; 404 on missing; single-tenant per repo convention |
| T-04-12 | DoS/cost | freshness route | mitigate | DB-only read (groupBy + findMany); NEVER RunPod fan-out — bounded constant-cost query regardless of batch size |
| T-04-13 | Information Disclosure | status route payload | mitigate | Return only id/status/attempt/timestamps; never secret/env names or raw recipe |
| T-04-SC | Tampering | npm installs | mitigate | No installs this plan (first-party components on existing primitives) |
</threat_model>

<verification>
- test/orch-progress.test.ts GREEN; `npx tsc --noEmit` exits 0.
- Neither page.tsx nor the status route imports lib/runpod (DB-only contract; 04-06's test/orch-db-only.test.ts asserts this for the pages).
- 'cancelling' is derived only while cancelRequestedAt is set and non-terminal jobs remain (consistent with 04-04's immediate-cancel model A).
</verification>

<success_criteria>
Batch progress is DB-derived and reused everywhere; the Batches list reads Postgres only and shows at-a-glance progress + derived status; a lightweight DB-only freshness endpoint backs the client poll; the cancel display model matches 04-04 (model A).
</success_criteria>

<output>
Create `.planning/phases/04-orchestration-status/04-05-SUMMARY.md` when done.
</output>
