---
phase: 04-orchestration-status
plan: 06
type: execute
wave: 4
depends_on: ["04-04", "04-05"]
files_modified:
  - app/(app)/batches/[id]/page.tsx
  - app/(app)/batches/[id]/jobs-monitor.tsx
  - app/(app)/batches/[id]/error-log.tsx
  - app/(app)/batches/[id]/freshness.tsx
  - app/(app)/batches/[id]/cancel-controls.tsx
  - app/components/ui/scroll-area.tsx
  - app/components/ui/alert-dialog.tsx
  - test/orch-db-only.test.ts
autonomous: false
requirements: [ORCH-04, ORCH-05, ORCH-03]

must_haves:
  truths:
    - "An operator opens /batches/[id] and sees the aggregate bar + stat row, a jobs status table (combo · status · attempt · duration · actions), and the derived batch pill — all from the DB"
    - "A failed job row expands to a monospace error-log viewer (Job.error tail) with a one-line summary + Copy; a completed row shows a light thumbnail + 'View in gallery' link target"
    - "The Attempt column surfaces auto-retry (attempt N of 2) read straight from the DB — retry is never client-triggered"
    - "A freshness chip shows 'updated Ns ago' and polls the DB-only status route while the batch is non-terminal, auto-stopping when terminal; manual Refresh always works"
    - "Cancel batch / Cancel job open a destructive confirm and call the cancel Server Actions; terminal jobs expose no cancel; the optimistic 'cancelling' pill resolves to 'cancelled' on the next DB read (immediate-cancel model A from 04-04)"
    - "No app/(app)/batches page imports lib/runpod (DB-only contract enforced by a source-text test)"
  artifacts:
    - path: "app/(app)/batches/[id]/page.tsx"
      provides: "Batch detail monitor Server Component (requireSession, IDOR, DB-only)"
      exports: ["default"]
    - path: "app/(app)/batches/[id]/jobs-monitor.tsx"
      provides: "Client jobs table + status filter + freshness poll wiring"
      min_lines: 60
    - path: "app/(app)/batches/[id]/error-log.tsx"
      provides: "Collapsible monospace stdout/stderr tail viewer + Copy"
    - path: "test/orch-db-only.test.ts"
      provides: "Source-text guard: no lib/runpod import in batches pages (now hard-fails)"
      contains: "page.tsx"
  key_links:
    - from: "app/(app)/batches/[id]/jobs-monitor.tsx"
      to: "app/api/batches/[id]/status route"
      via: "client freshness poll (DB-only)"
      pattern: "batches/.*status"
    - from: "app/(app)/batches/[id]/cancel-controls.tsx"
      to: "lib/orchestration/cancel"
      via: "cancelBatch / cancelJob Server Actions"
      pattern: "cancelBatch|cancelJob"
---

<objective>
Deliver the centerpiece operator surface — the batch detail / jobs monitor at `/batches/[id]` — and lock the DB-only-reads contract with a hard source test. This composes the Wave 3 batch-status engine, aggregate bar, and status pills with a jobs table, the error-log viewer, the freshness indicator (DB poll), and the cancel/retry controls wired to the Wave 2 cancel actions.

Purpose: This is the surface ORCH-04 + ORCH-05 are about and the reserved Phase-3 `/batches/[id]` link target. It must function entirely on DB reads (ORCH-02) — the webhook + crons move the numbers, the UI just re-reads.
Output: `/batches/[id]/page.tsx` + jobs-monitor / error-log / freshness / cancel-controls client components, the two new shadcn primitives (scroll-area, alert-dialog), and the now-hard-failing DB-only source test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-orchestration-status/04-UI-SPEC.md
@.planning/phases/04-orchestration-status/04-05-SUMMARY.md
@.planning/phases/04-orchestration-status/04-04-SUMMARY.md

<read_first>
@app/(app)/batches/page.tsx
@app/(app)/batches/status-pill.tsx
@app/(app)/batches/aggregate-bar.tsx
@lib/orchestration/batch-status.ts
@lib/orchestration/cancel.ts
@app/(app)/products/[id]/batches/new/batch-builder.tsx
@app/components/ui/table.tsx
@app/components/ui/collapsible.tsx
@app/components/ui/dialog.tsx
@app/components/ui/toggle-group.tsx
@test/orch-db-only.test.ts
@test/deny-default.test.ts
</read_first>

<interfaces>
From lib/orchestration/batch-status.ts: batchProgress, deriveBatchStatus, BatchProgress.
From lib/orchestration/cancel.ts (Wave 2): cancelBatch(batchId), cancelJob(jobId) Server Actions. These use the IMMEDIATE-CANCEL model A (04-04): a cancelable job is written straight to status 'cancelled' (cancelRequestedAt = audit timestamp). The UI 'cancelling' state is therefore a brief client-side optimistic label only (useTransition), resolving to 'cancelled' on the next freshness read — do NOT build a two-step "wait for reconcile to confirm" flow.
From app/(app)/batches/status-pill.tsx + aggregate-bar.tsx (Wave 3): JobStatusPill, BatchStatusPill, AggregateBar.
Freshness poll source (Wave 3): GET /api/batches/[id]/status → { status, progress, jobs:[{id,status,attempt,startedAt,finishedAt}] }.
Job row fields for the table: combo Json? (render as `view1 · white · diamond · stone`, mono), status, attempt, startedAt/finishedAt (duration, mono `1m 12s` or `—`), error String? (the stdout/stderr tail for the log viewer), result Json? (image thumbnail for a completed row — read Layer rows only for a thumbnail per UI-SPEC scale-forward note; do NOT build the gallery).
New shadcn components (official registry, UI-SPEC §Registry Safety): scroll-area (bounded error-log body), alert-dialog (destructive cancel confirm). Add via the official shadcn registry only — NO third-party registry.
Copywriting: use the exact strings from the UI-SPEC Copywriting Contract table (stat captions, batch-status lines, error-log affordance/summary/empty/copy-toast, freshness states, cancel confirm copy, retry copy).
Freshness interval: a single config value (~5s while running), read from config, not hardcoded in the component; auto-stop when the batch is terminal (reduced-motion → static, manual Refresh always available).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scroll-area + alert-dialog shadcn primitives</name>
  <files>app/components/ui/scroll-area.tsx, app/components/ui/alert-dialog.tsx</files>
  <read_first>@app/components/ui/dialog.tsx @app/components/ui/collapsible.tsx</read_first>
  <action>
Add the official shadcn `scroll-area` and `alert-dialog` components (new-york style, matching the inherited components.json) into `app/components/ui/`. Prefer the shadcn CLI add; if unavailable, hand-place the official component source consistent with the existing ui/ files (same import aliases, cn util, Radix primitives). NO third-party registry (UI-SPEC Registry Safety). These back the bounded error-log body and the destructive cancel confirm.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>scroll-area.tsx + alert-dialog.tsx exist in app/components/ui, compile, and match the inherited shadcn style; no new registry added.</done>
</task>

<task type="auto">
  <name>Task 2: Batch detail page + jobs monitor + error-log + freshness + cancel controls (ORCH-04/05/03)</name>
  <files>app/(app)/batches/[id]/page.tsx, app/(app)/batches/[id]/jobs-monitor.tsx, app/(app)/batches/[id]/error-log.tsx, app/(app)/batches/[id]/freshness.tsx, app/(app)/batches/[id]/cancel-controls.tsx</files>
  <read_first>@app/(app)/batches/page.tsx @app/(app)/batches/aggregate-bar.tsx @app/(app)/batches/status-pill.tsx @lib/orchestration/cancel.ts @.planning/phases/04-orchestration-status/04-UI-SPEC.md</read_first>
  <action>
Create `app/(app)/batches/[id]/page.tsx` — async Server Component (`runtime="nodejs"`, `dynamic="force-dynamic"`): requireSession() first; IDOR-load the batch by params.id with product + jobs (404 / inline error per UI-SPEC "Couldn't load this batch"); compute batchProgress + deriveBatchStatus server-side for the first paint; render the header (product name + mono batch id, derived BatchStatusPill, freshness chip, Cancel batch control, and — only when terminal-with-failures — the optional secondary "Retry failed jobs" control), the AggregateBar (full variant), and the client `<JobsMonitor>` seeded with the initial DB snapshot. This page imports prisma + batch-status ONLY — NEVER lib/runpod (DB-only, ORCH-02).
Create `jobs-monitor.tsx` (`"use client"`): the jobs status table (Table, dense) with columns Combo (mono) · Status (JobStatusPill) · Attempt (mono `n / 2`, rotate-cw glyph when >1 — the read-only auto-retry surface, ORCH-03) · Duration (mono, live-ticking for running) · Actions (`…`); a status-filter toggle-group (All/Queued/Running/Completed/Failed/Cancelled with mono counts); row expand → `<ErrorLog>` for failed / a light 160×160 thumbnail + "View in gallery" link target for completed (no gallery build). It polls GET /api/batches/[id]/status at the config interval while the batch is non-terminal, reseeds the table + aggregate, and renders the `<Freshness>` chip; it auto-stops polling on a terminal batch. Cancel actions are invoked through `<CancelControls>`.
Create `error-log.tsx` (`"use client"`): collapsible, monospace, bounded-height (scroll-area ~240–320px) viewer of the Job.error stdout/stderr tail with the one-line plain summary above the fold and a Copy ghost button (toast "Log copied."); empty state "No log was captured for this failure." NEVER render secret/env names (inherited config-error rule).
Create `freshness.tsx` (`"use client"`): the quiet `--muted-foreground` "updated Ns ago" mono chip + Refresh ghost icon-button; states fresh/refreshing/stale per the Copywriting table; interval from config; auto-stop when terminal.
Create `cancel-controls.tsx` (`"use client"`): Cancel batch (destructive secondary, enabled only while cancelable jobs exist) + per-row Cancel job (only for queued/running), each opening the alert-dialog destructive confirm with the exact UI-SPEC copy, calling cancelBatch/cancelJob via useTransition + toast. Under the immediate-cancel model A (04-04), the optimistic 'cancelling' pill is a brief client-side label that resolves to the persisted 'cancelled' on the next freshness read — do NOT implement a "poll until reconcile confirms" two-step; the Server Action already wrote 'cancelled'. If manual "Retry failed jobs" is in scope, wire it as a secondary action calling a re-queue path; per RESEARCH/UI-SPEC it MAY degrade to the read-only Attempt surface only — auto-retry (already shipped in Wave 2) satisfies ORCH-03 regardless.
Use the inherited tokens/components only; teal stays scarce; failures calm. Build with the ui-ux-pro-max sensibility per CLAUDE.md (no purple).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>/batches/[id] renders the aggregate block + jobs table + filters + row expansion (error-log / completed thumbnail) + freshness chip + cancel controls, all from DB reads; the cancel optimistic 'cancelling' resolves to 'cancelled' (model A, no reconcile two-step); requireSession + IDOR guard the page; tsc + next build succeed.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the DB-only-reads source test</name>
  <files>test/orch-db-only.test.ts</files>
  <read_first>@test/orch-db-only.test.ts @test/deny-default.test.ts @app/(app)/batches/page.tsx @app/(app)/batches/[id]/page.tsx</read_first>
  <action>
Update `test/orch-db-only.test.ts` (the Wave 0 scaffold) so it now HARD-asserts (readFileSync, mirror deny-default.test.ts) that neither `app/(app)/batches/page.tsx` nor `app/(app)/batches/[id]/page.tsx` contains the strings `@/lib/runpod`, `submitRunPod`, `getRunPodStatus`, `cancelRunPod`, or a bare `runpod` import — both files now exist so the previously skip-if-absent branch becomes a real gate (ORCH-02 DB-only contract, Pattern 6). Keep the `/api/batches/[id]/status` route in the assertion set too (it must also be DB-only).
  </action>
  <verify>
    <automated>npx vitest run test/orch-db-only.test.ts --reporter=dot</automated>
  </verify>
  <done>test/orch-db-only.test.ts GREEN and now hard-fails if any batches page or the status route imports lib/runpod.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the jobs monitor (visual + DB-only behavior)</name>
  <action>Manually verify the assembled /batches and /batches/[id] monitor surfaces in the browser after the automated tests + next build pass. This is a verification-only checkpoint — no code is written here; it confirms the visual states, the DB-only freshness behavior, and the cancel flow per the steps below.</action>
  <what-built>The batch detail / jobs monitor at /batches/[id]: aggregate progress bar + stat row, jobs status table with status filter, row expansion (error-log viewer for failed jobs, light thumbnail for completed), the freshness "updated Ns ago" chip with auto-poll/auto-stop, and the cancel batch / cancel job destructive-confirm controls — all reading the DB only.</what-built>
  <how-to-verify>
1. `npm run dev`, log in, open `/batches`. Confirm the list shows your existing batches with a mini progress bar + n/total + derived status pill; click a batch row → `/batches/[id]`.
2. On the detail page confirm: the aggregate bar + the COMPLETED/FAILED/RUNNING/QUEUED/TOTAL stat row; the jobs table with combo · status pill · attempt · duration · actions; the status-filter toggle group filters rows; the "updated Ns ago" chip ticks and Refresh re-reads.
3. Expand a failed job (if any) → the monospace render-log + Copy works; expand a completed job → the 160×160 thumbnail + "View in gallery" link.
4. Click "Cancel batch" → the destructive confirm copy reads correctly → on confirm, cancelable jobs move to cancelling then cancelled (model A — the cancelling label is brief, the persisted state is cancelled) and completed jobs stay green. (Live RunPod dispatch/webhook + cron timing are verified separately in prod per 04-VALIDATION Manual-Only table — here verify the UI states + DB-only behavior.)
5. Confirm no purple anywhere; status meaning carried by green/blue/amber/red/grey only; failures read calm, not alarming.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/interaction issues to fix.</resume-signal>
  <verify><human-check>Operator confirms the monitor states, freshness poll/auto-stop, error-log, and cancel flow render correctly with no purple and status-only colors.</human-check></verify>
  <done>Operator approves the jobs monitor surfaces; any reported issues are fixed before the phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → /batches/[id] | Untrusted batchId; must be auth-gated + IDOR-loaded |
| Client poll → /api/batches/[id]/status | DB-only; must never trigger RunPod |
| Operator → cancel controls | Invoke cancel Server Actions (guarded in Wave 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Spoofing | detail page | mitigate | requireSession() first; route tree already deny-by-default |
| T-04-11 | Tampering/EoP (IDOR) | detail page | mitigate | IDOR-load batch by params.id; 404/error on missing |
| T-04-12 | DoS/cost | freshness poll | mitigate | poll hits the DB-only status route; source test forbids lib/runpod import in pages → no per-request RunPod fan-out at batch scale |
| T-04-14 | Information Disclosure | error-log viewer | mitigate | render only the worker stdout/stderr tail (Job.error); NEVER secret/env names (inherited config-error rule) |
| T-04-SC | Tampering | scroll-area/alert-dialog add | mitigate | Official shadcn registry only (UI-SPEC Registry Safety); no third-party registry, no new npm dep beyond official component source |
</threat_model>

<verification>
- test/orch-db-only.test.ts GREEN and hard-asserting no runpod import in batches pages + status route.
- `npx tsc --noEmit` exits 0; `npx next build` succeeds.
- Full suite green: `npx vitest run` (phase gate).
- Human-verify checkpoint approved.
</verification>

<success_criteria>
The /batches/[id] monitor presents DB-derived progress, per-job status/attempt/duration, expandable error-logs and completed thumbnails, a freshness poll that auto-stops on terminal, and working cancel controls (immediate-cancel model A) — with the DB-only contract enforced by a source test and no purple/teal-as-status.
</success_criteria>

<output>
Create `.planning/phases/04-orchestration-status/04-06-SUMMARY.md` when done.
</output>
