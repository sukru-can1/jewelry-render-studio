---
phase: 04-orchestration-status
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - prisma/schema.prisma
  - lib/env.ts
  - .env.example
  - vercel.json
  - lib/orchestration/status-map.ts
  - test/orch-dispatch.test.ts
  - test/orch-webhook.test.ts
  - test/orch-reconcile.test.ts
  - test/orch-retry.test.ts
  - test/orch-cancel.test.ts
  - test/orch-progress.test.ts
  - test/orch-db-only.test.ts
autonomous: false
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04, ORCH-05]
user_setup:
  - service: vercel
    why: "CRON_SECRET authenticates Vercel Cron → cron route handlers; APP_URL builds the absolute webhook URL passed to RunPod. Both must exist in the deployed Vercel project env, not just .env.local. The per-minute cron cadence added to vercel.json (A3) requires a Vercel Pro plan — per-minute crons are not available on Hobby; the deploy orchestrator must verify the plan and adjust the cadence (e.g. to a coarser schedule) if Pro is unavailable."
    env_vars:
      - name: CRON_SECRET
        source: "Generate a random secret (e.g. `openssl rand -hex 32`); set in Vercel Project Settings → Environment Variables AND in .env.local for local test runs"
      - name: APP_URL
        source: "The deployed production base URL, e.g. https://jewelry-render-studio.vercel.app (in prod, VERCEL_PROJECT_PRODUCTION_URL is auto-provided as a fallback). If neither APP_URL nor VERCEL_PROJECT_PRODUCTION_URL resolves to a valid https origin, the dispatcher refuses to submit and leaves jobs queued (A5) — so this must resolve in the deployed env."
    dashboard_config:
      - task: "Confirm the project is on a Vercel Pro plan (per-minute crons require Pro) or adjust the vercel.json cron schedule to a supported cadence before deploy"
        location: "Vercel Dashboard → Project → Settings → Crons (and Plan/Billing)"

must_haves:
  truths:
    - "The Prisma migration adds Job.result, Job.startedAt, Job.cancelRequestedAt, and Batch.cancelRequestedAt without dropping or altering existing columns"
    - "A missing CRON_SECRET fails fast at import time via the typed env schema"
    - "mapRunPodStatus collapses every RunPod state to the DB JobStatus enum and exposes the terminal set"
    - "vercel.json schedules both /api/cron/dispatch and /api/cron/reconcile so the orchestration code actually fires; the file stays valid JSON and next build passes"
    - "Every Phase 4 requirement has a failing (RED) test file present that the implementation waves turn green"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Additive orchestration fields on Job and Batch"
      contains: "cancelRequestedAt"
    - path: "vercel.json"
      provides: "crons array scheduling the dispatch + reconcile cron routes"
      contains: "/api/cron/dispatch"
    - path: "lib/orchestration/status-map.ts"
      provides: "Shared RunPod→JobStatus mapping + terminal set used by webhook + reconcile"
      exports: ["mapRunPodStatus", "TERMINAL_STATUSES", "isTerminal"]
    - path: "lib/env.ts"
      provides: "CRON_SECRET (required) + APP_URL (optional) typed env"
      contains: "CRON_SECRET"
    - path: "test/orch-db-only.test.ts"
      provides: "Source-text guard scaffold asserting no lib/runpod import in batches pages"
      contains: "page.tsx"
  key_links:
    - from: "lib/orchestration/status-map.ts"
      to: "prisma JobStatus enum"
      via: "mapping returns enum literal values"
      pattern: "completed|failed|cancelled|in_progress|in_queue"
    - from: "vercel.json crons"
      to: "app/api/cron/dispatch + app/api/cron/reconcile routes"
      via: "scheduled GET invocations"
      pattern: "/api/cron/(dispatch|reconcile)"
---

<objective>
Lay the Wave-0 foundation every other Phase 4 plan builds on: the [BLOCKING] additive Prisma migration (decision #4), the two new typed env vars (decision #5), the Vercel cron schedule that actually fires the orchestration routes (A3), the single shared RunPod→DB status mapper that the webhook and reconcile paths both reuse, and the seven failing test files that lock in the behavioral contract per 04-VALIDATION.md.

Purpose: A migration must land before any code reads the new columns; the cron schedule must exist or the dispatch/reconcile routes built in later waves never run on a timer (queued jobs would sit forever); a shared status map prevents drift between the webhook and the reconcile cron (Don't Hand-Roll: "one shared mapRunPodStatus()"); RED test scaffolds make every requirement's acceptance criteria concrete before implementation.
Output: Migrated schema, regenerated Prisma client, extended env, the vercel.json crons block, `lib/orchestration/status-map.ts`, and 7 failing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-orchestration-status/04-RESEARCH.md
@.planning/phases/04-orchestration-status/04-VALIDATION.md

<read_first>
@prisma/schema.prisma
@lib/env.ts
@.env.example
@vercel.json
@lib/products/inspection.ts
@test/inspection-dispatch.test.ts
@test/webhook-auth.test.ts
@workers/runpod-blender/handler.py
</read_first>

<interfaces>
Existing Job fields (DO NOT re-add): id, batchId, status (JobStatus enum), runpodJobId String?, recipe Json?, combo Json?, attempt Int @default(0), error String?, outputPrefix String?, submittedAt DateTime?, finishedAt DateTime?, @@index([batchId, status]).
Existing JobStatus enum values: queued | submitted | in_queue | in_progress | completed | failed | cancelled.
Existing Batch fields: id, productId, createdById String?, status String @default("draft"), matrix Json?, jobCount Int, createdAt.
Existing vercel.json: { $schema, framework:"nextjs", functions:{ "app/api/**/*.ts": { maxDuration:60 } } } — NO crons block yet (the dispatch/reconcile routes built in Waves 1–2 would never be invoked on a schedule without it).

RunPod terminal callback shape (from handler.py + RunPod docs): { id, status, output?, error?, delayTime?, executionTime? } where status ∈ COMPLETED | FAILED | CANCELLED | IN_PROGRESS | IN_QUEUE | TIMED_OUT. Worker success output: { job_id, image_key, image_url, image_blob, metadata_key, metadata_blob }. Worker failure output (under output on COMPLETED-with-error, or top-level on FAILED): { error, stdout, stderr }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive Prisma migration + regenerate client</name>
  <files>prisma/schema.prisma</files>
  <read_first>@prisma/schema.prisma (lines 131-159: Batch + Job models)</read_first>
  <action>
Add ONLY these additive, nullable fields (decision #4). First confirm none already exist in schema.prisma; all four are confirmed ABSENT in the current schema — if any does already exist, skip just that field.
On `model Job`: add `result Json?` (persists the webhook `output` so Phase 5 reads layers from the DB without re-fetching RunPod — ORCH-02/04), `startedAt DateTime?` (render start for live duration in the monitor — ORCH-04), `cancelRequestedAt DateTime?` (distinguishes "cancelling" from "cancelled" — ORCH-05).
On `model Batch`: add `cancelRequestedAt DateTime?` (batch-level cancelling state — ORCH-05).
Per decision #4/A2: REUSE the existing `Job.error` for the stdout/stderr tail and the existing `Job.runpodJobId` as the idempotency/join key — do NOT add `log` or `runpodRequestId` columns. Per A4: leave `Batch.status` as a free `String` — batch status is derived from job counts, never stored as a 6th value.
After editing, run the [BLOCKING] migration against the live seeded DB (migrations use DIRECT_URL per the schema header) and regenerate the client. This migration is ADDITIVE-ONLY — it adds four nullable columns and MUST NOT drop, rename, or alter any existing User/Batch/Job column. `prisma migrate dev` must NOT be allowed to reset/drop the database: if it detects drift and would prompt to reset (or reports the schema is not in sync in a way that requires a reset), STOP IMMEDIATELY, do not confirm the reset, and surface the situation — do not proceed past a destructive prompt against the live seeded DB. For prod/Vercel builds the deploy path is `npx prisma migrate deploy` (non-interactive, applies committed migrations only — never resets); document this in the SUMMARY. This task is ordered before all verification; the migration MUST complete before any later wave reads the new columns.
  </action>
  <verify>
    <automated>npx prisma migrate dev --name orchestration-fields && npx prisma generate && npx prisma migrate status</automated>
  </verify>
  <done>Migration applied additively; `npx prisma migrate status` reports the DB up to date (no pending/failed migration, no reset performed); POST-migration the four nullable columns (Job.result, Job.startedAt, Job.cancelRequestedAt, Batch.cancelRequestedAt) now exist AND no existing User/Batch/Job column was dropped, renamed, or retyped (confirm via `prisma migrate status` + inspecting the generated migration SQL contains only ADD COLUMN statements for these four columns and no DROP/ALTER of existing columns).</done>
</task>

<task type="auto">
  <name>Task 2: Add CRON_SECRET + APP_URL to typed env and .env.example</name>
  <files>lib/env.ts, .env.example</files>
  <read_first>@lib/env.ts</read_first>
  <action>
In `lib/env.ts` server schema add `CRON_SECRET: z.string().min(1)` (required — Vercel auto-sends `Authorization: Bearer ${CRON_SECRET}` to cron routes; decision #5/A3). Add `APP_URL: z.string().url().optional()` (used at dispatch to build the absolute webhook URL; in production `VERCEL_PROJECT_PRODUCTION_URL` is the documented fallback — A5). Do NOT make APP_URL required: the dispatcher falls back to the Vercel-provided var, and if NEITHER resolves to a valid https origin the dispatcher refuses to submit (handled in 04-02 Task 1) rather than building a broken `https://undefined` URL. Optionally, to harden this, you MAY add a small exported helper here, e.g. `resolveAppBaseUrl(): string | null` that returns `env.APP_URL` or `https://${process.env.VERCEL_PROJECT_PRODUCTION_URL}` only when it parses as a valid https URL (via `new URL(...)` + protocol check), else null — 04-02 imports it. The existing `RUNPOD_WEBHOOK_SECRET` is already in this schema and is the canonical webhook-secret name used by both the dispatch URL builder (04-02) and the webhook route check (04-02) — do NOT introduce a second/different secret env var.
In `.env.example` add a commented section: `CRON_SECRET=` ("Vercel Cron auth — Authorization: Bearer; set in Vercel env + .env.local") and `APP_URL=` ("Deployed base URL for the RunPod webhook callback; prod falls back to VERCEL_PROJECT_PRODUCTION_URL").
Keep the createEnv pattern; never read these via process.env elsewhere except inside cron route auth where the existing webhook scaffold already reads process.env directly (consistency with that one scaffold is acceptable).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`env.CRON_SECRET` is typed/required; APP_URL optional; RUNPOD_WEBHOOK_SECRET confirmed as the single webhook-secret name; .env.example documents both new vars; tsc exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Vercel cron schedule for dispatch + reconcile</name>
  <files>vercel.json</files>
  <read_first>@vercel.json</read_first>
  <action>
Add a `crons` array to `vercel.json` (alongside the existing `framework` and `functions` keys — do NOT remove or alter them) scheduling BOTH orchestration cron routes built in Waves 1–2, so the dispatcher and reconciler actually fire on a timer instead of queued jobs sitting forever:
`"crons": [ { "path": "/api/cron/dispatch", "schedule": "* * * * *" }, { "path": "/api/cron/reconcile", "schedule": "* * * * *" } ]`
(every minute). Note in the SUMMARY that per-minute crons require a Vercel Pro plan (A3) — recorded in user_setup — and that the deploy orchestrator will verify the plan and adjust the cadence to a supported schedule if Pro is unavailable. The route paths MUST exactly match the routes created in 04-02 (`app/api/cron/dispatch/route.ts`) and 04-03 (`app/api/cron/reconcile/route.ts`). Keep the file valid JSON. vercel.json is owned exclusively by this Wave-0 plan in Phase 4 (no other plan touches it), so this addition is parallel-safe.
  </action>
  <verify>
    <automated>node -e "const c=require('./vercel.json'); const p=(c.crons||[]).map(x=>x.path); if(!p.includes('/api/cron/dispatch')||!p.includes('/api/cron/reconcile')){process.exit(1)}" && npx next build</automated>
  </verify>
  <done>vercel.json contains both cron paths (/api/cron/dispatch and /api/cron/reconcile), remains valid JSON (the node parse + path check exits 0), and `npx next build` still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Shared status-map module + 7 failing RED test scaffolds</name>
  <files>lib/orchestration/status-map.ts, test/orch-dispatch.test.ts, test/orch-webhook.test.ts, test/orch-reconcile.test.ts, test/orch-retry.test.ts, test/orch-cancel.test.ts, test/orch-progress.test.ts, test/orch-db-only.test.ts</files>
  <read_first>@test/inspection-dispatch.test.ts @test/webhook-auth.test.ts @test/deny-default.test.ts @test/setup.ts</read_first>
  <behavior>
    - mapRunPodStatus("COMPLETED") → "completed"; "FAILED" → "failed"; "TIMED_OUT" → "failed"; "CANCELLED" → "cancelled"; "IN_PROGRESS" → "in_progress"; "IN_QUEUE" → "in_queue"; unknown → null (caller ignores).
    - TERMINAL_STATUSES contains exactly completed, failed, cancelled; isTerminal(s) reflects membership.
    - The 7 test files import the (not-yet-existing) Wave 1/2/3 modules and assert their contracts, so they are RED now and the later waves turn them GREEN.
  </behavior>
  <action>
Create `lib/orchestration/status-map.ts` exporting: `mapRunPodStatus(runpodStatus: string): JobStatus | null` (mapping per behavior, using @prisma/client JobStatus literal values), `TERMINAL_STATUSES` as a readonly tuple `["completed","failed","cancelled"]`, and `isTerminal(status: JobStatus): boolean`. This is the SINGLE source the webhook and reconcile both import (Don't Hand-Roll). Also export a `tail(text, max=4000)` helper that truncates worker stdout/stderr to the repo's ~4000-char convention.
Create 7 RED test files mirroring the mocking style of inspection-dispatch.test.ts (vi.hoisted mocks for `@/lib/runpod`, `@/lib/db/prisma`, `@/lib/auth/rbac`, `next/cache`):
  - test/orch-dispatch.test.ts (ORCH-01): asserts the dispatcher claims ≤CHUNK queued jobs, optimistic-claims via updateMany where status:'queued', submits with recipe + an absolute webhook URL carrying the secret + job_id, persists runpodJobId + non-terminal status, skips jobs whose batch is cancelled, releases a job back to 'queued' on submit error, AND — when no base URL is resolvable (APP_URL and VERCEL_PROJECT_PRODUCTION_URL both unset) — does NOT call submitRunPod and returns any just-claimed job to status 'queued' (A5: never submit a `https://undefined` webhook URL). Import target: `@/lib/orchestration/dispatch` (Wave 1).
  - test/orch-webhook.test.ts (ORCH-02/04): asserts POST maps COMPLETED→completed+result+finishedAt, FAILED→failed+error tail+finishedAt, by matching body.id to runpodJobId; idempotency: a duplicate/late callback on an already-terminal job no-ops (updateMany where status notIn terminal) and STILL returns 200; bad/missing URL secret → 401. Import target: the webhook route + `@/lib/orchestration/webhook` helper (Wave 1).
  - test/orch-reconcile.test.ts (ORCH-02): asserts GET /api/cron/reconcile is CRON_SECRET-gated (bad secret → 401) and polls getRunPodStatus ONLY for non-terminal jobs with a runpodJobId, applying the same mapping; AND asserts the stranded-job sweep: a non-terminal job with a NULL runpodJobId older than the threshold is released back to 'queued' for re-dispatch (W-1). Import target: `@/lib/orchestration/reconcile` + the cron route (Wave 2).
  - test/orch-retry.test.ts (ORCH-03): asserts a failed job with attempt < CAP is re-queued (status→queued, attempt+1, runpodJobId→null, error→null) and a completed/cancelled or attempt≥CAP job is NEVER re-queued. Import target: `@/lib/orchestration/retry` (Wave 2).
  - test/orch-cancel.test.ts (ORCH-05): asserts cancelBatch/cancelJob call requireSession first, IDOR-reject an unknown/not-owned id with no write, call cancelRunPod(runpodJobId) for cancelable jobs, set status cancelled (+ cancelRequestedAt as the audit timestamp per the immediate-cancel model, W-4), and KEEP completed jobs. CRITICALLY, also explicitly assert the cancelRunPod CALL SHAPE — that cancelRunPod POSTs to `/v2/{endpoint}/cancel/{runpodJobId}` (the RunPod cancel endpoint), not merely that the Server Action runs requireSession/IDOR — so 04-04 Task 1 (the cancelRunPod helper) has real call-shape coverage (W-3). Import target: `@/lib/orchestration/cancel` (Wave 2) and `@/lib/runpod` cancelRunPod (Wave 1, 04-04).
  - test/orch-progress.test.ts (ORCH-04): asserts groupBy status produces completed/failed/running/queued/total counts and the derived batch status mapping (all completed→completed; ≥1 running→running; all-terminal mixed→partly failed; all failed→failed; cancel→cancelling/cancelled). Import target: `@/lib/orchestration/batch-status` (Wave 3).
  - test/orch-db-only.test.ts (ORCH-02): source-text guard (mirror deny-default.test.ts readFileSync style) asserting NO `@/lib/runpod`, `submitRunPod`, `getRunPodStatus`, or `runpod` import string appears in `app/(app)/batches/page.tsx` or `app/(app)/batches/[id]/page.tsx`. Since those files don't exist yet, the test reads them defensively (skip-if-absent now, hard-fail once Wave 3 creates them) — write it so it FAILS only when the file exists AND imports runpod; today it is GREEN-by-vacuity but becomes the Wave 3 gate. Document this in a comment.
Use `mapRunPodStatus`/`TERMINAL_STATUSES` from status-map in the webhook/reconcile/retry tests so the shared contract is exercised. Per Nyquist: these files are the automated coverage referenced by 04-VALIDATION.md.
NEVER place fenced code blocks describing implementation in production files — the helper is small directive code only.
  </action>
  <verify>
    <automated>npx vitest run test/orch-dispatch.test.ts test/orch-webhook.test.ts test/orch-reconcile.test.ts test/orch-retry.test.ts test/orch-cancel.test.ts test/orch-progress.test.ts test/orch-db-only.test.ts --reporter=dot</automated>
  </verify>
  <done>status-map.ts exports mapRunPodStatus/TERMINAL_STATUSES/isTerminal/tail; all 7 test files exist; the status-map tests PASS; the dispatch/webhook/reconcile/retry/cancel/progress tests are RED (import not-yet-built modules) — expected at Wave 0. test/orch-cancel.test.ts asserts the cancelRunPod /v2/{endpoint}/cancel/{id} call shape (W-3); test/orch-dispatch.test.ts asserts the no-base-URL no-submit case (A5); test/orch-reconcile.test.ts asserts the null-runpodJobId stranded-job sweep (W-1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron → cron route | Untrusted caller could hit the cron URL; only the Bearer CRON_SECRET proves it is Vercel |
| RunPod → webhook route | Untrusted M2M POST could forge a job completion |
| Operator browser → Server Action | Untrusted batchId/jobId could target another operator's batch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | cron routes | mitigate | CRON_SECRET required in env (fails fast if absent); constant-time Bearer compare in Wave 1/2 routes |
| T-04-02 | Information Disclosure | env schema | mitigate | env stays typed/centralized; never log secret values; .env.example carries empty placeholders only |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (RESEARCH Package Legitimacy Audit: none added); slopcheck N/A |
</threat_model>

<verification>
- `npx prisma migrate status` reports the DB up to date with the additive migration applied (no reset, no dropped columns).
- `npx tsc --noEmit` exits 0.
- vercel.json parses as valid JSON and contains both cron paths; `npx next build` passes.
- The status-map unit tests pass; the six implementation-target tests are RED (modules not built yet).
</verification>

<success_criteria>
Schema migrated with four additive nullable fields (no existing column altered); CRON_SECRET typed/required and APP_URL optional; vercel.json schedules both cron routes; a single shared status-map module exists and is tested; all seven Phase 4 test files exist and encode the requirement contracts.
</success_criteria>

<output>
Create `.planning/phases/04-orchestration-status/04-01-SUMMARY.md` when done.
</output>
