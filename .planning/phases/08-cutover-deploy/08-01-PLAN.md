---
phase: 08-cutover-deploy
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/enterprise-app.tsx (DELETE)
  - app/studio.tsx (DELETE)
  - app/lab/page.tsx (DELETE)
  - app/rater/page.tsx (DELETE)
  - app/default-recipe.json (DELETE)
  - app/styles.css (DELETE)
  - app/api/rating-sweeps/route.ts (DELETE)
  - app/api/render-jobs/route.ts (DELETE)
  - app/api/render-jobs/[id]/route.ts (DELETE)
  - app/api/material-inspections/route.ts (DELETE)
  - app/api/config/route.ts (DELETE)
  - lib/jobs.ts (DELETE)
  - .planning/phases/08-cutover-deploy/DATA-05-DECISION.md (NEW)
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SEC-05, DATA-05, DEPLOY-01]
must_haves:
  truths:
    - "All legacy render surfaces (enterprise-app, studio, lab, rater) and their JSON/CSS deps are deleted; the enterprise app/(app)/ product is untouched"
    - "Legacy API routes (rating-sweeps, render-jobs, render-jobs/[id], material-inspections, config) are deleted; new app uses Server Actions + /api/file + /api/blob"
    - "lib/jobs.ts (legacy Vercel-Blob job store) is deleted — no live importer remains after the routes are gone"
    - "ring99 hardcoded model URL, the u6oaq5xqg2yrxzlq.public.blob URL, and the local-FS outputs/ring99/recipes read are all gone from the live tree (SEC-05)"
    - "DATA-05 is recorded as met-by-rationale: legacy job history was disposable ring99 R&D in a rotated public Blob store; clean-slate Postgres product has no catalog history to migrate"
    - "DEPLOY-01 verified: production URL responds healthy; required env-var presence confirmed (names only, no values)"
    - "npx tsc --noEmit exit 0, npx next build succeeds, npx vitest run green after the sweep"
  artifacts:
    - path: ".planning/phases/08-cutover-deploy/DATA-05-DECISION.md"
      provides: "DATA-05 no-migration decision record (met-by-rationale)"
      contains: "met-by-rationale"
    - path: ".planning/phases/08-cutover-deploy/08-01-SUMMARY.md"
      provides: "Phase 8 execution summary"
      contains: "SEC-05"
---

<objective>
Cutover and deploy — the FINAL phase of the milestone. Retire the legacy
single-purpose render surfaces ENTIRELY (their only job was rendering the
ring99 test model; they are not linked from the enterprise app/(app)/ product),
remove the ring99 hardcodes / public-Blob URL / local-FS fallback recipe path
(SEC-05), document the no-migration decision for the disposable legacy job
history (DATA-05, met-by-rationale), and verify the production Vercel deploy +
env-var presence (DEPLOY-01).

Purpose: ship a clean enterprise-only product with no dead legacy attack surface
and no committed/hardcoded asset references, and close the milestone at 8/8.
Output: 12 legacy files/dirs deleted, 1 decision doc, REQUIREMENTS/STATE/ROADMAP
updated, build+tsc+tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<read_first>
@app/page.tsx
@app/layout.tsx
@lib/jobs.ts
</read_first>

<verified_dependency_graph>
<!-- Verified by grep before planning: -->
<!-- styles.css       <- imported ONLY by enterprise-app.tsx (legacy) -->
<!-- default-recipe.json <- imported ONLY by studio.tsx (legacy) -->
<!-- /api/render-jobs, /api/material-inspections, /api/config, /api/rating-sweeps -->
<!--   <- fetched ONLY by enterprise-app.tsx, studio.tsx, rater/page.tsx (all legacy) -->
<!-- lib/jobs.ts       <- imported ONLY by the 4 legacy API routes -->
<!-- lib/types.ts      <- imported ONLY by legacy files; NOT in deletion scope, KEEP (harmless orphan, tsc green) -->
<!-- KEEP (shared/live): lib/runpod.ts, lib/enterprise-recipes.ts, app/api/{blob,file,auth,cron,webhooks,admin,batches}, app/(app)/ -->
<!-- SEC-05 hardcode targets all live in deleted files: -->
<!--   app/studio.tsx (ring99 recipe), app/api/rating-sweeps/route.ts:39 (public blob URL), :116 (outputs/ring99/recipes local-FS read) -->
<!-- Remaining ring99 strings are benign test fixtures (models/ring99.glb, outputs/ring99/job-abc.png) — NOT the SEC-05 hardcodes. -->
</verified_dependency_graph>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-05 — delete legacy surfaces + routes + lib/jobs.ts; verify green</name>
  <files>app/enterprise-app.tsx, app/studio.tsx, app/lab/, app/rater/, app/default-recipe.json, app/styles.css, app/api/rating-sweeps/, app/api/render-jobs/, app/api/material-inspections/, app/api/config/, lib/jobs.ts</files>
  <action>
    Delete the verified-unused legacy cluster (pages, JSON/CSS deps, API routes,
    legacy job store). VERIFY-BEFORE-DELETE: after the sweep, tsc --noEmit must be
    exit 0, next build must succeed, vitest run must be green. If any deletion breaks
    a LIVE import, restore it and report. Then grep the whole tree and confirm zero
    remaining references to ring99 (excluding benign test fixtures), the
    u6oaq5xqg2yrxzlq.public.blob URL, and the outputs/ring99/recipes local-FS read.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot && npx next build</automated>
  </verify>
  <done>Legacy cluster deleted; tsc/tests/build green; ring99/public-URL/local-FS refs gone from live code.</done>
</task>

<task type="auto">
  <name>Task 2: DATA-05 — record the no-migration decision (met-by-rationale)</name>
  <files>.planning/phases/08-cutover-deploy/DATA-05-DECISION.md</files>
  <action>
    Write DATA-05-DECISION.md: legacy job history was scratch ring99 R&D in an old
    public Blob store whose access was rotated away — not catalog work. The enterprise
    product is clean-slate (Postgres via Prisma); there is no production catalog history
    to migrate, so DATA-05 is satisfied by rationale with no silent loss. Note the
    hypothetical future path (idempotent cursor-paginated backfill into a legacy-import
    Batch) is NOT built now.
  </action>
  <done>DATA-05-DECISION.md written; marked met-by-rationale.</done>
</task>

<task type="auto">
  <name>Task 3: DEPLOY-01 — verify production deploy + env-var presence</name>
  <files>(verification only; no source change)</files>
  <action>
    Confirm https://jewelry-render-studio.vercel.app/ responds healthy (200 on /login
    or 307 redirect to /login from /). Confirm required env vars are configured via
    `vercel env ls production` if the CLI is authed; otherwise note unverifiable and
    infer presence from the live app booting + auth working. Required: DATABASE_URL,
    DIRECT_URL, AUTH_SECRET, RUNPOD_API_KEY, RUNPOD_ENDPOINT_ID, BLOB_READ_WRITE_TOKEN,
    RUNPOD_WEBHOOK_SECRET, CRON_SECRET, APP_URL. NEVER print secret values — names/presence only.
  </action>
  <done>Production URL health + env-var presence reported (names only).</done>
</task>

<task type="auto">
  <name>Task 4: Close-out — mark requirements Complete, update STATE/ROADMAP, write SUMMARY</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/08-cutover-deploy/08-01-SUMMARY.md</files>
  <action>
    Mark SEC-05, DATA-05, DEPLOY-01 [x] and their traceability rows Complete in
    REQUIREMENTS.md. Update STATE.md (8/8 phases, 100%, milestone status) and
    ROADMAP.md (Phase 8 Complete). Write 08-01-SUMMARY.md.
  </action>
  <done>All three requirements Complete; STATE/ROADMAP at 8/8; SUMMARY written.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit exit 0; npx vitest run green; npx next build succeeds.
- grep confirms ring99/public-URL/local-FS hardcodes gone from live source.
- DATA-05 decision recorded; DEPLOY-01 env presence reported (names only).
- SEC-05/DATA-05/DEPLOY-01 + traceability rows Complete; STATE/ROADMAP 8/8.
</verification>

<success_criteria>
The milestone closes clean: legacy render surfaces fully retired with no dead
attack surface, SEC-05 hardcodes removed, DATA-05 no-migration decision recorded,
and DEPLOY-01 production deploy + env vars verified — all with tsc/tests/build green.
</success_criteria>

<output>
Create `.planning/phases/08-cutover-deploy/08-01-SUMMARY.md` when done.
</output>
